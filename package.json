{
  "name": "figma-edit-mcp",
  "description": "Figma Edit MCP - Connect AI coding assistants to Figma via Model Context Protocol",
  "version": "0.3.5",
  "module": "dist/server.js",
  "main": "dist/server.js",
  "bin": {
    "figma-edit-mcp": "dist/server.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "scripts": {
    "test": "bun test src/mcp_server/tests",
    "start": "bun run dist/server.js",
    "socket": "bun run src/socket.ts",
    "setup": "./scripts/setup.sh",
    "integrate": "./scripts/integrate.sh",
    "build": "tsup",
    "build:watch": "tsup --watch",
    "dev": "bun run build:watch",
    "plugin:build": "node src/figma_plugin/build.js",
    "plugin:watch": "node src/figma_plugin/build.js --watch",
    "build:all": "npm run build && npm run plugin:build",
    "pub:release": "bun run build && npm publish"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.123.0",
    "@types/bun": "latest",
    "bun-types": "^1.2.5",
    "esbuild": "^0.27.2",
    "ts-morph": "^27.0.2",
    "tsup": "^8.4.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.13.1",
    "uuid": "latest",
    "ws": "latest",
    "zod": "3.22.4"
  }
}

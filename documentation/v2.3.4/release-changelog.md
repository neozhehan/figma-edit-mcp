# v2.3.4 Release Changelog

This document centralizes the decision and PRD revision history for [v2.3.4](prd.md).

## Change 1: PRD revision history

> The entries below were moved from [`prd.md`](prd.md) without renumbering or reordering. Decision identifiers and requirement references refer to that PRD.

- **Rev 7, 2026-07-30** — corrects the Change 16 follow-up audit and incorporates the independent `b05y` live matrix. The mechanically central-backed Track 1 set is **47**, not 49: 26 direct `ERRORS` operands, 15 `formatScopeError(ERRORS...)` operands, and six `SCOPE_DELETED` templates; two other `formatScopeError` calls take handler-local strings and remain in the **266** ad-hoc sites. The current problem statement now also matches the already-corrected 29-definition registry shape (12 legacy strings + 17 factories, 11 legacy strings referenced). For Track 3, D10 keeps exact recursive comparison as its baseline and admits only two composable read-back normalizations: synthesized deprecated `action`, and one atomic NODE-action rewrite from owned `preserveScrollPosition:false` with no modern reset fields to exactly `resetScrollPosition:true` plus `resetVideoPosition:false`. It retains `compatibilityActionIndexes`, adds deterministic `nodeActionNormalizationPaths`, returns both fields for no-op and state-changing success, and keeps raw authoritative state/token unchanged. Phase, tests, rollout, risks, and provenance now cover exact composition, nested conditionals, deterministic path disclosure, rejection of every partial or broader delta, the `b05y` missing-root coverage evidence, the nested setter failures with verified non-application, and the successful top-level normalization/cleanup. Rev 6 and earlier remain unchanged as historical records superseded where this entry disagrees.
- **Rev 6, 2026-07-30** — corrects two measured baselines found wrong by the adversarial review of v2.3.3 Change 15. The central-table overlap was **43, and is 49**: the original method summed only the literal prefixes `throw new Error(ERRORS` (26) and `throw new Error(formatScopeError` (17), missing six sites that interpolate `ERRORS.SCOPE_DELETED` into a template literal. Those six are not an edge case for D2 — their appended operand is exactly the factory argument the conversion introduces — so they belong in Phase 2's mechanical batch. D2, scope item 2, Phase 2 (ratchet 270 → **264**), and the provenance row are corrected; the 313-site total and the per-file table were independently reproduced and are unchanged. The `ERRORS` table row is also re-measured: **29 keys (12 legacy + 17 `REFUSALS`)**, not the 2026-07-18 reading of 28 (13 + 15), which predates the Rev 27 migration, Change 8's two additions, and Rev 73's removal of `CONNECTOR_TEMPLATE_REQUIRED`; 11 of the 12 legacy keys are referenced, and dead `INVALID_TARGET_NODE_IDS` is marked delete-don't-convert. No decision changes; these are measurement corrections to numbers D2/D6 and Phase 2 depend on.
- **Rev 5, 2026-07-30** — records and closes the PRD gaps exposed by `mksu` plus adversarial review. An official-valid modern reaction was rejected once with its arbitrary thrown diagnostic collapsed to `undefined`, then the identical actions-only payload succeeded and read back with Figma's synthesized matching deprecated `action`. D10 now defines exact JSON-safe setter reports, guarded one-time arbitrary-throw capture for both synchronous invocation throws and asynchronous rejection, a 20-second internal timeout with late-mutation disclosure, narrow setter equivalence with raw authoritative output/token, disjoint zero-setter no-op success, exact migration/compatibility fields, non-coercible safe-integer indexes, machine-readable invalid-index ranges, and exact public success fields. D9 maps every coverage phase to a reviewed code, defines the ID-bearing path and deterministic result/coverage ordering, and adds `REACTION_STATE_UNENCODABLE`; T3 therefore owns six codes. The final recheck preserves the existing safety matrix rather than silently adding an instance-interior refusal. Phase/tests/live/risk/provenance sections cover these contracts and preserve the first failed attempt's unknown mutation outcome rather than inferring failure from rejection.
- **Rev 4, 2026-07-30** — adversarially tightened Track 3 against the official Reaction write contract. `SET_TRIGGER` is non-null; the exact `op` wire union, normalized success operation/counts, reaction-mixin/PAGE rows, document-relative depth, full coverage-error shape, supported canonical-token domain, and unsupported-value behavior are explicit. Adds one-object final synchronous TOCTOU revalidation; coherent legacy `action`→`actions` migration on only the modified reaction; `REACTION_STATE_NOT_WRITABLE`; one-round mismatch recovery from `currentNode`; and the complete setter resolve/reject × intended/pre-state/other/unreadable reconciliation matrix. Corrects D1's generic setter rule and D2's rebased 43-site count, specifies the outer/nested/lazy Zod schema structure and exact issue-path fixtures, scales the mandatory live matrix to authorable core reactions, and records that the five reaction-related declaration families are unchanged from typings 1.125.0 to 1.131.0.
- **Rev 3, 2026-07-30** — added **Track 3: lossless prototype-reaction reads and state-checked localized updates**, following v2.3.3 Rev 73's removal of `create_connection` and `reaction_to_connector_strategy`. Records the measured `reaction_list` `CHANGE_TO`/coverage/deduplication defects, `reaction_update` whole-array stale-overwrite risk, and schema drift from Figma's Reaction unions; decisions **D9–D11** define lossless deterministic reads, exact canonical state tokens, six one-operation writes, authoritative read-back, four reaction codes, setter-failure disclosure, strict-write/open-read separation, and 1.131.0 parity. Adds scope items 10–12, Track 3 non-goals, **Phase T3**, unit/boundary/live matrices, residual compare-to-set race disclosure, five risks, and five provenance rows. Rebaselines Track 1 after v2.3.3 Phase 11 from the historical 332/51 inventory to **313 inline throws across 15 files, 43 central-table-backed**, removes the deleted connector handler from phase ownership, and updates the title/release identity from two tracks to three.
- **Rev 2, 2026-07-26** — added **Track 2: the `@figma/plugin-typings` bump (1.125.0 → 1.131.0)**, accepted from the v2.3.3 Q35 residual. Records the measured facts (pinned vs. published version; `ShaderEffect` added in 1.131.0 and absent from 1.125.0; the live runtime already accepting `SHADER`; `noiseSizeVector` as the only other Effect-variant delta; **0 `tsc` errors** against the newer typings; the Q35 parity test correctly going red on the bump), the reason for deferral out of v2.3.3 (the typings-generated `nodeFields.generated.ts` changes what `node_info` can read, and that diff deserves its own review), decision **D8**, scope item 9, non-goals for the escape hatch and dependency sweep, **Phase T2** (labelled outside the numbered Track 1 sequence — it has no ordering dependency, and a decimal phase already means unplanned post-verification work in this repo), three risk rows, six provenance rows, and the testing additions. The release title and identity block now describe two tracks. Standing rule recorded in D8: future Figma variants are adopted by bumping the pin and letting the parity test fail — not by loosening the schema.
- **Rev 1, 2026-07-18** — initial PRD, created on the v2.3.3 Q16 resolution (Option A). Records the measured legacy surface (332 inline sites, 51 central-table-backed, 102 prose assertions, the `channel.ts` envelope, the UI-relay flattening), decisions D1–D7 (cause-level taxonomy, registry-only origination, v2.3.3-D9 message quality, playbook parity gate, code-based tests, no-control-flow-change with a ratchet gate, boundary unification), one open question (Q1 — `channel.ts` convergence, recommendation Option A), and the ten-phase plan gated on v2.3.3 merging first.

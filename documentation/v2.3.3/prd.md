# v2.3.3 PRD: Plugin Type-Check Restoration & Safety-Contract Gap Closure

This document is the product / implementation spec for the **v2.3.3** release of `figma-edit-mcp`. It follows v2.3.2 (Safety Contract Conformance & Atomicity Hardening) and has two tracks:

1. **Type-check restoration (developer infrastructure).** Restore TypeScript type-checking for the Figma plugin source, which is currently silently disabled by a misconfigured `figma_plugin/tsconfig.json`, and add a CI gate so it cannot regress.
2. **Safety-contract gap closure.** Close the three code-level gaps confirmed by the adversarial review of [`figma-edit-mcp-gap-details.md`](figma-edit-mcp-gap-details.md) — design-system updates that skip current-name verification (Gap 1), create-tool schemas that contradict the runtime parent-name requirement (Gap 3), and a batch delete that reports success on partial failure (Gap 5) — plus the additional findings of the 2026-07-10 follow-up review ([`revised-prd.md`](revised-prd.md), live channel `pn68`) that were independently re-verified against the source for this revision: an unusable `annotation_set` contract (Gap 6), batch-contract corrections (Gap 7), implicit-parent containment violations (Gap 8), an unbound plugin/server channel (Gap 10), and string-flattened errors with all-or-nothing page loads (Gap 11). Gaps 2 and 4 remain documentation-only (handled by the README rewording already in the working tree). Gap 9 (connectors) and the follow-up review's heavier prescriptions are **deferred pending explicit decisions** — see the non-goals.

> **Origin.** Found during the v2.3.2 Phase 8.5 review (2026-07-06). While adding a `clearTimeout` to `createComponentInstance`, the IDE flagged `setTimeout`/`clearTimeout` as `Cannot find name`. Investigation showed this is **not local** — *no* plugin ambient global (`figma`, `console`, `setTimeout`, node types) resolves under `tsc`, because the plugin tsconfig never loads `@figma/plugin-typings`. The plugin ships via **esbuild** (which does not type-check), so the whole plugin has been shipping **without a type-safety net** and with plugin-wide spurious IDE errors. This release closes that gap.

---

## Release identity

> [!IMPORTANT]
> **This is v2.3.3.** It grants **no new editing powers** and adds no new tools. Track 1 is build/tooling + type-hygiene only, with no runtime behavior change. Track 2 **tightens and repairs** the existing safety contract: previously optional verification fields become required (`currentVariableName`; new `currentStyleName` and `collectionName`; `parentNodeName` at the schema level, now including `create_component_set`); batch success semantics are corrected; the advertised-but-unusable `annotation_set` contract is restored to working order (a behavior fix, not a new permission — called out because callers will observe new successful behavior); input strictness becomes recursive; creations are contained to their verified parents; the channel binds one plugin peer to one MCP session; and plugin errors travel as structured codes. Agents that omitted required fields will now receive structured `Operation Denied` errors instead of unverified writes — an intended fail-closed change, following the v2.2.0 precedent of universalizing name verification. It depends on v2.3.2 being merged first (v2.3.2 is in progress at authoring time).
>
> Version surfaces to bump follow the v2.3.2 mechanism (`package.json`, root `package-lock.json`, both `server.json` fields, root `manifest.json`, and the plugin About handshake — enforced by `check:versions` and `check:plugin`): `2.3.2 → 2.3.3`.
>
> **Versioning policy (open-questions Q5, resolved 2026-07-09: Option A).** The release stays numbered **2.3.3**, and the rule is recorded as policy rather than precedent: **fail-closed safety tightenings may ship at patch level.** The project versions the safety contract's strength; making a guarantee stricter is a fix, and the `CHANGELOG.md` entry carries the breaking-change notice for agent callers. Agents do not reason from version numbers — the guides they load at runtime ship in the same artifact as the schemas, so guide/server consistency is independent of the number chosen. Revisit only if external integrators are known to pin versions.
>
> **Release sizing (open-questions Q15, resolved 2026-07-10: Option C).** v2.3.3 ships as a **single release containing every phase of this PRD** — both tracks and all of D5–D14. The Option A split (model-facing repairs first, execution-side guarantees in a later v2.4.0) was declined: one release cycle, one contract-doc/guide sync pass, no renumbering churn, and Q5 policy continuity. The reviewability risk this concentrates is carried by the mitigations already in the risk table (per-phase commits in dependency order under the Phase 3 type gate; rebuild diffs).

---

## The problem

**The bug.** `figma_plugin/tsconfig.json` declares:

```json
"typeRoots": ["../../node_modules/@figma"]
```

The tsconfig lives at `figma_plugin/tsconfig.json`, so `../../node_modules` resolves to the **parent of the repository** (`<repo>/../node_modules`), which does not exist. The repo's actual dependency tree is at `<repo>/node_modules` (one level up from `figma_plugin/`, i.e. `../node_modules`). As a result `@figma/plugin-typings` is **never loaded**, and every plugin ambient global is unresolved.

**Consequences.**

- **No type safety on the most safety-critical code in the project.** The plugin dispatcher (`figma_plugin/src/main.ts`) and handlers enforce the entire safety contract (scope, name verification, locked/instance guards, batch atomicity). Today a wrong Figma API call, a typo'd property, or a bad narrowing in that code is caught only at runtime (or live testing), never by `tsc`.
- **Plugin-wide spurious IDE errors.** `tsserver` reports the same unresolved globals, so contributors see red squiggles on `figma`, `console`, `setTimeout`, etc. across every plugin file — training the team to ignore IDE diagnostics on exactly the code where they matter most.
- **It is invisible to CI.** The plugin is bundled with **esbuild** (`figma_plugin/build.js`), which performs no type-checking, and there is **no `tsc` step** for the plugin anywhere in `package.json`, `figma_plugin/build.js`, or `.github/workflows/ci.yml`. So nothing surfaces the breakage.

**Not a runtime bug.** The shipped plugin works — the globals (`figma`, `console`, `setTimeout`, `clearTimeout`, `TextDecoder`, …) all exist in the Figma plugin sandbox at runtime, and esbuild bundles the source unchanged. This is a **lost safety net**, not a live defect. The v2.3.2 live testing confirms the affected code paths behave correctly.

---

## Evidence (empirical, 2026-07-06)

Ground-truth measured against the real plugin source (`bunx tsc --noEmit` with probe configs):

| Config | `tsc` errors | Interpretation |
| :- | :-: | :- |
| **Current** (`typeRoots: ["../../node_modules/@figma"]`) | ~309 | @figma typings never load; every `figma`/`console`/`setTimeout`/type-name unresolved |
| **Naive path fix** (`typeRoots: ["../node_modules/@figma"]`) | **309** | *Still broken.* Correcting the path alone does **not** load the typings — the one-line fix a maintainer would try first is insufficient |
| `types: ["@figma/plugin-typings"]` **+ `lib: [es2018, dom]`** | 14 | Types load, but **`dom` conflicts** with the Figma typings — duplicate/redeclared `console`, `fetch`, `Navigation` (TS2451/TS2300). `dom` is the wrong lib |
| `types: ["@figma/plugin-typings"]`, no `dom`, before the shared source is an explicit root | **9** | Intermediate probe: 4 `never` narrowings, 1 `loadAsync`, 2 broken shared-import paths, and 2 `TextDecoder` errors |
| **`types: ["@figma/plugin-typings"]`, no `dom`, exact imported shared file included** *(decided fix)* | **10** | The explicit `src/shared/nodeTypes.ts` root exposes one additional TS2554 in that file; all 10 genuine gaps are triaged below |

**Key insight:** the fix is *not* the `typeRoots` path. `@figma/plugin-typings` must be loaded via **`types: ["@figma/plugin-typings"]`**, and the `dom` lib must **not** be added (the Figma typings supply `console`/`fetch`/timers/`Navigation` themselves and collide with `dom`).

---

## Decisions

> [!NOTE]
> **D1 — Load the Figma typings via `types`, not `typeRoots`; do not add `dom`.** Replace the broken `typeRoots` with `"types": ["@figma/plugin-typings"]`. Keep `lib: ["es2018"]` (no `dom` — it redeclares globals the Figma typings own). With the exact imported shared file included, this configuration loads the typings and reports the 10 residual errors triaged below.

> [!NOTE]
> **D2 — Fix the residual 10, do not suppress them.** The 10 remaining errors are real type gaps, not noise. Fix each at the source (narrowing, casts, module resolution, the shared schema signature, and one ambient global) rather than blanket-suppressing with `// @ts-nocheck` or loosening `strict`. `strict` stays on. See the triage table.

> [!NOTE]
> **D3 — Add a CI type-check gate, but only after the residual is zero.** Add `check:types:plugin` (`tsc --noEmit -p figma_plugin/tsconfig.json`) to `package.json` and wire it into CI so this regression cannot recur. It must be added **only after D1/D2 bring the error count to 0** — or CI goes red on introduction. The sequencing is by condition, not position: in the phased plan the gate lands as Phase 3, ahead of the Track 2 handler edits (Phases 4–11), so those land under its protection *(wording clarified per open-questions Q6, resolved 2026-07-09: Option A — the original "added last" predates Track 2)*. The plugin build stays esbuild (fast, no type-check); the gate is a separate, additive check.

> [!NOTE]
> **D4 — Scope guard: plugin program only.** This release fixes the plugin tsconfig and the source files in that TypeScript program, including the imported shared type module. It does **not** re-audit the MCP-server tsconfig (`./tsconfig.json`), refactor beyond the 10 residual fixes and the separately ratified Rev 20 suppression-hygiene cleanup, or change runtime behavior in Track 1. If fixing a residual error would require a behavior change, stop and escalate rather than silently altering a live-verified code path.

---

## Residual error triage (the 10)

All measured with the decided fix (`types: ["@figma/plugin-typings"]`, no `dom`). **None is a confirmed runtime bug** — each is a type-expression gap that `tsc` correctly flags once it can finally see the code.

| # | Location | Error | Class | Disposition |
| :- | :- | :- | :- | :- |
| 1–2 | `handlers/componentHandlers.ts:707` (`.name`, `.type`) | TS2339 `Property … does not exist on type 'never'` | Narrowing artifact after `if (!("appendChild" in parentNode))` | Type `parentNode` explicitly (e.g. `BaseNode`) so the negative branch narrows to a real type, not `never`. Runtime is correct (live-verified). |
| 3–4 | `src/main.ts:194` (`.name`, `.type`) | TS2339 `… on type 'never'` | Same narrowing artifact in `validateCloneWrite`'s `!("appendChild" in parent)` branch | Same fix (explicit `BaseNode` typing / cast). |
| 5 | `handlers/connectHandlers.ts:42` | TS2339 `Property 'loadAsync' does not exist on type 'BaseNode'` | Invariant not expressed in types (`allowEditNode === "page"` ⟹ `scopeNode` is a `PageNode`) | Narrow/cast to `PageNode` before `loadAsync()`. No behavior change. |
| 6–7 | `handlers/nodeReaders.ts:8`, `utils/nodeUtils.ts:6` | TS2307 `Cannot find module '../../shared/nodeTypes.js'` | Broken relative paths — no repo-root `shared/` has ever existed; esbuild did not validate them because the imports are type-only and elided *(corrected, Rev 22)* | Rewrite both imports to `../../src/shared/nodeTypes.js` and include only the imported `../src/shared/nodeTypes.ts` file as an explicit plugin type-check root. Emitted-JS-identical because the imports remain type-only. |
| 8–9 | `utils/exportUtils.ts:124–125` | TS2304 `Cannot find name 'TextDecoder'` | Sandbox global not declared by `@figma/plugin-typings` or `es2018` | Add a minimal ambient declaration for `TextDecoder` (the Figma sandbox provides it at runtime). Do **not** pull in `dom`. |
| 10 | `../src/shared/nodeTypes.ts:40` | TS2554 `Expected 2–3 arguments, but got 1` | The exact shared include exposes a Zod 4 single-argument `z.record` signature | Use `z.record(z.string(), z.unknown())`. Runtime-equivalent under the pinned Zod version; `NodeEntrySchema` has no runtime importers. |

> [!NOTE]
> The two `never`-narrowing sites (#1–4) are in code **added and live-verified during v2.3.2** (parent-first creation cleanup, clone validation). Type-checking flags them as loosely typed, not as broken — a good demonstration of what the restored gate buys: it would have flagged these at author time.

---

## Track 2 — Safety-contract gap closure (Gaps 1, 3, 5)

> **Origin.** The adversarial review [`figma-edit-mcp-gap-details.md`](figma-edit-mcp-gap-details.md) identified five gaps between the documented safety contract and the shipped behavior. All five were verified against source and **live** against a Figma test document (2026-07-09). Gaps 2 and 4 need only wording changes (in flight in the working tree). A follow-up adversarial review (2026-07-10, [`revised-prd.md`](revised-prd.md), live channel `pn68`) exposed further contract failures; every finding incorporated below was independently re-verified against the source for this revision (locations in Provenance). The follow-up's heavier prescriptions — batch effect-state algebra and content digests, JCS/SHA-256 payload and protocol digests, page-scan scheduler constants, the connector redesign, and the Q1 reversal to discriminated unions — are **not** adopted here and remain open for explicit decision.
>
> **Open questions.** All eight questions raised by the PRD re-review are **resolved** (2026-07-09); [`open-questions.md`](open-questions.md) is retained as the decision record with options and rationale. Where each decision lives: Q1 schema mechanism → D5; Q2 Gap 3 verification split → D6 / Phase 9; Q3 G2 target wording → D8 / Phase 7; Q4 refusal-message convention → D9; Q5 versioning policy → Release identity block; Q6 D3 sequencing wording → applied to D3; Q7 `status` edge semantics → D7 / Phase 6; Q8 hard cutover → Compatibility posture. *(Rev 11 amends the Q7 record: its "zero-item batches are unreachable" premise was factually wrong — see D7.)* **Reopened 2026-07-10:** the follow-up review's deferred prescriptions are tracked as **Q9–Q15** with options and recommendations. **Q9–Q15 are resolved** (Q9: Option B — minimal per-row partial-mutation disclosure in D7/Phase 6, algebra and content digests rejected; Q10: Option B — D10's counts + list-before-retry confirmed, payload digests rejected; Q11: Option B — D13 lite confirmed with an evidence-based escalation trigger, digests/nonces/fingerprints rejected, scope-ready race verification added to Phase 9; Q12: Option B — D14 lite plus one bounded per-page timeout, scheduler constants and heartbeats rejected; Q13: revised hybrid — creation-only connectors with a required name-verified template on every call, cache and auto-adoption removed, Design support kept and FigJam-only rejected, recorded in D12/Phase 11; Q14: Option A — the Q1 mechanism reaffirmed, the discriminated-union reversal rejected, CI checks adapted to `safeParse` + description markers, recorded in D5; Q15: Option C — a single v2.3.3 release containing every phase of this PRD, recorded in the Release identity block). **All fifteen questions are resolved; nothing blocks implementation.** **Reopened 2026-07-17:** the Phase 3–4 implementation review raised **Q16–Q19**. All four are resolved 2026-07-18: Q16 (error-code inventory → D9 code-inventory note, Rev 23), Q17 (pre-check scope → D5, Rev 24), Q18 (partial-disclosure shape → D5/D7, Rev 25), and Q19 (TEXT-style font-loading order → D5, Rev 26). **Reopened 2026-07-18** by the follow-up implementation review ([reviews/Phase-3-&-4-review.md](reviews/Phase-3-&-4-review.md)), which raised **Q20–Q21**: Q20 (`channel_join` pass-through vs. the v2.3.4 Q1 deferral → D9, Rev 28) and Q21 (dual-description wording → D5's Q14 sentence corrected in place, Rev 29) are resolved 2026-07-18. **All twenty-one questions are resolved; the review's remaining findings are remediation work under recorded decisions, and Phase 4 closes when they land.** **Reopened 2026-07-18** by the Phase 5–6 implementation review ([reviews/Phase-5-&-6-review.md](reviews/Phase-5-%26-6-review.md)), which raised **Q22–Q26**. Q22 (parent-name refusal code regime → D6 and the D9 inventory, Rev 31) is resolved 2026-07-18: Option A — the coded `PARENT_NAME_MISSING`/`PARENT_NAME_MISMATCH` pair. Q23 (duplicate-target rejection layer → D7, Rev 32) is resolved 2026-07-18: Option B — Layer 1 schema `.superRefine()` rejection, no new code, the plugin check retained as uncoded defense in depth. Q24 (text partial-mutation before-value → D7, Rev 33) is resolved 2026-07-18: Option A — the text row carries `before: {fontName}` and `whatChanged`, the flag set only when the font fallback applied, superseding Q9's `characters` before-value. Q25 (batch row vocabulary → D7, Rev 34) is resolved 2026-07-18: Option A — all four aggregators' per-item rows converge on `nodeId`/`status`/`error`, renaming the instance rows. Q26 (legacy count aliases → D7, Rev 35) is resolved 2026-07-18: Option B — the batch handlers drop every tool-specific duplicate count in favor of the shared envelope counts (no marked aliases). **All of Q22–Q26 are resolved; the Phase 5–6 review's remaining findings are remediation work under recorded decisions, and Phases 5–6 close when they land.**

### The problems (all verified in code and live)

**Gap 1 — design-system updates skip right-object verification.** Node writes require ID + current name; variable and style *updates* do not.

- `variable_manage` / `UPDATE_VARIABLE` checks `currentVariableName` **only when supplied** (`figma_plugin/handlers/variableHandlers.ts:1032`); the schema marks it optional (`src/mcp_server/tools/variable.ts:115`). Live-verified: an ID-only value update mutates the variable with no verification; a wrong name is refused only because it was volunteered.
- `style_manage` with `styleId` has **no** current-name verification at all (`figma_plugin/handlers/styleHandlers.ts:13–23`), and its **required** `name` param is applied as the *new* name on every update (`styleHandlers.ts:47`). Live-verified: an update against a stale/mistaken ID both mutates the wrong style **and renames it** to the caller's wrong belief — destroying the evidence of what it was called.
- `variable_manage` / `CREATE_VARIABLE` accepts `collectionId` with no `collectionName` verification (`variableHandlers.ts:963`), unlike `variable_delete`, which requires and verifies it.
- `SAFETY.md` **G2** claims name verification covers "every write tool" — contradicted by its own Part B matrix rows for `variable_manage` and `style_manage`, and by the live behavior above.
- `UPDATE_VARIABLE` mutates before it finishes validating: `name`, `description`, and `scopes` are applied (`variableHandlers.ts:1040–1051`) before a missing `modeId` for a supplied `value` is discovered (`:1053`) — a predictable late failure leaves a half-updated global token. The style path has the same shape: `name`/properties are applied before later variable bindings resolve, and the rollback at `styleHandlers.ts:130–135` removes only freshly *created* styles; it never restores an updated one.
- `CREATE_VARIABLE` accepts omitted `scopes` (`if (scopes !== undefined)`, `variableHandlers.ts:990`), although the tool description says scopes must always be set explicitly on create — an underspecified global asset is created silently.
- The verification error at `variableHandlers.ts:1033` labels the stored name "Expected" and the caller's value "got" — arguable rather than wrong, but recovery should not depend on interpreting operand order.

**Gap 3 — create-tool schemas contradict the runtime contract.** `parentNodeName` is `.optional()` in the `create_shape`/`create_frame`/`create_text`/`create_svg`/`create_instance` schemas (`src/mcp_server/tools/create.ts`), while the runtime fails closed when it is absent (`verifyParentName`, `figma_plugin/src/main.ts:151`) and `SAFETY.md` A3/A4 already document it as required. Live-verified: omitting it is refused with `PARENT_NAME_MISMATCH` — a safe but avoidable failure, made worse by the error text ("recheck to ensure correct parentId is passed in"), which misdirects the agent toward the parent *ID* when the parent *name* was simply missing.

**Gap 5 — `node_delete` reports success on partial failure.** `deleteMultipleNodes` returns `success: successCount > 0` (`figma_plugin/handlers/nodeModifiers.ts:294`). Live-verified: a batch that deleted 1 of 2 items returned `{"success":true,"nodesDeleted":1,"nodesFailed":1}`. The other three batch aggregators (`text_set_content`, `annotation_set`, `instance_set_overrides`) already require `failureCount === 0`.

**Gap 6 — `annotation_set`'s advertised contract cannot reach its handler's success path.** The registered item schema (`src/mcp_server/tools/annotation.ts:42–49`) requires `categoryId`, exposes `annotationId` and `status`, shapes `properties` as a record — and omits `labelMarkdown`, the one field the handler requires (`annotationHandlers.ts:142–144`). The handler ignores `annotationId`/`status` entirely, and Figma's pinned `Annotation` type supports neither. Because strictness is not recursive (Gap 7), a caller who supplies `labelMarkdown` anyway has it silently stripped before dispatch. Net: **no conforming MCP call can ever create an annotation** — the tool is advertised, marked `idempotentHint: true` (it appends; append is not idempotent), and unusable. The read side drifts too: `annotation_list`'s registered output advertises `annotations` while the page-mode handler returns `annotatedNodes` (`annotationHandlers.ts:48`), masked by `looseOutput`.

**Gap 7 — the batch contract has holes the Q7 resolution missed.** Corrections to Rev 9, all verified in code:

- **Empty batches pass the schemas.** No batch array has `.min(1)`; `[]` is accepted at the boundary and produces a different ad-hoc failure shape per tool. Rev 9's claim that "zero-item batches are unreachable through the schemas" was **false**.
- **Stop-on-first omits rows.** `setMultipleTextContents` `break`s on first failure (`textHandlers.ts:108–169`) without emitting rows for unattempted items — a caller cannot distinguish "failed" from "never attempted".
- **Duplicate targets are accepted.** The live duplicate-delete probe (4n79, 2026-07-09) passed prevalidation twice for the same node and manufactured a spurious failure row.
- **Dispatcher prevalidation throws.** Per-item prevalidation failures are thrown errors, not envelopes, so "`status` on every return path" cannot be delivered by the aggregators alone — the contract must define its layers explicitly.
- **Output schemas drift from handlers.** `node_delete` advertises `deletedCount` (`node.ts:181`) but returns `nodesDeleted`/`nodesFailed`; `text_set_content` advertises `count` (`text.ts:25`) but returns `replacementsApplied`/`replacementsFailed`. `looseOutput` hides the drift.
- **Strictness is shallow.** The central wrapper's `.strict()` applies only to the top-level object (`tools/index.ts:31–40`); nested objects silently strip unknown keys — exactly the intent loss the wrapper's own comment says it exists to prevent.

**Gap 8 — implicit-parent Figma APIs place nodes outside the verified destination.** `node_flatten` calls `figma.flatten([node])` with no parent (`nodeModifiers.ts:505`); the API defaults the result to `figma.currentPage`, so a nested flatten jumps to page root — outside the editable subtree in node scope. `create_component_set` combines on the containing page and reparents only afterward (`componentHandlers.ts:962, 984–985`), so a no-parent call leaves the set at page level. The ordinary creators construct on `currentPage` first and append later — `create_shape` creates at `nodeCreators.ts:74–96` and appends at `:146` with fallible setters in between, and `create_text` creates (`:363`), **awaits fonts** (`:369`), and only then appends (`:396`), sitting observably outside its destination across an async boundary. Clone paths append only after `clone()` (`:454`), which likewise defaults to `currentPage`.

**Gap 9 (connectors) — verified 2026-07-10 (source + live channel `90vr`); the product decision remains open (Q13).** All five reported defects are confirmed: the default-template auto-adoption scans `figma.currentPage` with no scope check (`connectorHandlers.ts:245`, and silently again inside `createConnections` at `:461`); the default is cached in plugin-wide `clientStorage` under a key with no document scoping (`:199, :253, :457`), so a stale ID can resolve to an arbitrary same-ID node in another file (only the type check stands between that and a wrong template); `connectorId` has no name-verification field in schema or handler (existence + type only, `:188–196`); per-item failures do not clean up — a cursor created for a start endpoint leaks if the end endpoint fails (`:493–513`), a failed SVG-cursor append leaks the import (`:349, :398–401`), and the catch path removes nothing (`:603–621`); and the aggregate returns `success: true` unconditionally with `count = results.length`, which counts **error rows as connections** (`:635–639`). Live on `90vr` (Design file): the no-default flow, the type-only template validation, and the bootstrap dead-end ("copy a connector from FigJam manually") all reproduced; clone/cursor leak and mixed-result probes are blocked pending a FigJam-pasted connector fixture and remain source-verified only.

**Gap 10 — nothing binds a command to the plugin peer that will execute it.** The socket broadcasts every message to all other peers in the channel (`src/socket.ts:139–155`); joins carry no client type or peer identity; the join payload carries no plugin version (verified live on 4n79 — `channel_join` returns none); and `resetChannel()` clears only a local variable without leaving the socket room (`figma-client.ts:295–297`). With two plugins in one channel, one can answer the handshake and a different one can execute a later write, and neither the server nor the user can tell.

**Gap 11 — errors are flattened to strings and page loads are all-or-nothing.** The dispatcher catch converts every plugin failure to a string (`describeError`, `main.ts:319–324`) and `figma-client.ts` reconstructs plain `Error`s, so stable failure codes cannot survive to the MCP boundary. Multi-page reads have no per-page recovery: `getPagesInfo` awaits `loadAsync()` per page inside its loop (`nodeReaders.ts`), so one unloadable page fails the entire command with Figma's raw error. Destructive document-wide scans (`variable_delete`) currently fail closed only *by accident* — a page-load throw happens to abort the command — which must become an explicit, tested contract rather than a side effect.

### Decisions

> [!NOTE]
> **D5 — Bring design-system updates up to the node-write standard (Gap 1).** Fail closed on a missing or mismatched current name, exactly like `verifyNodeName`:
> - `UPDATE_VARIABLE`: `currentVariableName` becomes **required** — in the `variable_manage` schema and in the handler (absent ⇒ reject, mismatch ⇒ reject, before any mutation).
> - `CREATE_VARIABLE`: new **required** `collectionName`, verified against the resolved collection's name before `createVariable` (same pattern as `variable_delete`).
> - `style_manage` with `styleId`: new **required** `currentStyleName`, verified against the resolved style's name before any mutation.
> - `style_manage` `name` becomes **optional at the schema level**: the handler enforces it on create; on update it renames only when explicitly provided. This removes the accidental-rename side effect — a properties-only update no longer has to pass (and therefore overwrite) the name.
> - `scopes` becomes **required on `CREATE_VARIABLE`** (schema refinement + handler defense in depth). Explicitly supplied empty scopes remain the caller's deliberate choice; omission is rejected.
> - **Validate the complete plan before the first write (Rev 11, Gap 1 late-mutation fix).** On `UPDATE_VARIABLE`, a supplied `value` requires a valid `modeId` *before* `name`/`description`/`scopes` are touched; on `style_manage` updates, resolve every requested variable binding before applying `name` or properties. Predictable failures return with zero mutation; an unexpected mid-update failure is reported explicitly as partial via a D9 error — never as a clean failure.
> - **Pre-check scope (open-questions Q17, resolved 2026-07-18: Option B).** "The complete plan" means everything one read or one property check can confirm. Two checks join the letter above, both before any mutation: an alias `value` (`type: "VARIABLE_ALIAS"`) resolves its target variable via `getVariableByIdAsync` — a dangling target is a predictable failure and must mutate nothing; and a PAINT-style `bindVariables` request verifies the style has paints to bind before `name`/`description`/`properties` apply — the implementation-review finding was that this refusal fires only after those mutations. Deep value-type validation is **rejected**: simulating Figma's coercion rules invites refusing calls Figma would accept, and drifts as the plugin API evolves. The boundary rule, recorded for future tools: *pre-validate what one read can confirm; disclose what only execution can reveal* (disclosure shape: the Q18 bullet below).
> - **Partial-disclosure shape (open-questions Q18, resolved 2026-07-18: Option A).** When an unexpected mid-update failure occurs despite the pre-checks, the D9 error's `details` reuses the Q9 vocabulary from D7: `partialMutation: true`, a plain-language statement of what changed, and cheap before-values — the original `name` and `description` are already in hand, because the handler resolved the object and verified its current name before mutating. A clean failure never carries the flag. Tool-local rollback was considered and **rejected**: it contradicts the recorded no-transaction posture (the Gap 2 non-goal), and a rollback that itself fails produces an undisclosed partial — the exact state this requirement exists to eliminate. The field names are shared with D7's batch failure rows so the model learns one partial-mutation vocabulary; drift between the Phase 4 and Phase 6 shapes fails review.
> - **TEXT-style font-loading order (open-questions Q19, resolved 2026-07-18: Option A).** Validate-before-mutate hoists font loading only where an existing style is at risk. On **updates**, the target font — the explicitly supplied `properties.fontName`, else the style's actual `fontName` — is loaded before `name`/`description`/`properties` apply. On **creates** there is no style to read yet, so the order is: create the style, read its **actual** `fontName` (or use the supplied one), load, then write properties — all inside the existing rollback guard, which removes the fresh style on any failure. Hardcoding a guessed default font (the implementation review found `{family: "Inter", style: "Regular"}`) is **rejected**: `createTextStyle()`'s default is Figma's to choose, and a wrong guess makes create-with-text-properties fail on documents whose default differs — a contract-invisible, document-dependent failure. A one-sentence comment at the fork records why the two paths order operations differently (an existing style must not mutate before its font is proven loadable; a fresh style is disposable under the rollback guard). The unit-test mock's default font must **not** be Inter, so a hardcoded guess can never silently return.
> - Replace the ambiguous `Expected`/`got` operands in the variable verification error with explicit `stored name` / `received currentVariableName` labels (per the D9 convention).
> - **Mechanism (open-questions Q1, resolved 2026-07-09: Option B).** The schemas keep their **flat object shapes** — no discriminated union. Each conditional requirement is stated twice in prose: in the field description (e.g. "REQUIRED for UPDATE_VARIABLE — the variable's **current exact** name, passed back verbatim from `variable_list`") and in the top-level tool description ("UPDATE_VARIABLE requires `currentVariableName`"). Enforcement is layered: a server-side `.superRefine()` rejects a non-conforming call with an actionable message (the violation, the read tool that supplies the correct value, and "pass it back verbatim"), and the plugin's fail-closed checks above remain as defense in depth (AS1 — the server is not the trust boundary). Rationale: the contract's consumer is an LLM; a flat, consistent schema with the requirement stated twice maximizes first-call correctness, and a fully-controlled refinement error gives one-round-trip recovery — where a discriminated union cannot cover `style_manage` (no discriminator) and would make `variable_manage` the one structurally alien schema on the server.
> **Reaffirmed against the reversal (open-questions Q14, resolved 2026-07-10: Option A).** The follow-up review's argument — refinements do not mark fields required in the emitted `tools/list`, so a JSON-Schema-level requiredness CI check is unsatisfiable under this mechanism — is acknowledged and the reversal (discriminated unions plus a new required `action` field on `style_manage`) is **rejected**: its gain is *advertisement* for structural-schema consumers the criterion ranks secondary, its union errors are terser than the authored superRefine messages, and the new `action` field would guarantee a first-call failure for every existing caller to prevent a hypothesized one. The CI guarantee is expressed at the level this mechanism supports: `safeParse`-behavior tests (omission rejected, with the actionable message) plus description-marker tests — per the Q21 correction (resolved 2026-07-18: Option B, follow-up review finding P4-8): the literal "REQUIRED for …" marker is asserted in the **field** description, the **tool** description is asserted to state the requirement as a natural sentence naming the action and the field (this bullet's own example form, "UPDATE_VARIABLE requires `currentVariableName`"), and both are asserted against the **emitted `tools/list` result** rather than raw registration configuration. *(The phrasing previously here — "the 'REQUIRED for …' text present in both the field and tool descriptions" — loosely summarized the test mechanism and contradicted this bullet's own example wording; Q21 ratifies the example as the contract, at zero description churn.)* **Revisit trigger:** live usage showing agents omitting the twice-documented fields at a meaningful rate.

> [!NOTE]
> **D6 — Make create schemas match the runtime (Gap 3).** Remove `.optional()` from `parentNodeName` in the five create-tool schemas so the model is told to supply what the plugin already requires. `create_component_set` **loses its no-parent exception** (Rev 11): `parentId` + `parentNodeName` become required there too — the handler combines on the containing page and reparents only afterward (`componentHandlers.ts:962, 984–985`), so a no-parent call places the set at page level, escaping node scope (Gap 8; placement fix in D11). **The parent-name refusal joins the coded regime (open-questions Q22, resolved 2026-07-18: Option A — review findings P5-1/S1).** It becomes a factory-built pair with distinct causes per D9 — `PARENT_NAME_MISSING` (the field was omitted) and `PARENT_NAME_MISMATCH` (the field does not match) — defined in the central registry and added to the D9 inventory (Rev 31), on the same "adds or edits" logic that added `VARIABLE_SCOPES_MISSING` (Rev 27). Each message embeds its recovery (read the parent's current name with `node_info`, pass it back verbatim), so an agent that omits the name is not steered into swapping a correct `parentId`; the `create_component_set` missing-`parentId` branch gets its own message naming `parentId`. *(This corrects, in place, the prescription previously here — one merged message, "parentNodeName is missing or does not match…" — which contradicted D9's distinct-cause rule; the Q21 precedent for in-place correction.)*
> **Verification split (open-questions Q2, resolved 2026-07-09: Option A).** Once the schema requires the field, a conforming client can no longer reach the plugin's "missing" branch — the message keeps that branch anyway, as defense in depth for older servers and non-conforming clients. Verification therefore splits by layer: the omission case is asserted by the Phase 5 schema-boundary tests, and the Phase 14 live probe asserts the *mismatch* case against the structured `PARENT_NAME_MISMATCH` refusal (coded per Q22). Each layer is tested for the job it does: the schema delivers first-call correctness, the plugin error delivers one-round-trip recovery.

> [!NOTE]
> **D7 — Batch `success` means all-succeeded; partial success is first-class (Gap 5).** `deleteMultipleNodes` returns `success: true` only when `failureCount === 0`. Additionally, all four batch aggregators gain an explicit `status: "success" | "partial_success" | "failed"` field (`partial_success` = some succeeded and some failed; `failed` = none succeeded). The field is additive — `looseOutput` schemas already accept extra keys — and the batch tools' descriptions instruct the agent to treat `partial_success` as an incomplete operation and report every non-success row (`failed` and `skipped`).
> **Edge semantics (Q7, resolved 2026-07-09; corrected and extended by Rev 11 — Gap 7).** One shared contract across all four aggregators, defined in three layers:
> - **Layer 1 — schema rejection.** Every batch array gains `.min(1)`. A schema-invalid call (`[]`, unknown keys at any depth, a malformed item, or a duplicate target — see the duplicates bullet) is an MCP validation error with **no** execution envelope — the operation was never accepted. *(Rev 9's claim that zero-item batches were already unreachable was false; this makes it true.)*
> - **Layer 2 — refused before execution.** A call refused by dispatcher prevalidation, permissions, or scope returns a structured D9 error with no envelope and **no mutation**. For `instance_set_overrides`, the use-time predicate treats a stored Figma reference with `removed === true`—including the editable scope root—as disappeared; a target removed during an awaited window is refused before `swapComponent()`.
> - **Layer 3 — accepted execution.** Every accepted call returns `status: "success" | "partial_success" | "failed"` derived from row outcomes (`succeeded > 0 && failed === 0` ⇒ `success`; `succeeded > 0` with any failed/skipped ⇒ `partial_success`; `succeeded === 0` ⇒ `failed`), a derived boolean with the invariant **`success === (status === "success")`**, counts (`requestedCount`, `succeededCount`, `failedCount`, `skippedCount`), and **exactly one row per input, in input order** — a stop-on-first handler emits explicit `skipped` rows for unattempted items instead of omitting them (fixes the verified missing-row defect). Every failure/skip row carries an actionable per-item reason so a `partial_success` lets the agent retry every non-success row (`failed` and `skipped`) in one follow-up call. Because JavaScript can throw any value, all four aggregators normalize caught values through the shared null-safe `describeError()` fallback; empty errors, raw strings, `null`/`undefined`, and hostile renderers cannot produce a blank row or invalidate the D7 envelope.
> - **Telemetry isolation (fourth closure recheck, R14/R15 — Rev 45).** Progress and user notifications are best-effort telemetry, not part of mutation outcome control flow. Delivery failure—including a hostile thrown value whose property access or renderer throws—and the act of formatting that failure cannot change rows or counts, fabricate a second outcome, reject after a completed mutation, or erase the Layer 3 envelope. This guarantee assumes the plugin host's standard `console` methods remain nonthrowing.
> - **One row vocabulary across all four aggregators (open-questions Q25, resolved 2026-07-18: Option A — review finding P6-9).** Every per-item row uses the same keys regardless of tool: `nodeId` (identity), `status` (`"success" | "failed" | "skipped"`), and on any non-success row `error` (the actionable reason); the partial-mutation fields (`partialMutation`, `whatChanged`, `before`) attach where applicable (Q9/Q24). `instance_set_overrides`, whose rows previously used `instanceId`/`instanceName`/`message`, is brought onto this vocabulary — it **may** keep `instanceName` as an additive convenience field, but `nodeId` and `error` are the contract keys. The top-level `message` summary is unchanged. Rationale on the shared criterion: the batch descriptions teach one retry loop ("retry every non-success row (`failed` and `skipped`)"), so a single row vocabulary lets the model read any batch result — and the guides teach the row contract — in one form instead of four dialects. This is a breaking rename for readers of the instance rows; per Q8 (hard cutover) and Q15 (single release) it lands now, in the release already changing that tool's envelope, with a CHANGELOG before/after example (Phase 13). The Q9/Q24 `effectState`-algebra rejection still stands — this unifies the row *keys*, not their semantics.
> - **Duplicates are rejected before mutation** for `node_delete`, `text_set_content`, and `instance_set_overrides`, after normalizing URL/API node-ID spellings (`1-2` vs `1:2`); annotation batches may legitimately repeat a node. (The live 4n79 duplicate-delete probe showed duplicates manufacturing spurious failures.) **Layer and code (open-questions Q23, resolved 2026-07-18: Option B — review findings P6-6/S2, Rev 32).** Duplicate rejection lives at **Layer 1**, alongside `[]` and unknown keys: a per-tool schema `.superRefine()` normalizes spellings (via the already-exported `normalizeNodeId`) and rejects a repeated target at the boundary with an authored message (the offending ID, the normalized-spelling rule, and the recovery "remove the duplicate entry and resend the batch") — payload-only validation, which is Layer 1's defining property, the same reasoning that moved `[]` there in Rev 11. **No new D9 code is minted:** a Layer 1 rejection is an MCP validation error, not a coded execution refusal. The plugin dispatcher's own duplicate check is **retained as uncoded defense in depth** on the `UNKNOWN_ERROR` fallback (the Q2 precedent for the plugin's parent-name "missing" branch; its conversion rides the [v2.3.4 PRD](../v2.3.4/prd.md)), so a non-conforming client that bypasses the schema still fails closed. The follow-up review's minted-`DUPLICATE_TARGET`-code alternative (Option A) is **rejected**: duplicates are detectable from the payload alone, so pushing the first-class rejection to a coded plugin refusal would cost a socket round trip that schema validation prevents.
> - **Output schemas expose only the shared envelope counts (open-questions Q26, resolved 2026-07-18: Option B — review finding P6-11).** The batch handlers return one count vocabulary — `requestedCount`, `succeededCount`, `failedCount`, `skippedCount` (with `status`/`success` and `results`) — and every tool-specific duplicate count is **removed from both handler and schema**, not kept as a marked alias: `node_delete`'s `nodesDeleted`/`nodesFailed`, `text_set_content`'s `replacementsApplied`/`replacementsFailed`, `annotation_set`'s `annotationsApplied`/`annotationsFailed`, `instance_set_overrides`' `totalCount` (a pure duplicate of `totalAppliedCount`; the genuine override-property count stays as one field), and the older `totalNodes`/`totalReplacements`/`totalAnnotations` totals all go. The rule is general: **no field duplicates an envelope count.** This also eliminates the original advertised-but-absent drift the review first flagged (`node_delete`'s `deletedCount`, text's `count` — never present in the handler output): there is nothing left to *align* because there is one vocabulary. **Enforcement level, stated precisely (R3, second closure recheck 2026-07-25 — Rev 43; supersedes this bullet's original "registered-callback tests assert the returned key set is exactly the envelope, so `looseOutput` cannot re-mask drift", which overstated what the boundary does).** The advertised output schema **cannot** be made exact at the top level: the pinned SDK client validates `structuredContent` against the advertised schema even on `isError` results and cannot advertise a union, so every declared field is relaxed to optional and an `error` envelope field plus a `catchall` are added (see D9's mechanism note). Consequently the registered schema still accepts a legacy-only top-level payload such as `{nodesDeleted: 1}`, and accepts extra top-level keys generally. What **is** mechanically enforced at the protocol boundary is the **per-row Q25 vocabulary**: each `results` row must carry `nodeId` and a valid `status`, and every `failed`/`skipped` row must carry a non-empty actionable `error`; a legacy `instanceId`/`message`-only row is rejected. Everything else — the absence of legacy top-level counts, and exact envelope conformance of real handler output — is enforced by **tests, not by the schema**: real-handler tests validate each aggregator's current output against the registered schema and assert the envelope with no legacy count, while controlled registered-callback tests separately prove that compliant payloads are preserved; negative row-schema tests red-proof omission of the required Q25 keys. The residual is accepted and recorded here rather than papered over: a *future transport or handler* that emits a legacy-only top-level payload would pass schema validation and be caught only by those tests. A runtime exact-result validator at the callback boundary was considered and **deferred** — it duplicates the test-enforced invariant at every call's cost, and the SDK constraint above means it could not simply be expressed as the advertised schema. The mark-as-alias alternative (Option A) is **rejected** — a marked alias is still a second vocabulary the model can key on and a maintenance surface that drifts, i.e. cruft the release should not ship. Breaking for readers of the old counts; lands now under Q8 (hard cutover) / Q15 (single release) with a Phase 13 CHANGELOG before/after example.
> - **Partial-mutation disclosure (open-questions Q9, resolved 2026-07-10: Option B).** Where a tool can mutate and then fail — reported by the follow-up review for text content (font fallback before character assignment) and instance overrides (main-component swap before later override fields); Phase 6 confirms the exact fields during implementation — the failure row's error details carry `partialMutation: true`, a plain-language statement of what changed, and cheap before-values where available (for instance overrides, the original `mainComponentId`; for text, the original `fontName` — corrected per Q24 below), so the caller can see exactly what changed without a re-read. **Instance truth-capture rule (third closure recheck, Rev 44).** A batch-wide async preflight proves every target's main component is readable before the first mutation. The first target is then captured authoritatively and the whole batch synchronously rechecked before the first swap; every later target is captured again immediately before its own final synchronous predicate gate and swap. A thrown, null, or invalid capture fails before that target is swapped. Thus a later target's awaited read cannot stale an earlier failure row's `before.mainComponentId`, and capture failure is never silently represented as `null`. **Reachability of the restoring write — accepted diagnostic residual (R10, second closure recheck 2026-07-25 — Rev 43).** This bullet originally promised that "the restoring write can be composed directly from the error in one round trip." That promise is **withdrawn**: it overstated the exposed write surface. No registered tool can apply a per-segment font map (`text_set_style` sets one whole-node `fontName`; no tool accepts segment ranges and no plugin write path calls `setRangeFontName` outside `setCharacters`' internal strategies), and no registered tool swaps an *existing* instance back to a main component by ID (`instance_set_overrides` needs a source *instance*, `instance_set_property` sets component properties, `create_instance` makes a different node). The before-values are therefore **truthful diagnostic evidence** — they tell the agent and the user precisely what changed so the situation can be reported and judged — but they are **not** guaranteed to be an executable one-round-trip recovery payload. Composing a full restore may require a re-read and manual steps in Figma. Building the missing write surface (segment-range font writes; swap-existing-instance-to-main-component) or automatic rollback was considered and **deferred**: both are new write capability, each needing its own name/scope/lock verification and safety review, which is out of scope for a release whose subject is the batch envelope. A clean failure never carries the flag. The fields are additive inside failure rows — no envelope shape change. The follow-up review's full `effectState`/`mutationState` algebra and SHA-256 content digests are **rejected** on the shared criterion: a hash cannot tell the model what to write back.
> **Text before-value corrected (open-questions Q24, resolved 2026-07-18: Option A — review findings P6-2/P6-3/P6-4/S4; amends the Q9 record, the Rev 11 precedent for correcting a resolved question's factual premise; further corrected 2026-07-22 for the adversarial critique's C1/C2).** The text mutate-then-fail path mutates **`fontName`**, not `characters`, before the character assignment can fail — so the failure row carries `before: {fontName: …}` and a `whatChanged` statement, and the flag is set **only when `setCharacters` actually changed the font** (reported via a small out-parameter). **Correction (C1/C2, 2026-07-22):** the mutation is *not* only the fallback path. `setCharacters` also mutates `fontName` when it **normalizes a mixed-font node** to a single font before assignment (`node.fontName = firstCharFont`/prevailed font). Both paths are now reported (`report.fontMutated`), and the before-value is a serializable pre-mutation snapshot captured by `setCharacters` itself — `{family, style}` for a single font, or `{mixed: true, segments}` (the styled segments — a complete record of the pre-mutation font map) for a mixed-font node — never `null`. *(R10, Rev 43: "genuinely restorable" is corrected to a record/evidence claim. The segments describe the pre-state completely, but no registered tool can write a per-segment font map back, so restoring is not a one-call operation — see the Q9 bullet's accepted diagnostic residual.)* **Capture-failure residual (R1, closure audit 2026-07-24 — Rev 41):** if the styled-segment API is unavailable — a stubbed/mocked node where `getStyledTextSegments` is absent or throws — the snapshot degrades to a non-restorable `{mixed: true}` marker (no `segments`) and the edit still proceeds, rather than blocking. This is an accepted best-effort residual, not a hard stop: `getStyledTextSegments` is a stable API on a live mixed-font `TextNode`, so the degradation does not arise in the plugin runtime, only under test stubs. A caller that receives `before: {fontName: {mixed: true}}` without `segments` treats the before-state as non-restorable and re-reads the node before composing a restoring write. **C1 fix:** the handler reads the loop-scoped report inside its `catch`, so the disclosure survives the character-assignment throw (the earlier implementation read a stale local assigned only on the success path, and disclosed nothing). The zero-mutation throw paths — node deleted, node no longer `TEXT`, a character-assignment failure with **no** font change — carry neither the flag nor a before-value. Q9's originally prescribed `characters` before-value is **superseded**: it names a property the path never mutated. The Q18 shared-vocabulary contract is unaffected — the field *names* (`partialMutation`, `whatChanged`, `before`) are unchanged; only the text tool's before *content* is corrected. **The vocabulary is shared beyond batches (open-questions Q18, resolved 2026-07-18: Option A):** the non-batch D5 tools (`variable_manage`, `style_manage`) report unexpected mid-update failures with the same fields inside the D9 error's `details` — one convention across batch rows and single-object tools; drift between the Phase 4 and Phase 6 field names fails review.

> [!NOTE]
> **D8 — Code, contract docs, and executable contract move in the same change.** Every D5–D14 change updates, in the same PR: `SAFETY.md` (G2's "every write tool" wording, A3, the Part B rows for `variable_manage`/`style_manage` and the create tools, the "Name fields" bullet, and the new G1/residual entries from D11), the mechanically-diffed `src/mcp_server/tests/unit/figma_plugin/safetyContract.test.ts`, and the agent guides (`skills/figma-edit/references/constraints.md`, `error-playbook.md`, `tool-selection.md`, `workflows.md`, mirrored as the `figma-edit://guide/*` resources). A gate that exists in code but not in the manual — or vice versa — fails CI by design.
> **Recursive strictness (Rev 11, Gap 7).** The central strict wrapper's `.strict()` currently applies only to the top-level object (`tools/index.ts:31–40`); nested objects silently strip unknown keys — exactly the intent loss the wrapper's own comment says it exists to prevent. Apply strictness at every object depth. Enumerate the intentional catchalls (the polymorphic `paint`, `effects`, and `layoutGrids` style payloads) and expand the strict-input tests from a top-level sentinel to unknown keys injected at every depth.
> **G2 target wording (open-questions Q3, resolved 2026-07-09: Option A).** G2 becomes a universal rule over existing objects: *"No write against an existing object proceeds unless the caller-supplied current name matches the resolved object's actual name — nodes, variables, styles, and collections alike. Creation verifies the identified parent or collection instead."* Publishing it is gated on the Phase 12 audit of every write tool against the rule (the safetyContract mechanical diff provides most of it; the Part B matrix stays the per-tool proof). The agent guides state the rule in exactly one sentence — one internalized rule produces correct first calls even on tools the agent has not used before, where an enumerated category list invites wrong guesses at its boundary.

> [!NOTE]
> **D9 — Refusal-message convention (open-questions Q4, resolved 2026-07-09: Option A).** Every verification refusal that this release adds or edits uses the `Operation Denied: …` prefix, is defined centrally alongside the other guard messages (`main.ts` `ERRORS` or a shared plugin constants module) rather than as a handler-local string, and gets an `error-playbook.md` entry. The prefix alone is half the job — each message must embed its own recovery so a failed call costs exactly one round trip: name the cause distinctly (missing vs. mismatched vs. stale), name the read tool that supplies the correct value (`variable_list`, `style_list`, `node_info`), and say "pass it back verbatim". The in-repo model is the instance-interior delete refusal, which names both alternatives ("Edit the main component, or use instance overrides"); the counterexample this release fixes is `PARENT_NAME_MISMATCH`, which steers agents toward the wrong field. Acceptance check per message: an agent given only the error text and the tool list can produce the correct retry without further discovery. Existing tests that assert on changed message text are updated in the same change. This convention governs the D5 verification errors and D6's reworded parent-name error.
> **Layer scope (open-questions Q29, resolved 2026-07-23: Option B).** The two halves of this convention apply to different layers. The `Operation Denied:` prefix and the central-registry rule (every coded refusal originates from the `REFUSALS` factory module — Q16/Q27) govern the **coded plugin refusals**: the D5 verification errors and D6's parent-name error, which carry a stable `code`, are thrown by the plugin dispatcher, and recur across many handlers. The **D5-mechanism server-side `.superRefine()` messages are a different layer** — Zod schema-boundary validation errors that carry no `code`, fire before anything crosses the socket, and are each coupled to one schema's specific field/action constraint. They **legitimately co-locate with the schema they refine** (rather than sourcing from the plugin registry, which the server does not import at runtime — Q27) and carry **no `Operation Denied:` prefix**, being validation errors rather than policy refusals. They are held to D9's **content bar** instead: where a read tool supplies the correct value, the message names the cause, names the read tool, and says "pass it back verbatim" — which the name-verification refinements (`currentVariableName`, `collectionName`, `currentStyleName`) already satisfy, now asserted by a Phase 4 content-bar test so the guarantee cannot silently regress. (The original D9 wording named the superRefine messages as requiring central plugin-module definition; that was written before the server/plugin registry split was understood and is corrected here — the messages were never movable to a plugin module the server cannot import.)
> **Structured transport (Rev 11, Gap 11).** Stable codes are worthless if the transport destroys them: today the plugin dispatcher flattens every failure to a string (`describeError`, `main.ts:319–324`) and `figma-client.ts` reconstructs plain `Error`s, so no code can survive to the MCP boundary. Every refusal and operational failure this release adds or edits travels as a structured `{error: {code, message, details?}}` object from the plugin dispatcher through the UI relay and socket to `figma-client`, and surfaces at the MCP boundary as `isError: true` with machine-readable `structuredContent` plus a text fallback carrying the same code. Codes are never reconstructed by parsing prose. **Delivery mechanism (review finding P4-1, fixed 2026-07-18):** the pinned SDK *client* validates any present `structuredContent` against the advertised output schema even on error results, and the SDK cannot advertise a union — so every registered output schema is centrally relaxed to optional fields plus an advertised optional `error` envelope field (types still validate when present; exact success-field conformance stays enforced by the D7 registered-callback tests), and the official-SDK boundary suite proves every tool delivers the envelope without a `-32602`. New codes introduced by this release (`PLUGIN_PEER_UNAVAILABLE`, `PLUGIN_PEER_AMBIGUOUS`, `CHANNEL_IN_USE`, `VERSION_MISMATCH`, `PAGE_LOAD_FAILED`, `PAGE_NOT_FOUND`, `TARGET_NOT_PAGE`, `DOCUMENT_SCAN_INCOMPLETE`) follow the same convention and get playbook entries.
> **Code inventory (open-questions Q16, resolved 2026-07-18: Option A).** The Phase 3–4 implementation review found the release minting codes this PRD never enumerated, deriving codes from message prose, and duplicating message text between the central table and handler-local throws. The ratified inventory is: the ten operational codes (the eight above plus `PAGE_LOAD_TIMEOUT` from the Q12 resolution and `CONNECTOR_TEMPLATE_REQUIRED` from D12), plus seven D5 verification codes — `VARIABLE_NAME_MISSING`, `VARIABLE_NAME_MISMATCH`, `COLLECTION_NAME_MISSING`, `COLLECTION_NAME_MISMATCH`, `STYLE_NAME_MISSING`, `STYLE_NAME_MISMATCH`, and `VARIABLE_SCOPES_MISSING` (the required-`scopes` backstop, added to the inventory during implementation, Rev 27 — D9's "adds or edits" rule covers it), plus two D6 parent-verification codes — `PARENT_NAME_MISSING` and `PARENT_NAME_MISMATCH` (the refusal D6 edits, joined to the coded regime by Q22, resolved 2026-07-18: Option A, Rev 31 — the same "adds or edits" rule) — each with a Phase 12 playbook entry, plus **`UNKNOWN_ERROR`, the ratified legacy fallback**: a failure not thrown as a coded object surfaces with `UNKNOWN_ERROR`, documented as "no machine classification exists; the recovery is in the message text", with a playbook entry saying exactly that. Three rules follow. (1) Codes are never derived from prose — the dispatcher's only classification is *structured object thrown ⇒ pass it through; anything else ⇒ `UNKNOWN_ERROR`*; the implementation's substring/prefix matching over the `ERRORS` table is deleted. (2) Every coded refusal originates from a central registry of message factories — handlers pass operands in and never compose refusal text locally — so a single authored text carries both the D5 stored/received operands and this convention's recovery, ending the observed divergence where two message texts shared one code. (3) The client's ad-hoc `JOIN_ERROR` default is removed: the join path passes through the socket's real code and falls back to `UNKNOWN_ERROR`. Out of scope here: the plugin's legacy failure surface (measured 2026-07-18: 332 inline `throw new Error` sites across 15 files, 51 of them wrapping central-table strings) stays on the fallback; its conversion is specified in the [v2.3.4 PRD](../v2.3.4/prd.md), where `UNKNOWN_ERROR` becomes a burn-down metric. **Registry scope (open-questions Q27, resolved 2026-07-23: Option B).** Rule (2) above — "a central registry of message factories" — is now fully literal on the plugin side: the ten operational codes move from the legacy `ERRORS` plain-string table into the `REFUSALS` factory registry alongside the D5/D6 codes, so every code this release adds or edits is a factory with no exceptions; `ERRORS` is re-scoped, by comment and by rule, to hold only messages that predate v2.3.3's D9 convention. The plugin's `UNKNOWN_ERROR` fallback becomes one named local constant instead of a repeated literal. The plugin registry and the server-side mirror (`src/shared/errorCodes.ts`) remain two separate definitions by design — the plugin bundle does not import `src/shared` at runtime, to avoid pulling server-side modules into `figma_plugin/code.js` for what a parity test already keeps in sync — a deliberate, now-recorded tradeoff rather than an undocumented scope-down.
> **`channel_join` pass-through (open-questions Q20, resolved 2026-07-18: Option A — follow-up review finding P4-4).** The join tool's failure handling is brought under the Q16 rules without deciding the envelope. Every structured `error.code` reaching `channel.ts` passes through into the in-band `errorCode` verbatim — unknown codes included, never collapsed to `UNKNOWN_ERROR` — and `details` is preserved. The prose-classification branches (`message.includes("timed out")`, `includes("Connection closed")`) are deleted; those two locally-generated failures are coded **at origin** in `figma-client` (`CHANNEL_JOIN_FAILED` on request timeout, `PLUGIN_DISCONNECTED` on connection close — the values the tool already advertises, now assigned where the error is created, both pre-existing rather than new inventory). The in-band `status: "error"` envelope itself is unchanged: its convergence on the D9 `isError` boundary is deliberately left to the [v2.3.4 PRD](../v2.3.4/prd.md)'s Q1, whose context records that this pass-through fix reduces Q1 to the envelope question alone. Phase 9's join refusals therefore arrive with their codes intact.

> [!NOTE]
> **D10 — Repair the annotation contract (Gap 6).** Align the schema to the handler and the pinned Figma typings. Each item requires `nodeId`, `nodeName`, and `labelMarkdown` (rejected when blank); `categoryId` becomes optional and, when supplied, is verified via `getAnnotationCategoryByIdAsync` before mutation; `properties` matches the pinned shape (an array of `{type}` objects, duplicate types rejected) rather than a record; the unsupported `annotationId` and `status` fields are removed. The tool's metadata drops `idempotentHint` — append is not idempotent. `annotation_list`'s registered output is corrected to the real grouped shape (`annotatedNodes`, preserving node ownership in both page and node modes) so the schema stops advertising a field the handler never returns. `annotation_set` adopts the D7 envelope. **Retry identity (open-questions Q10, resolved 2026-07-10: Option B).** The follow-up review's JCS/SHA-256 payload digest is **rejected**: an LLM cannot compute a hash, and in the timeout scenario the digest exists for, the append response never arrived — so the model holds no digest to compare. Per-item before/after annotation counts plus list-before-retry guidance are the contract: after an uncertain outcome, one `annotation_list` call returns labels the model reads and compares against what it sent — one round trip, using nothing but the model's ability to read text. Identical-text duplicates remain ambiguous (they would under the digest too); the guides say so.

> [!NOTE]
> **D11 — Containment by construction for implicit-parent APIs (Gap 8).** The guarantee is an **observable boundary**: no command may await, emit progress or a response, or terminate while a created node sits outside its verified destination. Concretely: resolve and verify the destination *first*; call the implicit creator; make append/insert into the verified parent the **immediate next synchronous operation** — before any `await` or fallible property assignment; then configure inside a guarded block that removes the artifact on later failure (`committed` flag, cleanup in `finally` only when uncommitted). `create_text` inserts before font loading and character assignment. `node_flatten` captures and verifies the source's parent and index and passes both to `figma.flatten(nodes, parent, index)` — true zero-transient placement. `create_component_set` passes its (now required, D6) verified parent directly to `combineAsVariants`. `node_clone` appends the clone immediately after `clone()`. Audit `create_shape`, `create_frame`, `create_text`, `create_svg`, `create_instance`, `create_component`, and `node_clone`. The unavoidable same-stack micro-transient (an implicit API constructs on `currentPage` for part of one synchronous stack) is recorded in `SAFETY.md` as a residual risk, and G1's wording claims the observable boundary — not literal zero-transient containment.

> [!NOTE]
> **D12 — Connector repair: explicit template, honest results, Design support kept (Gap 9; open-questions Q13, resolved 2026-07-10: revised hybrid).** All five verified defects (see the Gap 9 problem statement and Provenance) are fixed within the tool's existing purpose, adopting the follow-up review's explicit-template core while **rejecting its FigJam-only restriction** — the flagship use case, visualizing `reaction_list` prototype flows, lives in Figma Design, and the repo's own reaction prompt teaches the paste-into-Design workflow, which works via clone:
> - `create_connection` becomes **creation-only**: the set-default and check-default modes, the `clientStorage` cache, and the silent `currentPage` auto-adoption are **removed**. Every call passes `connectorId` plus new required `connectorName`, resolved and verified (existence, exact current name, `CONNECTOR` type, in scope) before any mutation — G2 conformance for the one reference that materially determines the artifact. Verification showed the hidden default bought nothing: the bootstrap requires template discovery at least once anyway, and an LLM re-passes a known ID for free.
> - A call with no valid template returns the structured D9 error **`CONNECTOR_TEMPLATE_REQUIRED`**, whose message is the bootstrap UX: find a connector with `page_info`/`node_info` (pasting one from FigJam if the file has none) and pass its ID and exact current name. The error carries its own recovery.
> - Results adopt the **D7 envelope** — per-item ordered rows, honest `status`, no unconditional `success: true`, and error rows never counted as created connections. Clone and cursor creation fall under **D11**: verify the (derived) destination, insert as the immediate next synchronous operation, track every created ID, and clean up uncommitted artifacts on later failure — a cursor created for one endpoint may not leak when the other endpoint fails.
> - The reaction prompt and guides are updated to teach explicit template discovery instead of default management.

> [!NOTE]
> **D13 — Bind one plugin peer to one MCP session (Gap 10).** The minimal peer-binding core, deliberately without the follow-up review's protocol-digest machinery: the plugin's join message carries `clientType: "plugin"` and its build version; the socket assigns each connection a random `peerId`, reserves a channel as **exactly one plugin peer plus one MCP session**, and routes commands and responses only within that bound pair (responses are correlated by peer; a response from any other peer is ignored). Joining with zero plugin peers returns `PLUGIN_PEER_UNAVAILABLE`; a second plugin returns `PLUGIN_PEER_AMBIGUOUS`; a second MCP client returns `CHANNEL_IN_USE`. `channel_join`'s result includes `serverVersion` and `pluginVersion`, and a known-unequal pair is refused with `VERSION_MISMATCH`. `resetChannel`/leave becomes a real socket-level leave that unbinds the pair and clears state; the binding is invalidated when the bound peer disconnects, and every subsequent tool call is blocked until a successful rejoin. This is a peer-bound, self-reported version check — not cryptographic attestation — and is claimed as exactly that.
> **Handshake depth (open-questions Q11, resolved 2026-07-10: Option B).** The follow-up review's protocol digests, nonces, and scope fingerprints are **rejected until evidence demands them**. Rationale on the shared criterion: released-version skew — the class that reaches agents — is already covered by the version fields plus `check:versions`/`check:plugin` coupling at release time, and neither `VERSION_MISMATCH` nor `PROTOCOL_MISMATCH` is model-recoverable (both end in a human rebuild/reinstall), so the digest machinery adds no recovery and only marginal first-call protection against a contributor-side dev-build hazard. **Escalation trigger:** a real incident of same-version different-contract skew causing a live failure that version fields could not have caught — at which point the digest apparatus becomes a sized work item, not a speculative one. One loose end is adopted as work, not deferred: **verify the reported `ui.html` scope-ready race** (the UI allegedly opens the socket before plugin-main acknowledges the committed scope — unverified); if confirmed, the fix is to make the UI wait for plugin-main's acknowledgement before joining, a D13-lite-sized change (Phase 9).

> [!NOTE]
> **D14 — Isolate page-load failures; make destructive scans fail closed explicitly (Gap 11).** Multi-page reads (`page_info` with `pageIds`, `node_info` traversal, `component_list`, `variable_list` consumer scans, `annotation_list` by page) wrap each page's `loadAsync()` individually: a failing page becomes a structured per-page error (`PAGE_LOAD_FAILED` / `PAGE_NOT_FOUND` / `TARGET_NOT_PAGE`) inside a shared `coverage` object (`{complete, pageErrors: [{pageId, error}]}`) while successful pages still return their data; `complete === (pageErrors.length === 0)`. Single-page commands return the structured error directly instead of Figma's raw string. Destructive document-wide scans (`variable_delete`, including collection mode and apparently empty collections) abort **before any `remove()`** with `DOCUMENT_SCAN_INCOMPLETE` when any page could not be loaded and scanned — today's accidental fail-closed-by-thrown-error becomes an explicit, tested contract: a page error can never mean zero consumers.
> **Scheduling depth (open-questions Q12, resolved 2026-07-10: Option B).** One addition beyond lite: a **single bounded per-page timeout** — value chosen in implementation and documented as behavior, not contract — so a hung `loadAsync()` becomes a structured `PAGE_LOAD_TIMEOUT` page error (added to the D9 code list) instead of an indefinite hang that wedges the serialized command queue. Rationale on the shared criterion: recovery requires an error to exist — a hang returns *nothing*, the one state the D9 convention cannot recover from — while the follow-up review's pinned constants, concurrency caps, total deadlines, and heartbeat cadence are all model-invisible and are **rejected**: they add nothing the model can read or act on beyond the timeout's structured error. Residual to test: a `loadAsync()` that settles *after* its timeout must be provably ignored — the late result may not alter a returned response or authorize later work.

### Compatibility posture

These are **deliberate breaking tightenings** for agent callers, not regressions: a call that previously performed an *unverified* global write now fails with a structured error naming the missing field, and the updated guides tell the agent how to recover (read the current name, pass it back verbatim). Gap 3's schema change is not behaviorally breaking — calls omitting `parentNodeName` already failed at runtime; they now fail earlier with a better contract. The `CHANGELOG.md` entry must call out all newly required fields and the new batch `status` field explicitly.

Rev 11 widens the breaking surface: `annotation_set`'s field set changes (`labelMarkdown` required; `annotationId`/`status` removed; `categoryId` optional-but-verified) — a strict correction, since no conforming call could previously succeed at all; `create_component_set` requires an explicit parent; recursive strictness rejects nested unknown keys that were previously stripped silently; batch inputs require at least one item and refuse duplicates; the batch handlers drop their tool-specific count fields (`nodesDeleted`/`nodesFailed`, `replacementsApplied`/`replacementsFailed`, `annotationsApplied`/`annotationsFailed`, the duplicate `totalCount`, and the `total*` totals) in favor of the shared envelope counts (`requestedCount`/`succeededCount`/`failedCount`/`skippedCount`), removing the advertised-but-absent drift rather than aliasing it (Q26); the four batch tools' per-item rows converge on one vocabulary (`nodeId`/`status`/`error`), which renames `instance_set_overrides`' previous `instanceId`/`instanceName`/`message` row keys (Q25 — `instanceName` may remain as an additive field); and `channel_join` gains version/peer fields and can newly refuse empty, ambiguous, or version-mismatched channels. Per Q13, `create_connection` becomes creation-only with a required, name-verified template on every call — the default-template/check modes and the cache are removed, and the structured `CONNECTOR_TEMPLATE_REQUIRED` error teaches the bootstrap. The CHANGELOG lists each with a before/after example.

Rev 20 adds one **additive** repair: `create_instance` gains the `componentId` response field its handler always advertised but never delivered — `InstanceNode.componentId` does not exist at runtime, so the value was `undefined` and the key was dropped in serialization (live-verified 2026-07-17, channel `p28j`). No caller has ever received the field, so nothing breaks; `looseOutput` accepts the new key by design.

**Hard cutover confirmed (open-questions Q8, resolved 2026-07-09: Option A).** There is no warn-only transitional release. A warning attached to a successful result is precisely the signal agents are known to ignore (Gap 5's live lesson), so a grace period would keep the unverified-write path open while buying no real migration safety. With the D5 dual descriptions preventing most first-call omissions and the D9 self-recovering errors, the entire migration cost is one recovery round trip per affected agent — for a safety fix, a grace period is a period in which the fix is off.

---

## Scope & non-goals

**In scope**

1. Fix `figma_plugin/tsconfig.json` per D1 (`types`, no `dom`).
2. Resolve the 10 residual type errors per D2/the triage table.
3. Add `check:types:plugin` and wire it into CI per D3.
4. Gap 1 (D5): required current-name verification for variable updates (`currentVariableName`), style updates (`currentStyleName`), and variable creation (`collectionName`); required `scopes` on create; validate-before-mutate ordering, extended per Q17 with the alias-target and paints-present pre-checks (no value-type simulation); TEXT-style font loading ordered per Q19 (updates load before mutation; creates read the fresh style's actual font — no hardcoded default); rename-on-update only when `name` is explicitly provided; stored/received error wording.
5. Gaps 3/8 (D6/D11): `parentNodeName` required in the five create-tool schemas **and** `create_component_set` (with `parentId`); the parent-name refusal becomes the coded, factory-built `PARENT_NAME_MISSING`/`PARENT_NAME_MISMATCH` pair, naming the omitted and mismatched causes distinctly (Q22, Rev 31); observable-boundary containment for every implicit creator, clone, flatten, and component-set path.
6. Gaps 5/7 (D7): batch `success` true only on zero failures; tri-state `status`; `.min(1)` inputs; one ordered row per input including explicit skipped rows; duplicate-target rejection with normalized IDs at the schema boundary (Layer 1, Q23) with the plugin check retained as defense in depth; batch handlers exposing only the shared envelope counts, the tool-specific duplicates dropped from handler and schema (Q26); per-row partial-mutation disclosure with before-values on text and instance failures (Q9; the text before-value is `fontName`, corrected per Q24, and the flag is set only when the fallback actually applied); one shared per-item row vocabulary (`nodeId`/`status`/`error`) across all four aggregators, renaming the instance rows (Q25).
7. Gap 6 (D10/D8): working `annotation_set` contract with category verification; corrected `annotation_list` output; non-idempotent metadata; recursive input strictness with enumerated catchalls.
8. Gap 10 (D13): peer-bound channel — one plugin + one MCP session, plugin version in the join, pair-only routing, real leave/reset, fail-closed empty/ambiguous/mismatched channels.
9. Gap 11 (D9/D14): structured `{error}` transport end to end; per-page load-failure isolation with `coverage` reporting; explicit fail-closed destructive document scans.
10. Gap 9 (D12): creation-only `create_connection` with a required, name-verified template on every call; cache and auto-adoption removed; `CONNECTOR_TEMPLATE_REQUIRED` bootstrap error; D7 envelope and D11 cleanup; Design-file support kept.
11. Contract sync: `SAFETY.md`, `safetyContract.test.ts`, and the four agent guides updated in the same change (D8).
12. Version bump `2.3.2 → 2.3.3` across all surfaces; `CHANGELOG.md` entry naming every newly required field and breaking repair, with before/after examples.

**Explicit non-goals**

- No MCP-server tsconfig audit (`./tsconfig.json`) — separate concern (D4). **Now tracked, not unstated (Rev 21, review decision 5, accepted 2026-07-17):** root `tsc --noEmit -p tsconfig.json` is currently unrunnable (TS6059 — `rootDir: "src"` is violated by tests importing `figma_plugin/` sources) and CI runs no server type-check at all (`build` is tsup, which does not type-check), so the Phase 4 `.superRefine()` enforcement lives in unchecked code. The follow-up — acceptance shape: a `check:types:server` gate analogous to `check:types:plugin` — is recorded in the task list's follow-ups section.
- No runtime behavior change from Track 1 — its edits are types/casts/config only. The only behavior changes in this release are the D5–D14 changes; anything further is out of scope.
- No new tools, no new permissions, no scope-model changes. Repairing `annotation_set` restores an advertised capability; it does not add one.
- No Gap 2 engineering fix: no *general cross-tool* rollback/transaction layer. D5's validate-before-mutate ordering and D7's honest partial reporting are tool-specific hardening, not transactions; prevalidation atomicity stays the contract and the README wording correction is already in flight outside this PRD.
- No Gap 4 code change: the structural-blocked / override-allowed instance policy is intentional (`SAFETY.md` A9, R5) and stays as is.
- **Deferred pending explicit decision (from the 2026-07-10 follow-up review), tracked as [`open-questions.md`](open-questions.md) Q9–Q15:** the batch effect-state algebra and SHA-256 content digests (Q9 — **resolved 2026-07-10: rejected**; D7 instead adopts minimal per-row partial-mutation disclosure); JCS/SHA-256 annotation payload digests (Q10 — **resolved 2026-07-10: rejected**; D10's counts + list-before-retry stand); protocol digests, nonces, and scope fingerprints beyond D13's version fields (Q11 — **resolved 2026-07-10: rejected until evidence**; escalation trigger and the scope-ready race verification recorded in D13/Phase 9); fixed page-scan scheduler constants and heartbeat cadence (Q12 — **resolved 2026-07-10: rejected**; D14 gains only a bounded per-page timeout); the connector redesign (Q13 — **resolved 2026-07-10: revised hybrid adopted** — explicit-template core in D12/Phase 11; FigJam-only restriction rejected because the flagship reaction-flow use case lives in Design); the Q1 reversal to discriminated unions (Q14 — **resolved 2026-07-10: reversal rejected**; Q1's Option B mechanism reaffirmed in D5 with `safeParse`/description-marker CI checks and a recorded revisit trigger); and release sizing (Q15 — **resolved 2026-07-10: Option C** — a single v2.3.3 release containing every phase of this PRD, recorded in the Release identity block). **All of Q9–Q15 are resolved; nothing gates Phase 4.** The rejected prescriptions above remain follow-up scope, not part of this release.
- No annotation update/delete-by-ID: the pinned Figma API exposes no stable annotation ID; D10 narrows the claim to append.
- No `strict` relaxation, no `@ts-nocheck`, no blanket `any` casts.
- No plugin refactor beyond the 10 residual fixes, the separately ratified Rev 20 suppression-hygiene cleanup, and the D5–D14 changes.
- No change to the esbuild build pipeline (the type gate is additive, not a replacement).

---

## Implementation plan (phased)

**Phase 1 — Fix the config (D1).** Replace `typeRoots` with `types: ["@figma/plugin-typings"]`; confirm `lib` stays `["es2018"]` (no `dom`); include only the imported `../src/shared/nodeTypes.ts` shared file. Re-run `tsc --noEmit -p figma_plugin/tsconfig.json` and confirm the decided configuration reports 10 residual errors (the intermediate probe before the explicit shared root reports 9).

**Phase 2 — Triage the residual (D2).** Fix all 10 per the table. After each, re-run `tsc` to confirm the count strictly decreases and no *new* errors appear. Do not proceed to a green state by suppression. If any fix appears to require a runtime behavior change, stop and escalate (D4). Two Rev 21 additions (review decisions 3 and 4, accepted 2026-07-17), both landing before the Phase 1–2 commit: the two `never`-narrowing fixes use a zero-emitted-JS form (a type annotation on the existing declaration or an inline cast, not a const alias), so Track 1 contributes no code-line changes to the rebuilt bundle; and both guard sites gain a one-sentence comment recording that the typings prove the guarded branch statically unreachable (`node.parent` is `(BaseNode & ChildrenMixin) | null`) and that the guard is kept deliberately as runtime defense in depth — removing it would be a behavior change on a live-verified path. Completion is evaluated over the two-commit sequence `bf4a4c0` followed immediately by `3f62505`: the first commit accidentally omitted the ambient declaration file, and the immediate follow-up added it and restored the required zero-error state; history is recorded rather than rewritten.

**Phase 3 — Regression tests / gate (D3).**
- Add `check:types:plugin` → `tsc --noEmit -p figma_plugin/tsconfig.json` to `package.json` scripts.
- Wire it into `.github/workflows/ci.yml` (alongside `check:plugin` / `check:versions`).
- Confirm the gate is green locally and would fail if `typeRoots`/`types` were reverted or a real type error were introduced (prove it: temporarily reintroduce a break and confirm red).
- **Rev 21 (review decision 2, accepted 2026-07-17):** ship a companion `check:suppressions` gate in an adjacent CI step (wording per ratification 5: adjacent steps satisfy the intent) — `@ts-ignore` and (per review P3-2) `@ts-nocheck` are forbidden in `figma_plugin/**/*.ts`, and `@ts-expect-error` is allowed only with a meaningful description; directive *recognition* is delegated to TypeScript itself — `sourceFile.commentDirectives` for `@ts-ignore`/`@ts-expect-error` and `sourceFile.checkJsDirective` for `@ts-nocheck`, from the same `typescript` the type gate runs — so the gate matches the compiler's own directive grammar by construction — classifying each directive by TypeScript's own `directive.type` rather than any regex — and never mistakes directive-shaped text in strings/templates/regex for a directive, misses a prefix-suffixed `@ts-ignorefoo`, or misflags an `@ts-expect-error` whose description mentions `@ts-ignore` (Rev 37 / Q28, hardened in Revs 39–40 after follow-ups found the interim comment-enumeration and comment-body-regex forms still diverged from TS on multiline and prefix/suffix grammar), and the checking logic is exported with differential fixtures in the unit suite (P3-3); the failure output names the offending file:line and states the fix (the one-round-trip criterion, transposed to the contributor loop). The repo has no ESLint, so a small script is the mechanism (`@typescript-eslint/ban-ts-comment` is the equivalent if ESLint is ever adopted). The current tree passes: the 2026-07-17 conversion left 50 `@ts-expect-error` suppressions, each carrying its suppressed error as a description (49 as of 2026-07-18 — two `TS2551` suppressions merged into one during the Phase 4 bind-loop rework), tracked for touch-it-clear-it burn-down.

**Phase 4 — Structured error transport + design-system verification (D9/D5).** First make `{error: {code, message, details?}}` survive plugin → UI relay → socket → `figma-client` → MCP boundary (replace the `describeError` string flattening; transport tests assert code preservation and `isError`/`structuredContent` at the actual MCP boundary). Then the D5 schema/handler work: in `src/mcp_server/tools/variable.ts` and `style.ts`, make `currentVariableName` required for `UPDATE_VARIABLE`, add required `collectionName` and required `scopes` for `CREATE_VARIABLE`, add `currentStyleName` (required with `styleId`) to `style_manage`, and make `name` optional there — all via the D5 mechanism, reaffirmed by Q14 (flat shapes, dual descriptions, `.superRefine()` with actionable errors; no discriminated union; CI asserts requiredness via `safeParse` and description-marker tests — per Q21: the literal "REQUIRED for …" marker in field descriptions, the natural action-names-field sentence in tool descriptions, both against the emitted `tools/list`). In the handlers: enforce everything fail-closed before any mutation, apply validate-before-mutate ordering per the Q17 scope (a supplied `value` with a missing/invalid `modeId` mutates nothing; an alias `value` resolves its target variable first; style bindings resolve — and PAINT binds verify the style has paints — before `name`/properties apply; no value-type simulation), order TEXT-style font loading per Q19 (updates load the target font before any mutation; creates read the fresh style's actual font under the rollback guard — no hardcoded default), enforce `name` required-on-create / rename-only-when-provided, and replace the `Expected`/`got` operands with stored/received labels (all messages per the D9 convention). Unit tests per change: missing, stale, and mismatched names fail closed with zero mutation; late-plan failures mutate nothing; properties-only style updates do not rename; a deterministic injected mid-update failure carries the Q18 disclosure (`partialMutation: true`, the what-changed statement, before-values) and a clean failure never does; verified operations succeed.

**Phase 5 — Explicit-parent conformance (D6).** Remove `.optional()` from `parentNodeName` in the `create_shape`/`create_frame`/`create_text`/`create_svg`/`create_instance` schemas; make `parentId` + `parentNodeName` required on `create_component_set`. Replace the parent-name refusal with the coded `PARENT_NAME_MISSING`/`PARENT_NAME_MISMATCH` factory pair, each embedding its recovery per the D9 convention (Q22, Rev 31); the `create_component_set` missing-`parentId` branch gets its own message naming `parentId`. Schema tests assert all six tools reject calls missing the parent fields at the schema boundary; plugin tests (bypassing the server) assert missing/mismatched names still fail closed.

**Phase 6 — Batch contract corrections (D7).** `.min(1)` on all batch arrays; duplicate-target rejection (normalized `1-2`/`1:2` ID forms) for delete/text/instance at the schema boundary via `.superRefine()` (Layer 1, Q23 Option B — no new code), with the plugin dispatcher check kept as uncoded defense in depth; `deleteMultipleNodes` returns `success: failureCount === 0 && successCount > 0`; `status` + `skippedCount` + exactly one ordered row per input (unattempted items become explicit `skipped` rows); the three-layer boundary (schema rejection / structured command error / execution envelope); batch handlers and schemas exposing only the shared envelope counts with every tool-specific duplicate removed and validated via registered-callback tests (Q26 Option B — no marked aliases); and one shared per-item row vocabulary (`nodeId`/`status`/`error`) across all four aggregators, renaming `instance_set_overrides`' `instanceId`/`instanceName`/`message` keys (Q25 Option A — `instanceName` may remain additively). Per the Q9 resolution as corrected by Q24, the text and instance handlers disclose partial mutation on failure rows (`partialMutation: true`, `whatChanged`, before-values — for text the original `fontName` under the font-fallback path, for instance overrides the original `mainComponentId`), with the flag set only when a mutation actually occurred. Unit tests: a simulated mid-batch `remove()` failure yields `success: false`, `status: "partial_success"`; the property `success === (status === "success")` holds across all four aggregators; `[]` and duplicates are refused with no envelope and no mutation; a deterministic text failure on the fallback-then-assign path proves the disclosure carries `before: {fontName}` and `whatChanged`, while the zero-mutation throw paths (node deleted, not TEXT, no-fallback assign failure) carry neither the flag nor a before-value.

**Phase 7 — Annotation repair + recursive strictness (D10/D8).** Fix the `annotation_set` item schema and handler alignment (`labelMarkdown` required; `annotationId`/`status` removed; `categoryId` optional and verified; pinned-shape `properties`); correct `annotation_list`'s registered output to the grouped `annotatedNodes` shape; drop `idempotentHint`; adopt the D7 envelope. Make the central strict wrapper recursive with the enumerated catchalls; tests inject unknown keys at every object depth and assert legacy annotation fields are rejected rather than stripped.

**Phase 8 — Containment (D11).** Audit and fix `create_shape`, `create_frame`, `create_text`, `create_svg`, `create_instance`, `create_component`, `node_clone`, `node_flatten` (pass captured parent + index to `figma.flatten`), and `create_component_set` (pass the verified explicit parent directly to `combineAsVariants`). Mocked call-trace tests assert insertion is the immediate next synchronous call after every implicit creation and that cleanup runs only on failed, uncommitted attempts; `create_text` inserts before font awaits. Record the same-stack micro-transient residual and the G1 observable-boundary wording in `SAFETY.md` (Phase 12 syncs it). While `createComponentInstance` is open for this audit, fix its dead `componentId` response field (Rev 20; Provenance: `p28j` live probe, 2026-07-17): return `componentId: component.id` from the already-resolved component — both the `componentId` and `componentKey` resolution paths hold it — and remove the satisfied suppression; a unit test asserts the field equals the resolved component's id in both paths.

**Phase 9 — Peer-bound channel (D13).** Plugin join identity + build version; one-plugin/one-MCP channel binding with pair-only routing and per-peer response correlation; the `PLUGIN_PEER_UNAVAILABLE`/`PLUGIN_PEER_AMBIGUOUS`/`CHANNEL_IN_USE` refusals; `serverVersion` + `pluginVersion` in the `channel_join` result with `VERSION_MISMATCH` refusal; real socket-level leave/reset in `resetChannel` and `figma-client`. Per the Q11 resolution, verify the reported `ui.html` scope-ready race first: if the UI opens the socket before plugin-main acknowledges the committed scope, make it wait for the acknowledgement before joining (and add a test); if the report does not reproduce, record that in Provenance. Integration tests: zero/two plugin peers, two MCP clients, bound-peer disconnect invalidation, mismatch → detach → matching rejoin, and no cross-peer response acceptance.

**Phase 10 — Page-load isolation (D14).** Per-page `loadAsync` isolation with the shared `coverage` object across the multi-page read surface; the Q12 bounded per-page timeout (hung loads become `PAGE_LOAD_TIMEOUT` page errors; the timeout value is implementation-chosen and documented as behavior); structured single-page errors replacing Figma's raw strings; `DOCUMENT_SCAN_INCOMPLETE` refusal before any destructive mutation when coverage is incomplete. Tests: variable and collection deletion fail closed on incomplete coverage (including apparently empty collections and one failing page among successes); a timed-out page yields its structured error while other pages return data; a `loadAsync` that settles after its timeout is ignored — it must not alter the returned result or enable later mutation.

**Phase 11 — Connector repair (D12).** Make `create_connection` creation-only: remove the set/check-default modes, the `clientStorage` cache, and the auto-adoption scan; require and verify `connectorId` + `connectorName` on every call; return `CONNECTOR_TEMPLATE_REQUIRED` with its recovery guidance when the template is missing or invalid; adopt the D7 envelope and D11 clone/cursor containment with committed-guard cleanup; update the reaction prompt and guides to explicit template discovery. Tests: a missing or mismatched template name fails closed with no mutation; the bootstrap error carries its recovery; injected per-item failures clean up their clones and cursors; the aggregate never reports error rows as created connections; the Design-file clone path is covered by unit tests and by the Phase 14 live matrix once a connector fixture exists.

**Phase 12 — Contract sync (D8).** Update `SAFETY.md` (G2 per the Q3 resolution — the universal existing-object rule, published only after auditing every write tool against it; A3; Part B rows for every changed tool; G1's observable-boundary wording and the D11 residual-risk entry), `safetyContract.test.ts` (mechanical diff must pass in both directions), and the four `skills/figma-edit/references/` guides / `figma-edit://guide/*` resources (each states the G2 rule in exactly one sentence; the batch row contract in one sentence — every row carries `nodeId`/`status`/`error`, per Q25; `error-playbook.md` gains an entry for every D9 code).

**Phase 13 — Version & docs.** Bump `2.3.2 → 2.3.3` on all surfaces (`check:versions` passes); rebuild the plugin bundle so `check:plugin` passes; add the `CHANGELOG.md` v2.3.3 entry with a before/after example for every breaking repair; confirm the full unit suite (`bun run test`) and existing checks stay green.

**Phase 14 — Verify.** `bun run build:all`, `bun run check:plugin`, `bun run check:versions`, `bun run check:types:plugin`, and `bun run test` all pass. IDE opens the plugin with no spurious ambient-global errors. For Track 1, a rebuild-diff of the Phase 1–2 commits confirms the type fixes did not alter emitted JS; every other emitted-JS change must map to D5–D14. Track 2 **requires live Figma verification** — the original probes plus the Rev 11 additions: ID-only `UPDATE_VARIABLE` refused; `style_manage`-by-ID without `currentStyleName` refused (and a properties-only update no longer renames); `CREATE_VARIABLE` without `collectionName` or `scopes` refused; per the Q2 resolution, `parentNodeName` omission rejected at the schema boundary (Phase 5 tests) and a live *mismatched* name returning the structured `PARENT_NAME_MISMATCH` refusal (coded per Q22, Rev 31); `[]` and duplicate-node `node_delete` batches refused with no mutation; a normal multi-item batch returning one ordered row per input; an annotation append through MCP succeeding and being rediscovered via `annotation_list`; a nested `node_flatten` result staying under its original parent; a `create_component_set` landing under its supplied parent; `channel_join` reporting both versions for one bound peer and refusing an empty or two-plugin channel; `create_connection` without a valid template returning `CONNECTOR_TEMPLATE_REQUIRED`, and — once a pasted connector fixture exists — an explicit-template connection returning per-item truth (fixture-dependent; recorded as blocked if unavailable, per the Gap 9 precedent). Two Rev 20 additions: `create_instance`'s response includes `componentId` matching the resolved component (baseline: the 2026-07-17 `p28j` probe showed the advertised field absent), and one **manual plugin-UI check** — a percent-encoded scope link (`node-id=1%3A2`) pasted into the plugin's scope box settles whether the sandbox provides `URL` (validates ⇒ add an ambient declaration beside `TextDecoder`'s and delete the suppression; "Node not found" while the dash form validates ⇒ record the absence and defer the `decodeURIComponent` regex fix to post-release UI polish). The `URL` path is unreachable over MCP (UI-only `validate-scope-link`; MCP node-ID URLs are converted server-side), so this probe is manual by necessity and its outcome is recorded in Provenance either way.

---

## Testing & rollout

- **Type gate:** `check:types:plugin` returns 0 errors and is in CI, with the companion `check:suppressions` gate in the adjacent step (Rev 21, ratification 5): no `@ts-ignore`, no `@ts-nocheck`; `@ts-expect-error` only with a meaningful description; directive recognition AND classification are delegated to TypeScript's own `commentDirectives`/`directive.type`/`checkJsDirective` (Rev 37 / Q28, hardened Revs 39–40); the checker's differential fixtures — the regex-desync regression plus the starred-block/`////`/non-final-line/`@ts-nocheck`/suffixed-`@ts-ignore`/description-mentions-`@ts-ignore` cases ground-truthed to TS — run in the unit suite.
- **No emitted-JS drift from Track 1:** rebuilding `figma_plugin/code.js` after the type fixes produces no functional change — the residual fixes are types/casts/config only. Confirm via `check:plugin` (rebuild + `git diff`); any *intended* diff must map to the deliberate D5–D14 edits, reviewed explicitly. Strengthened by Rev 21 (review decision 3): the Track 1 portion of the rebuild diff contains no code-line changes at all — comment-text only, machine-distinguishable from the D5–D14 edits.
- **Fail-closed tests (D5/D6):** schema-level tests that the newly required fields are required (including `scopes` on create and `create_component_set`'s parent); handler tests that missing, stale, and mismatched `currentVariableName`/`currentStyleName`/`collectionName` are rejected before any mutation; a supplied `value` with a missing `modeId` mutates nothing; an alias `value` with an unresolvable target mutates nothing and a PAINT-style bind against a style with no paints mutates nothing (Q17); a `style_manage` update without `name` does not rename; an injected unexpected mid-update failure returns a D9 error whose `details` carry `partialMutation: true`, the what-changed statement, and before-values, a clean failure never carries the flag, and the field names match D7's batch-row vocabulary (Q18). A created TEXT style loads its own actual default font — asserted against a mock whose default is deliberately not Inter (Q19).
- **Batch semantics tests (D7):** the three-layer boundary holds (`[]`, nested unknown keys, and duplicates are schema rejections with no envelope; refused calls are structured errors with no mutation); accepted calls return `status`, `skippedCount`, and exactly one ordered row per input; `success === (status === "success")` holds as a property across all four aggregators; failure/skip entries carry actionable reasons; a failure row following a partial write carries `partialMutation: true`, `whatChanged`, and its before-values (the text tool's is `fontName`, per Q24) while a clean failure never does — the zero-mutation text throw paths are asserted to carry no flag (Q9/Q24); all four aggregators' rows share the `nodeId`/`status`/`error` keys, asserted across the four handlers (Q25); registered output schemas match handler field names via registered-callback tests.
- **Recursive strictness (D8):** unknown keys injected at every object depth are rejected on every non-catchall tool; the intentional catchalls are enumerated and tested as the only exceptions.
- **Annotation (D10):** a conforming append succeeds end to end and is rediscovered via `annotation_list`'s corrected grouped output; legacy `annotationId`/`status` fields are rejected, not stripped; category mismatches fail before mutation; metadata reports non-idempotent.
- **Containment (D11):** mocked call traces show insertion as the immediate next synchronous call after every implicit creation (text before font awaits); later failures clean up uncommitted artifacts; flatten and component-set use direct-parent placement.
- **Peer binding (D13):** one MCP session routes only to its bound plugin peer; empty/ambiguous/in-use channels and known version mismatches are refused; leave/disconnect invalidates the binding and blocks tools until rejoin.
- **Page coverage (D14):** one failing page yields partial data plus `coverage.complete: false` on reads and `DOCUMENT_SCAN_INCOMPLETE` refusal on destructive scans; single-page failures return structured codes, not raw Figma strings; a hung load times out into `PAGE_LOAD_TIMEOUT` and its late settlement is provably ignored (Q12).
- **Connectors (D12):** template omission or name mismatch fails closed with no mutation; `CONNECTOR_TEMPLATE_REQUIRED` carries its recovery; injected per-item failures leave no leaked clones or cursors; aggregates report per-item truth and never count error rows as created connections.
- **Contract sync (D8):** the `safetyContract.test.ts` mechanical diff passes in both directions after the `SAFETY.md` matrix updates.
- **Refusal messages (D9):** every new or changed refusal uses the `Operation Denied:` prefix (safety policy) or a stable code (operational), embeds its recovery (distinct cause, the read tool, "pass it back verbatim"), and survives the transport as structured `{error}` — asserted at the MCP boundary, not only plugin-internally. Review check per message: the correct retry is derivable from the error text and the tool list alone. `channel_join` failures return their originating codes verbatim — unknown codes included — with no code derived from message text (Q20).
- **Live probes:** the Phase 14 matrix passes against a live document.
- **Full suite:** `bun run test` stays green (647+ tests as of v2.3.2, plus the new D5–D14 tests).
- **Version:** `check:versions` green at `2.3.3`.
- **Rollout:** merge after v2.3.2; tag only after CI (now including `check:types:plugin`) passes.

---

## Risks & mitigations

| Risk | Likelihood | Mitigation |
| :- | :- | :- |
| A residual fix accidentally changes runtime behavior on a live-verified path | Low | D4 stop-and-escalate rule; `check:plugin` rebuild-diff catches any unexpected emitted-JS change; the fixes are types/casts/config only |
| Fixing `shared/nodeTypes.js` resolution un-masks further errors in `shared/` | Low–Med | Include only the imported `../src/shared/nodeTypes.ts` file, triage its surfaced error explicitly, and do not make the entire shared directory a plugin root |
| `TextDecoder` ambient declaration drifts from a future `@figma/plugin-typings` that adds it | Low | Prefer the typings if a version provides it; otherwise a one-line ambient `declare` with a comment pointing here |
| Turning on the CI gate blocks unrelated PRs if the residual isn't truly zero | Low | D3 sequencing — gate added only after Phase 2 reaches 0; prove-red/prove-green before wiring into CI |
| Existing agent workflows break on the newly required fields (D5/D6) | Med | Intended fail-closed tightening (v2.2.0 precedent); structured errors name the missing field; guides/error-playbook updated in the same change (D8); `CHANGELOG.md` calls out every new requirement |
| `SAFETY.md` matrix and `safetyContract.test.ts` drift apart during the change | Low | D8 same-change rule; the bidirectional mechanical diff fails CI on any mismatch |
| New `status` field breaks outputSchema validation on older clients | Low | Additive only; `looseOutput` catchall accepts extra keys by design |
| Agents keying on `node_delete`'s old lenient `success` misread the corrected value | Low | The corrected value is the safe direction (false where formerly true); descriptions and guides explain `status` |
| Recursive strictness rejects nested fields older clients had silently ignored | Med | Desired correction of silent intent loss; the MCP validation error names the exact path; CHANGELOG migration notes list removed/renamed nested fields |
| Peer binding changes the socket's broadcast behavior for existing setups | Med | One-to-one pair table with per-peer response correlation; explicit leave/unbind; integration tests cover two-plugin, two-client, and disconnect cases |
| Rev 11 scope growth makes the release harder to review | Med | The follow-up review's heavy machinery is explicitly deferred (see non-goals); remaining phases land in dependency order under the Phase 3 type gate with phase-scoped commits/diffs. Rev 28 records the Phase 1–2 exception: one combined commit, one immediate missing-file follow-up, and unrelated documentation churn preserved in history. |
| Removing the connector default/cache breaks existing default-template workflows | Low–Med | The default only saved re-passing an ID the caller had to discover once anyway; `CONNECTOR_TEMPLATE_REQUIRED` teaches the explicit flow; CHANGELOG migration example |

---

## Provenance

| Item | Verified at | Finding |
| :- | :- | :- |
| Broken `typeRoots` | `figma_plugin/tsconfig.json` | `["../../node_modules/@figma"]` resolves outside the repo; `@figma/plugin-typings` never loads |
| No plugin type-check in build/CI | `package.json`, `figma_plugin/build.js`, `.github/workflows/ci.yml` | Plugin ships via esbuild only; no `tsc` step exists — breakage is invisible |
| Path fix insufficient | `tsc --noEmit` probe | Corrected `typeRoots` still yields 309 errors; typings load only via `types: [...]` |
| `dom` lib conflict | `tsc --noEmit` probe | `lib: [..., "dom"]` redeclares `console`/`fetch`/`Navigation` against the Figma typings (TS2451/TS2300) |
| Decided-fix residual = 10 | `tsc --noEmit` probe | `types: ["@figma/plugin-typings"]`, no `dom`, exact shared file included: 4 `never`-narrowing, 1 `loadAsync`, 2 broken import paths, 2 `TextDecoder`, and 1 shared `z.record` signature |
| Not a runtime bug | v2.3.2 live testing (2026-07-06) | All affected paths behave correctly live; this is a lost type-safety net, not a live defect |
| Gap 1, variable path | `variableHandlers.ts:1032` + live probe (2026-07-09) | ID-only `UPDATE_VARIABLE` mutates unverified; a wrong `currentVariableName` is refused only when volunteered |
| Gap 1, style path | `styleHandlers.ts:13–23, 47` + live probe (2026-07-09) | Update-by-`styleId` has no current-name check; required `name` renames the style as a side effect of every update |
| Gap 1, contract contradiction | `SAFETY.md` G2 vs Part B rows | G2 claims "every write tool" verifies names; the matrix rows for `variable_manage`/`style_manage` list no name gate |
| Gap 3 | `create.ts` schemas vs `main.ts:151` + live probe (2026-07-09) | Schema-optional, runtime fail-closed; `PARENT_NAME_MISMATCH` text misdirects toward `parentId` when the name was omitted |
| Gap 5 | `nodeModifiers.ts:294` + live probe (2026-07-09) | `success: successCount > 0`; live duplicate-delete batch returned `{success:true, nodesDeleted:1, nodesFailed:1}`; the other three aggregators already require zero failures |
| Gap 1, late-mutation ordering | `variableHandlers.ts:1040–1059`; `styleHandlers.ts:43–135` (re-verified 2026-07-10) | `UPDATE_VARIABLE` applies name/description/scopes before discovering a missing `modeId`; style updates apply name/properties before binding resolution; rollback removes only freshly created styles |
| `CREATE_VARIABLE` scopes omission | `variableHandlers.ts:990` vs the `variable.ts` field description (2026-07-10) | Omitted `scopes` silently inherits Figma's default despite the "ALWAYS set explicitly on create" contract |
| Gap 6, annotation schema seam | `annotation.ts:39–60` vs `annotationHandlers.ts:142–144` (verified 2026-07-10) | Schema requires `categoryId`, exposes unsupported `annotationId`/`status`, omits required `labelMarkdown`; `idempotentHint: true` on an append; no conforming call can succeed |
| Gap 6, annotation read drift | `annotation.ts:18–21` vs `annotationHandlers.ts:48` (verified 2026-07-10) | Registered output advertises `annotations`; the page-mode handler returns `annotatedNodes`; `looseOutput` masks the drift |
| Gap 7, shallow strictness | `tools/index.ts:31–40` (verified 2026-07-10) | Central `.strict()` applies to the top-level object only; nested objects silently strip unknown keys |
| Gap 7, empty batches | batch input schemas (verified 2026-07-10) | No batch array has `.min(1)`; `[]` passes the schema boundary — corrects Rev 9's "unreachable" claim |
| Gap 7, missing rows | `textHandlers.ts:108–169` (verified 2026-07-10) | Stop-on-first `break` emits no rows for unattempted items |
| Gap 7, output drift | `node.ts:181` (`deletedCount`), `text.ts:25` (`count`) vs handler returns (verified 2026-07-10) | Advertised output fields do not exist in handler outputs (`nodesDeleted`/`replacementsApplied`); `looseOutput` hides it |
| Gap 8, flatten/creators/component set | `nodeModifiers.ts:505`; `nodeCreators.ts:74–146, 363–396, 454`; `componentHandlers.ts:962, 984–985` (verified 2026-07-10) | `figma.flatten` without a parent defaults to `currentPage`; creators create-then-append with fallible setters between; `create_text` awaits fonts before append; component set combines on the containing page and reparents afterward |
| Gap 10, unbound channel | `socket.ts:139–155`; `figma-client.ts:295–297`; 4n79 live join (2026-07-09) | Broadcast reaches every channel peer with no identity; `resetChannel` never leaves the socket room; the join payload carries no plugin version |
| Gap 11, error flattening & page loads | `main.ts:319–324`; `figma-client.ts` error rebuild; `nodeReaders.ts` per-page `loadAsync` (verified 2026-07-10) | Plugin failures are flattened to strings end to end; one unloadable page fails an entire multi-page read with Figma's raw error |
| Gap 9, connectors | `connectorHandlers.ts:188–196, 245, 199/253/457, 349/398–401, 493–513, 603–621, 635–639` + live `90vr` probes (2026-07-10) | All five reported defects confirmed, plus: `count` counts error rows as connections; cursors leak on later same-item failure. Live: no-default flow, type-only template check, and the Design-file bootstrap dead-end reproduced; clone/cursor-leak and mixed-result probes blocked pending a FigJam connector fixture |
| `create_instance` dead `componentId` field | `componentHandlers.ts:306` + live probe on `p28j` (2026-07-17) | Response omits the advertised field — `InstanceNode.componentId` does not exist at runtime, so the `undefined` key is dropped in serialization; the fix is `component.id` from the already-resolved component, which both resolution paths hold (Phase 8, Rev 20) |
| `parseNodeIdFromUrl` `URL` reachability | `main.ts:231, 272` (verified 2026-07-17) | Only the plugin UI's `validate-scope-link` message reaches `new URL()`; MCP traffic uses server-side dash→colon ID conversion and never touches it. Live probe blocked for MCP (Gap 9 precedent); manual percent-encoded scope-link check added to Phase 14 with both outcomes pre-decided (Rev 20) |
| Residual #6–7 mechanism | git history (`src/shared/nodeTypes.ts` created at `src/shared/` in v1.4.0; no repo-root `shared/` ever existed) + esbuild elision probe (2026-07-17) | The `../../shared/nodeTypes.js` import never resolved for esbuild — type-only imports are elided before resolution — so the Rev 1 claim that esbuild resolved it was wrong and the config-only disposition was unimplementable. Fixed by rewriting the two imports; bundle-identical (Rev 22) |
| Server-side single-arg `z.record` drive-bys | `src/mcp_server/tools/annotation.ts:48`, `create.ts:259`, `node.ts:67,725` (2026-07-18) | Three more single-argument `z.record` sites — the same zod-4 type error Rev 22 fixed in `src/shared/nodeTypes.ts` — corrected to the two-argument form during the Phase 3–4 implementation-review fixes. Runtime-equivalent; kept with this recorded note per the minor-ratifications list (Rev 27) |
| Tenth residual (shared include) | `src/shared/nodeTypes.ts:40` + zod 4.4.3 runtime probe (2026-07-17) | Including the exact imported `../src/shared/nodeTypes.ts` file surfaced single-argument `z.record(z.unknown())` (TS2554) — the risk table's predicted shared-source unmasking case. Fixed to `z.record(z.string(), z.unknown())`; runtime-equivalent (single-arg still parses under zod 4.4.3) and `NodeEntrySchema` has no importers. The decided-fix residual was therefore 10, not 9; this row is the recorded note the risk mitigation requires (Rev 22/28) |
| Phase 1–2 completion range | commits `bf4a4c0`, `3f62505` + `bun run check:types:plugin` reconstruction (2026-07-18) | `bf4a4c0` omitted `figma_plugin/types/sandbox-globals.d.ts` and therefore still produced two TS2304 errors; immediate follow-up `3f62505` added the missed file and reached zero. The phase is green at the range tip, not at the first commit alone; the historical commits are preserved. |

---

## Revision history

- **Rev 1, 2026-07-06** — initial PRD. Discovery during v2.3.2 Phase 8.5 review; empirical intermediate measurement of the fix (path-only insufficient; `types` + no-`dom`, before the explicit shared root → 9 residuals); initial triage; decision to fix-not-suppress and add a CI type-check gate sequenced after the residual reaches zero. Rev 22/28 correct the decided final configuration to 10 after the exact imported shared file is included.
- **Rev 2, 2026-07-09** — added Track 2 (safety-contract gap closure). Source: the adversarial review `figma-edit-mcp-gap-details.md`, with every claim re-verified against source and live against a Figma test document. Adds D5–D8 (required current-name verification for variable/style updates and variable creation; schema-required `parentNodeName`; honest batch success semantics with tri-state `status`; same-change contract-doc sync), Phases 4–9 renumbering, and the compatibility posture for the fail-closed tightenings. Release identity updated accordingly — v2.3.3 is no longer a no-behavior-change release.
- **Rev 3, 2026-07-09** — resolved open-questions Q1 (schema mechanism for D5's conditionally required fields): **Option B** — flat schemas, requirement stated in both field and tool descriptions, server-side `.superRefine()` with actionable errors, plugin fail-closed check retained as defense in depth. Recorded as the D5 mechanism bullet; Phase 4 updated to reference it. Q2–Q8 remain open in `open-questions.md`.
- **Rev 4, 2026-07-09** — resolved open-questions Q2 (Gap 3 verification plan): **Option A** — the omission and mismatch cases are verified separately, at the layer that enforces each. Schema-boundary tests (Phase 5) cover omission; the Phase 9 live probe covers mismatch; the plugin error keeps its "missing" branch as defense in depth for non-conforming clients. Recorded in D6; Phase 9's probe list updated. Q3–Q8 remain open.
- **Rev 5, 2026-07-09** — resolved open-questions Q3 (SAFETY.md G2 target wording): **Option A** — G2 becomes the universal existing-object rule ("no write against an existing object proceeds unless the caller-supplied current name matches; creation verifies the identified parent or collection instead"), published only after the Phase 7 audit of every write tool against it. Recorded in D8; Phase 7 updated. Q4–Q8 remain open.
- **Rev 6, 2026-07-09** — resolved open-questions Q4 (refusal-message convention): **Option A**, recorded as new decision **D9** — `Operation Denied:` prefix, centrally defined, playbook entry per refusal, and every message embeds its own recovery (distinct cause, the read tool that supplies the correct value, "pass it back verbatim"); acceptance check: the correct retry is derivable from the error text and tool list alone. Phases 4, 5, 7 and the testing list reference it. Q5–Q8 remain open.
- **Rev 7, 2026-07-09** — resolved open-questions Q5 (versioning posture): **Option A** — the release stays 2.3.3, and the rationale is recorded as policy in the Release identity block: fail-closed safety tightenings may ship at patch level; the CHANGELOG carries the breaking-change notice; revisit only if external integrators pin versions. Q6–Q8 remain open.
- **Rev 8, 2026-07-09** — resolved open-questions Q6 (D3 "added last" wording): **Option A** — D3 reworded to sequence the CI type gate by condition (residual reaches zero), not position; the gate stays at Phase 3 so the Track 2 handler edits (Phases 4–6) land under its protection. Q7–Q8 remain open.
- **Rev 9, 2026-07-09** — resolved open-questions Q7 (D7 edge semantics): **Option A** — one shared contract across all four batch aggregators: `status` derived from the succeeded/failed counts, present on every return path (early returns ⇒ `"failed"`), the derived-boolean invariant `success === (status === "success")` as a property test, and actionable per-item reasons in `results`. Recorded in D7; Phase 6 and the testing list updated. Q8 remains open.
- **Rev 10, 2026-07-09** — resolved open-questions Q8 (migration posture): **Option A** — hard cutover confirmed, no warn-only transitional release; recorded in the Compatibility posture section. **All eight open questions are now resolved**; `open-questions.md` is retained as the decision record, and Phases 4–7 are unblocked.
- **Rev 11, 2026-07-10** — incorporated the independently re-verified findings of the follow-up adversarial review (`revised-prd.md`, live channel `pn68`; re-verification locations in Provenance): Gap 6 (unusable `annotation_set` contract; `annotation_list` output drift), Gap 7 batch corrections (empty arrays pass the schemas — **corrects Rev 9's false "unreachable" claim** — missing rows on stop-on-first, accepted duplicates, output-schema drift, shallow strictness, dispatcher-throw layering), Gap 8 containment (flatten/creators/`create_text`/component-set/clone), Gap 10 (unbound plugin peer, no version in the join, no real leave), Gap 11 (string-flattened errors, all-or-nothing page loads), plus Gap 1 additions (late-mutation ordering, omitted `scopes`). Adds D10, D11, D13, D14 (lite variants) and D12 (reserved/deferred); amends D5, D6, D7, D8, D9; renumbers Phases 4–9 → 4–13. **Not adopted — deferred for explicit decision:** batch effect-state algebra and content digests, JCS/SHA-256 payload and protocol digests, page-scan scheduler constants, the connector redesign, the Q1 reversal to discriminated unions.
- **Rev 12, 2026-07-10** — resolved open-questions Q9 (batch mutation truth): **Option B** — minimal per-row partial-mutation disclosure on the tools that can mutate then fail (text content, instance overrides): failure rows gain `partialMutation: true`, a plain statement of what changed, and before-values (the original `characters`, the original `mainComponentId`) so the restoring write composes directly from the error in one round trip. The follow-up review's full effect-state algebra and SHA-256 content digests are **rejected** on the shared criterion — a hash cannot tell the model what to write back. Recorded in D7; Phase 6, scope, and the testing list updated. Q10–Q15 remain open.
- **Rev 13, 2026-07-10** — resolved open-questions Q10 (annotation retry identity): **Option B** — D10's per-item before/after counts plus list-before-retry guidance are confirmed as the contract; the JCS/SHA-256 payload digest is **rejected** on the shared criterion (an LLM cannot compute a hash, and in the timeout case the digest exists for, no digest ever reached the model — read-and-compare via `annotation_list` is one round trip within the model's native capability). Recorded in D10. Q11–Q15 remain open.
- **Rev 14, 2026-07-10** — resolved open-questions Q11 (handshake depth): **Option B** — D13 lite confirmed; protocol digests, nonces, and scope fingerprints **rejected until evidence** (recorded escalation trigger: a live same-version different-contract skew that version fields could not catch). Rationale: released-version skew is already covered by version fields plus release-time coupling, and neither mismatch code is model-recoverable, so the machinery adds no recovery and only contributor-side first-call protection. The unverified `ui.html` scope-ready race becomes a Phase 9 verification task with a defined fix if confirmed. Recorded in D13. Q12–Q15 remain open.
- **Rev 15, 2026-07-10** — resolved open-questions Q12 (page-scan scheduling depth): **Option B** — D14 lite plus one bounded per-page timeout so a hung `loadAsync` becomes a structured `PAGE_LOAD_TIMEOUT` error instead of a queue-wedging hang with no error to recover from; the timeout value is implementation behavior, not contract. The follow-up review's pinned constants, concurrency caps, total deadlines, and heartbeat cadence are **rejected** as model-invisible. Late settlements must be provably ignored. Recorded in D14; Phase 10 and the testing list updated. Q13–Q15 remain open.
- **Rev 16, 2026-07-10** — completed the Q13 verification step (source read + live probes on channel `90vr`): all five Gap 9 connector defects **confirmed**, with two additions (error rows counted as created connections; cursor leak on later same-item failure). The Design-file bootstrap dead-end reproduced live; clone/cursor-leak and mixed-result probes are blocked pending a FigJam connector fixture and remain source-verified. Gap 9 problem statement, D12, and Provenance updated. Q13 now reduces to the product decision alone (in-shape fix vs. redesign); Q13–Q15 remain open.
- **Rev 17, 2026-07-10** — resolved open-questions Q13 (connectors): **revised hybrid** — `create_connection` becomes creation-only with a required, name-verified `connectorId` + `connectorName` on every call; the `clientStorage` cache and `currentPage` auto-adoption are removed in favor of the structured `CONNECTOR_TEMPLATE_REQUIRED` bootstrap error; results adopt the D7 envelope and clones/cursors fall under D11 cleanup; Design-file support is kept and the follow-up review's FigJam-only restriction is **rejected** (the flagship reaction-flow use case lives in Design). Evidence basis: the Rev 16 verification showed the hidden default bought nothing (bootstrap requires discovery once anyway) while costing first-call correctness on every call. Recorded as D12; new Phase 11 inserted (old Phases 11–13 → 12–14); scope, posture, testing, and risks updated. Q14–Q15 remain open.
- **Rev 18, 2026-07-10** — resolved open-questions Q14 (schema mechanism, revisited): **Option A** — the Q1/Rev 3 mechanism (flat schemas, dual descriptions, `.superRefine()`, plugin fail-closed backstop) is **reaffirmed** and the follow-up review's reversal (discriminated unions; new required `action` on `style_manage`) is **rejected**: its gain is advertisement for a secondary consumer, its errors are terser than the authored messages, and the new field would guarantee migration first-call failures. CI expresses requiredness via `safeParse`-behavior and description-marker tests; revisit trigger recorded (observed omission of twice-documented fields at a meaningful rate). Recorded in D5; Phase 4 updated. **Q15 is the last open question.**
- **Rev 19, 2026-07-12** — recorded the Q15 resolution (release sizing, resolved 2026-07-10): **Option C** — v2.3.3 ships as a **single release containing every phase of this PRD**; the Option A split (Phases 1–7 first, remainder in v2.4.0) was declined in favor of one release cycle, one contract-doc/guide sync pass, and Q5 policy continuity, with the reviewability risk carried by the risk table's existing mitigations. Recorded in the Release identity block; the stale "Q15 remains open" wording in the Track 2 open-questions note and the non-goals is corrected. **All fifteen open questions are now resolved; implementation is unblocked.**
- **Rev 20, 2026-07-17** — folded in two live-verified findings from the Phase 1–2 adversarial-review follow-up (the plugin-wide `@ts-ignore` → `@ts-expect-error` conversion: 72 sites converted, 13 dead directives removed, the dead `handlers/index.ts` re-export module deleted, two optional-parameter signatures corrected; 50 annotated suppressions remain, tracked for touch-it-clear-it burn-down). Finding 1: `create_instance`'s `componentId` response field is dead — `InstanceNode.componentId` does not exist at runtime (live probe, channel `p28j`) — fixed in Phase 8 as an additive behavior repair (`component.id` from the already-resolved component). Finding 2: the `new URL()` site in `parseNodeIdFromUrl` is unreachable over MCP (UI-only `validate-scope-link` path), so a manual percent-encoded scope-link check joins the Phase 14 matrix with both outcomes pre-decided. Provenance and Compatibility posture updated; the Phase 13 CHANGELOG checklist gains the additive field.
- **Rev 21, 2026-07-17** — recorded the acceptance of the remaining Phase 1–2 adversarial-review decisions (2–5; decision 1 was executed under Rev 20). Decision 2: a companion `check:suppressions` CI gate beside `check:types:plugin` (Phase 3) — `@ts-ignore` banned in the plugin source, `@ts-expect-error` requires a same-line description, and failures name the file:line and the fix. Decision 3: the two `never`-narrowing fixes are reworked to a zero-emitted-JS form before the Phase 1–2 commit, strengthening the Track 1 rebuild-diff expectation to comment-text only (Phase 2, Phase 14, Testing & rollout). Decision 4: the statically-unreachable parent guards are kept as runtime defense in depth, with a rationale comment at both sites (Phase 2). Decision 5: the MCP-server type-check gap becomes tracked follow-up work with `check:types:server` as its acceptance shape (non-goals; the task list's follow-ups section).
- **Rev 22, 2026-07-17** — provenance corrections and Track 1 cleanup from the review's no-decision actions. Corrected the residual #6–7 record: esbuild elides the type-only shared import and never resolved it (no repo-root `shared/` has ever existed), so the config-only disposition was unimplementable and the implemented fix — rewriting the two imports — is recorded, bundle-identical. Recorded the tenth residual: single-argument `z.record` in `src/shared/nodeTypes.ts`, surfaced by making the imported shared file an explicit plugin root and fixed runtime-equivalently — the recorded note the risk table's shared-source unmasking mitigation requires. Removed the dead `baseUrl`/`paths`/`ignoreDeprecations` chain from the plugin tsconfig (probe-verified: 0 errors without it). Executed the Rev 21 Phase 2 additions (zero-drift inline casts; guard-rationale comments) — the Track 1 bundle diff is now comment-text only, as Rev 21 requires. task.md: corrected five broken Phase 2 file links and added the run-the-suite step Track 1 demonstrably needed (the Phase 4 §3a static test had to be updated for the `PageNode` cast).
- **Rev 23, 2026-07-18** — resolved open-questions Q16 (error-code inventory, raised 2026-07-17 by the Phase 3–4 implementation review): **Option A** — the ratified inventory (ten operational codes + six D5 verification codes + `UNKNOWN_ERROR` as the documented legacy fallback), codes never derived from prose (structured objects pass through; everything else defaults to `UNKNOWN_ERROR`; the dispatcher's substring matching is deleted), every coded refusal built by central message factories, and the client's `JOIN_ERROR` default removed. Recorded in D9's code-inventory note; the task-list header inventory amended. The legacy conversion (332 inline throw sites across 15 files) is deferred to the new [v2.3.4 PRD](../v2.3.4/prd.md). Q17–Q19 (validate-before-mutate scope, non-batch partial disclosure, TEXT-style font order) remain open.
- **Rev 24, 2026-07-18** — resolved open-questions Q17 (validate-before-mutate scope): **Option B** — the letter plus the two cheap predictable checks, both before any mutation: an alias `value` on `UPDATE_VARIABLE` resolves its target variable (`getVariableByIdAsync`), and a PAINT-style `bindVariables` request verifies the style has paints before `name`/`description`/`properties` apply. Deep value-type/coercion simulation is **rejected** — a wrong pre-validation would refuse calls Figma accepts, and drifts with the API. The boundary rule is recorded in D5 for future tools: pre-validate what one read can confirm; disclose what only execution can reveal. Scope item 4, Phase 4, and the testing list updated; the Track 2 open-questions note now records Q16–Q19. Q18–Q19 remain open.
- **Rev 25, 2026-07-18** — resolved open-questions Q18 (non-batch partial-disclosure shape): **Option A** — `variable_manage`/`style_manage` report unexpected mid-update failures via the D9 error's `details` carrying the Q9 vocabulary (`partialMutation: true`, a plain-language what-changed statement, cheap before-values — the original `name`/`description`, already held from verification); a clean failure never carries the flag; tool-local rollback **rejected** (the no-transaction posture; a failed rollback is an undisclosed partial). The field names are recorded as shared with D7's batch failure rows so Phase 4 and Phase 6 cannot drift. D5 and D7 updated; Phase 4/6 tasks and the testing list updated. Q19 remains open.
- **Rev 26, 2026-07-18** — resolved open-questions Q19 (TEXT-style font-loading order): **Option A** — font loading is hoisted only for updates (the target font — supplied `properties.fontName`, else the style's actual `fontName` — loads before any mutation); creates return to create → read the fresh style's **actual** font → load → write properties, inside the existing rollback guard. The hardcoded `{family: "Inter", style: "Regular"}` guess is **rejected** as a contract-invisible, document-dependent failure; a fork comment records why the two paths order operations differently, and the unit-test mock's default font must not be Inter so the guess cannot silently return. Recorded in D5; scope item 4, Phase 4, and the testing list updated. **Q16–Q19 are all resolved; nothing blocks Phase 4 finalization.**
- **Rev 27, 2026-07-18** — implementation of the Phase 3–4 review fixes and the Q16–Q19 resolutions, with three records: (1) the code inventory gains a **seventh** D5 verification code, `VARIABLE_SCOPES_MISSING` — the handler backstop for required `scopes` is a refusal this release adds, so D9's "adds or edits" rule requires it to be coded; D9 and the task-list header amended. (2) The four page-family operational messages (`PAGE_LOAD_FAILED`, `PAGE_NOT_FOUND`, `TARGET_NOT_PAGE`, `PAGE_LOAD_TIMEOUT`) drop the "Operation Denied:" prefix (D9 reserves it for policy/verification refusals) and gain embedded recovery; `DOCUMENT_SCAN_INCOMPLETE` keeps the prefix as a fail-closed destructive refusal. (3) The three server-side single-argument `z.record` drive-by fixes are recorded in Provenance per the minor-ratifications list. Implementation summary: the dispatcher's test-environment compat shim removed and all dispatcher tests migrated to the structured envelope; `getStructuredError`'s prose-matching deleted (structured pass-through, else `UNKNOWN_ERROR`); the verification refusals moved into central `REFUSALS` message factories carrying operands + read tool + "pass it back verbatim"; the Q17 alias-target and paints-present pre-checks, Q18 `withPartialDisclosure` on both handlers, and Q19 font-loading order implemented with their tests; the `joinErrorCode` regression fixed (`channel.ts` reads `error.code`; the client's `JOIN_ERROR` default removed); the transport tests now exercise the exported production wrapper and the real dispatcher.
- **Rev 28, 2026-07-18** — resolved open-questions Q20 (`channel_join` failure handling; raised by the follow-up implementation review, finding P4-4): **Option A** — the fix splits along the recorded decision boundary. v2.3.3 fixes the Q16 violations inside the existing in-band envelope: structured codes pass through verbatim (unknown codes never collapse to `UNKNOWN_ERROR`), `details` is preserved, and the prose-classification branches are deleted — the two locally-generated failures are coded at origin in `figma-client` (`CHANNEL_JOIN_FAILED` on request timeout, `PLUGIN_DISCONNECTED` on connection close; both pre-existing values, now assigned where the error is created). Envelope convergence to `isError` remains deferred to the v2.3.4 PRD's Q1, whose context records this split. Pulling Q1 forward was rejected: it would resolve a documented open decision as a side effect of a bug fix, and it depends on the review's P4-1 remediation landing first. Recorded in D9; Phase 4 tasks and the testing list updated. Q21 (dual-description wording, finding P4-8) remains open.
- **Rev 29, 2026-07-18** — resolved open-questions Q21 (dual-description wording; raised by the follow-up implementation review, finding P4-8): **Option B** — D5's example wording is ratified as the contract: the literal "REQUIRED for …" marker lives in the field description, and the tool description states the requirement as a natural sentence naming the action and the field ("UPDATE_VARIABLE requires `currentVariableName`"). The Q14 sentence in D5 — whose "'REQUIRED for …' in both" phrasing loosely summarized the test mechanism and contradicted the bullet's own example — is corrected in place. Adopted regardless of wording (the valid half of P4-8): the marker tests assert against the emitted `tools/list` result, not raw registration configuration. Zero description churn — the implementation already matches the ratified form. Recorded in D5; the Phase 4 marker-test task updated. **All twenty-one open questions are resolved; the follow-up review's remaining findings are remediation work under recorded decisions, not open decisions.**
- **Rev 30, 2026-07-18** — remediation of the follow-up review's findings (P3-1–P3-5, P4-1–P4-9), implemented under the recorded decisions. **P4-1 (P0):** every registered output schema is centrally relaxed to optional fields plus an advertised `error` envelope field (mechanism note in D9 — the pinned SDK client validates `structuredContent` on error results and cannot advertise a union), proven by a new official-SDK boundary suite (`mcpBoundary.test.ts`: real `Client` + `InMemoryTransport` + registered production server; coded refusal arrives intact; every tool's schema accepts the envelope; success validation unaffected; the Q21 dual-description contract asserted against the emitted `tools/list`). **P3-2/3/4:** `check:suppressions` rewritten — `@ts-nocheck` forbidden, directive-syntax matching with string-literal stripping, meaningful-description requirement, exported logic with negative fixtures (`checkSuppressions.test.ts`); red/green re-proven and durably recorded in the task list. **Q20 landed:** `channel.ts` keys recovery by code with verbatim pass-through (unknown codes and `details` included); `figma-client` codes the close-handler (`PLUGIN_DISCONNECTED`) and join-flow (`CHANNEL_JOIN_FAILED`) failures at origin. **P4-2/3/6:** create/update splits on `styleId` *presence* with empty IDs schema-rejected; an empty `bindVariables` map is a documented no-op matching the Q17 pre-check's condition exactly; empty names are rejected at schema and plugin so no object can escape exact-name verification. **P4-5 lite:** a shared ratified-code inventory (`src/shared/errorCodes.ts`) used by the server layers and parity-tested against the plugin registries. **P4-9/P3-5:** task-ledger count/link corrections; "same CI step" wording aligned to the ratified adjacent-step layout. All new files tracked in git.
- **Rev 28, 2026-07-18** — closed the Phase 1–2 adversarial-review findings without rewriting history. Reconciled D1/D2, the evidence table, triage, scope, plan, risk, provenance, and task ledger to the decided count of 10; replaced the broad `../src/shared/**/*` plugin root with the exact imported `../src/shared/nodeTypes.ts`; and recorded that `bf4a4c0` omitted the ambient `TextDecoder` declaration while immediate follow-up `3f62505` supplied it and established the zero-error completion state. The unrelated documentation churn already present in `bf4a4c0` remains historical; future phase reviewability is assessed with phase-scoped diffs rather than rewriting published commits.
- **Rev 31, 2026-07-18** — resolved open-questions Q22 (parent-name refusal code regime; raised by the Phase 5–6 implementation review [reviews/Phase-5-&-6-review.md](reviews/Phase-5-%26-6-review.md), findings P5-1/S1): **Option A** — the refusal D6 edits joins the coded regime as a factory-built `PARENT_NAME_MISSING`/`PARENT_NAME_MISMATCH` pair with distinct causes and Phase 12 playbook entries, per the Rev 27 "adds or edits" precedent; the `create_component_set` missing-`parentId` branch gets its own message naming `parentId`; and D6's merged single-message prescription is corrected in place (it contradicted D9's distinct-cause rule — the Q21 in-place-correction precedent). Recorded in D6 and D9's code inventory; the task-list header inventory grows to nineteen codes plus the fallback, and Phase 5's handler work is reopened to carry the coded-pair remediation. Q23–Q26 remain open.
- **Rev 32, 2026-07-18** — resolved open-questions Q23 (duplicate-target rejection layer and code; raised by the Phase 5–6 implementation review [reviews/Phase-5-&-6-review.md](reviews/Phase-5-%26-6-review.md), findings P6-6/S2): **Option B** — duplicate rejection moves to **Layer 1** as a per-tool schema `.superRefine()` (normalizing spellings via the exported `normalizeNodeId`, authored message with recovery), alongside `[]` and unknown keys, because duplicates are payload-only validation (the Rev 11 reasoning that placed `[]` at Layer 1). **No D9 code is minted** — a Layer 1 rejection is an MCP validation error, not a coded refusal — so the header inventory is unchanged at nineteen codes plus the fallback. The plugin dispatcher's inline duplicate check is retained as **uncoded defense in depth** on the `UNKNOWN_ERROR` fallback (the Q2 precedent), resolving the P6-6 finding that the *new* refusal was an uncoded prose throw without demanding a code the schema layer makes unnecessary; its conversion rides the v2.3.4 legacy-surface burn-down. The minted-code alternative (Option A) is **rejected** — it would cost a socket round trip that boundary validation prevents. Recorded in D7 (Layer 1 and the duplicates bullet); Phase 6's duplicate-rejection task is reopened to add the schema layer and re-point its tests at the boundary. Q24–Q26 remain open.
- **Rev 33, 2026-07-18** — resolved open-questions Q24 (text partial-mutation before-value; raised by the Phase 5–6 implementation review [reviews/Phase-5-&-6-review.md](reviews/Phase-5-%26-6-review.md), findings P6-2/P6-3/P6-4/S4): **Option A** — the text failure row carries `before: {fontName}` and a `whatChanged` statement, with `partialMutation: true` set **only when the font fallback was actually applied** (requiring `setCharacters` to report that fact). This corrects the Q9 record in place (the Rev 11 precedent): the path mutates `fontName`, not `characters`, so Q9's `characters` before-value is superseded — it named an unmutated property, and the zero-mutation throw paths (node deleted, not TEXT, no-fallback assign failure) were carrying a fabricated flag and before-value (P6-2), while the text rows also lacked the `whatChanged` field the instance rows carry (P6-4). The Q18 shared-vocabulary contract is unaffected (field names unchanged; only the text before *content* changes). The instance `before: {mainComponentId}` is unchanged. Recorded in D7's Q9 bullet; Phase 6's disclosure task and its unit test are reopened to carry the `setCharacters` reporting change and assert the corrected before-value and the no-flag zero-mutation paths. Q25–Q26 remain open.
- **Rev 34, 2026-07-18** — resolved open-questions Q25 (batch failure-row vocabulary; raised by the Phase 5–6 implementation review [reviews/Phase-5-&-6-review.md](reviews/Phase-5-%26-6-review.md), finding P6-9): **Option A** — the four batch aggregators' per-item rows converge on one vocabulary (`nodeId` identity, `status`, and `error` as the actionable reason on any non-success row; the Q9/Q24 partial-mutation fields attach where applicable). `instance_set_overrides`, whose rows used `instanceId`/`instanceName`/`message`, is renamed onto it (`instanceName` may remain as an additive convenience field); the top-level `message` summary is unchanged. D7 defines "one shared contract across all four aggregators", and unifying the row keys is what makes the single retry loop the descriptions teach readable without per-tool knowledge. The divergence-as-deliberate alternative (Option B) is **rejected**: it would permanently contradict D7's own wording and keep four dialects as standing wrong-first-read hazards. The rename is breaking for instance-row readers; per Q8 (hard cutover) and Q15 (single release) it lands now — the release already changes that tool's envelope — with a Phase 13 CHANGELOG before/after example. Recorded in D7 (new row-vocabulary bullet), the Compatibility posture, and scope item 6; the Phase 6 handler/test tasks, the Phase 12 guide-sentence task, and the Phase 13 CHANGELOG checklist are updated. Q26 remains open.
- **Rev 35, 2026-07-18** — resolved open-questions Q26 (legacy batch count fields; raised by the Phase 5–6 implementation review [reviews/Phase-5-&-6-review.md](reviews/Phase-5-%26-6-review.md), finding P6-11): **Option B** — the batch handlers and schemas expose **only** the shared envelope counts (`requestedCount`/`succeededCount`/`failedCount`/`skippedCount`, with `status`/`success`/`results`); every tool-specific duplicate is removed rather than kept as a marked alias — `nodesDeleted`/`nodesFailed`, `replacementsApplied`/`replacementsFailed`, `annotationsApplied`/`annotationsFailed`, the duplicate `totalCount`, and the `totalNodes`/`totalReplacements`/`totalAnnotations` totals — under the general rule *no field duplicates an envelope count*. The mark-as-alias resolution (Option A, the original recommendation) is **rejected on the "do not accrue legacy" principle**: a marked alias is still a second vocabulary and a drift surface, i.e. cruft. This supersedes the Rev 9/earlier D7 "align advertised names to what the handlers return" instruction — the review's advertised-but-absent drift (`deletedCount`, `count`) is now eliminated by having one vocabulary rather than by aliasing. Breaking for readers of the old counts; lands now under Q8/Q15 with a Phase 13 CHANGELOG before/after example. Recorded in D7's output bullet, the Compatibility posture, scope item 6, and the Phase 6 plan; the Phase 6 output-field and registered-callback-test tasks and the Phase 13 CHANGELOG checklist are updated. **All twenty-six open questions are resolved.**
- **Rev 36, 2026-07-23** — resolved open-questions Q27 (error-registry centralization scope; raised as a spot-check follow-up on the Phase 3–4 review's finding P4-5): **Option B** — the ten Phase 9–11 operational codes move from the legacy `ERRORS` plain-string table into the `REFUSALS` factory registry alongside the D5/D6 codes, so D9's "central registry of message factories" rule is fully literal on the plugin side with no exceptions; `ERRORS` is re-scoped, by comment and by rule, to the closed pre-v2.3.3 surface only. The plugin's `UNKNOWN_ERROR` fallback becomes one named local constant instead of a repeated literal. The plugin registry and the server-side `src/shared/errorCodes.ts` mirror remain two separate definitions by design (no runtime import across the plugin-bundle boundary), now recorded as a decision rather than a stray code comment; the existing parity test is the enforcement mechanism. Full cross-boundary unification (Option A) is **rejected**: it would pull server-side modules into the plugin bundle for a gain a passing parity test already delivers. Recorded in D9's code-inventory note and in `errors.ts`'s/`errorCodes.ts`'s header comments; the Phase 4 inventory-parity test updated to assert operational codes are factories, not `ERRORS` strings. **All twenty-seven open questions are resolved.**
- **Rev 37, 2026-07-23** — resolved open-questions Q28 (suppression-checker comment detection; raised by a second spot-check on the Phase 3–4 review's P3-4 finding, which found the first-round multiline-masking fix carried a *false negative* — a regex literal inside a `${…}` interpolation desynced the hand-rolled brace counter and hid a subsequent real `// @ts-ignore`, passing a suppressed `TS2304` through the gate): **Option C** — `scripts/check-suppressions.ts` is rewritten onto the TypeScript parser (`ts.createSourceFile` comment enumeration), which is the authoritative tokenizer and eliminates the entire hand-rolled-lexer bug class (regex literals, nested templates, strings-with-braces) rather than patching instances. The `maskStringsAndTemplates` state machine is deleted; a regression test replays the exact desync repro and asserts the directive is now caught. In a safety gate the false-negative direction is the dangerous one (a hidden suppression means the type gate passes while a real error is masked), which is why the authoritative tokenizer — not a narrower hand-rolled rule (Option B) or more lexer patches (Option A) — was chosen; `typescript` was already a dev dependency, so no new dependency was added. The description requirement is reworded from "same-line" to "a meaningful description" (a block comment's description may span lines). Recorded in D3's Rev 21 bullet and the Testing type-gate bullet. The separate mechanical anchoring fix and the `UNKNOWN_ERROR` dedup from the same spot-check needed no decision and landed directly. **Q29 (D5 `.superRefine()` message centralization) remains the one open question.**
- **Rev 38, 2026-07-23** — resolved open-questions Q29 (D5 `.superRefine()` message centralization; raised by a second spot-check on the Phase 3–4 review's P4-5 finding, which noted D9's text named the server-side superRefine messages as requiring central plugin-module definition while they are authored inline): **Option B** — D9 gains a layer-scope note: the `Operation Denied:` prefix and central-`REFUSALS`-registry rule govern the *coded plugin refusals* (D5 verification errors, D6 parent-name error — they carry a `code`, are dispatcher-thrown, recur across handlers), while the server-side `.superRefine()` messages are a *different layer* (Zod schema-boundary validation, no `code`, coupled to one schema's constraint) that legitimately co-locates with its schema — it cannot source from the plugin registry the server does not import at runtime (Q27) — carries no prefix, and is held to D9's *content bar* instead. The name-verification refinements already meet the bar (name the read tool, say "pass it back verbatim"); a new Phase 4 test asserts this so it cannot regress. D9's original "governs … the D5-mechanism `.superRefine()` messages" wording is corrected in place: it predated the server/plugin registry split and demanded something structurally impossible. No production message changed. Full unification (Option A, a server-side message module) and single-sourced text via `src/shared` (Option C) were the alternatives; B was chosen because a superRefine message *is* part of the schema definition it describes, so co-location is correct design rather than a shortcut. **All twenty-nine open questions are resolved; nothing remains open.**
- **Rev 39, 2026-07-24** — hardened the Q28 and Q27 remediations after a follow-up review (`reviews/P3-4-P4-5-remaining-gaps.md`) verified two residual gaps against ground truth (real `tsc` + `sourceFile.commentDirectives`). **P3-4:** the Rev 37 checker enumerated comments correctly but re-derived directive recognition with its own regex on the comment body, which did not match TypeScript's multiline grammar — it missed a starred continuation-line block directive (an `@ts-ignore` on a ` * …` final line, which `tsc` honors: a safety-relevant *false negative*) and over-rejected inert forms (`////`, non-final block lines). Recognition is now delegated to TypeScript's own `sourceFile.commentDirectives` (`@ts-ignore`/`@ts-expect-error`) and `sourceFile.checkJsDirective` (`@ts-nocheck`), so the gate matches the compiler by construction; six differential fixtures ground-truth it and the starred form is red-proofed. **P4-5:** the inventory test proved only the registry's shape, not the production-source invariants; it now types the registry (no `as any`), asserts every ratified code (operational + D5 + D6) is absent from legacy `ERRORS`, asserts operational messages carry actionable recovery content, and adds a TS-AST source-use invariant that fails if any ratified code appears as a string literal outside `utils/errors.ts` — mechanically enforcing "every live coded throw routes through `REFUSALS`". Both P4-5 failure modes are red-proofed. No production behavior changed beyond the checker's directive recognition; suite green. Recorded in the Q27/Q28 entries and D3.
- **Rev 40, 2026-07-24** — a third recheck of `reviews/P3-4-P4-5-remaining-gaps.md` found the Rev 39 remediations still incomplete; both now closed and red-proofed. **P3-4:** Rev 39 took directive *ranges* from TypeScript but re-*classified* them with a `@ts-ignore\b` regex, which does not match TS's grammar — TS recognizes the directive as a PREFIX (no word boundary), so `@ts-ignorefoo`/`@ts-ignore123` are active `Ignore` directives the regex missed (safety-relevant false negatives), `@ts-expect-error_` bypassed the description rule, and an `@ts-expect-error` whose description mentioned `@ts-ignore` was misflagged (false positive). The checker now branches on TypeScript's own `directive.type`, reads the expect-error description as the fixed-length suffix after the canonical marker, and fails closed on an unknown type; nine differential fixtures and a suffixed-ignore red-proof lock it. **P4-5:** three enforcement holes closed — a new AST invariant forbids any object-literal `throw` in plugin source (catching the reconstruct/spread-override factory bypass the literal scan missed); a new scan confines the `"UNKNOWN_ERROR"` string literal to its two approved definitions and the connect-handler test now requires the imported identifier (rejecting a re-hardcoded quoted literal); and `VARIABLE_SCOPES_MISSING` joins the actionable-recovery assertion. All three P4-5 regressions are red-proofed. Suite green (766); no production behavior changed beyond the checker's classification.
- **Rev 41, 2026-07-24** — closure-audit remediation of the Phase 5–6 adversarial critique's reopened findings ([reviews/Phase-5-&-6-critique.md](reviews/Phase-5-%26-6-critique.md), R1–R9). Six were adjudicated and land here; three (R4 aggregator test-matrix completion, R7 missing-`parentId` error text, R8 the C4/C9 regression groups) remain open remediation items. **R1 (Q24 amendment):** the Q24 record admits the capture-failure residual — when `getStyledTextSegments` is unavailable (test stubs only; the API is stable on a live mixed-font `TextNode`) the before-value degrades to a non-restorable `{mixed: true}` marker and the edit proceeds best-effort rather than blocking. No code change: the existing `captureFontSnapshot` fallback is now the ratified contract, not a defect. **R2 (instance TOCTOU, reopens the C5 resolution):** the second target resolution (`getValidTargetInstances`), which runs immediately before execution, is upgraded from existence+type to the **full prevalidation predicate set** — identity/ID, `INSTANCE` type, exact requested name, scope-root membership, and lock/locked-ancestor state — re-asserted once against the original request; any drift fails the whole command before `swapComponent()` runs. C5's "fail on disappearance/type-change" is the narrow case this generalizes. **R3 (Q25 enforcement, corrects the Q26 premise):** the four batch `results` schemas stop being `z.array(z.any())` and encode the Q25 row vocabulary (`nodeId` + `status` required, `error`/partial-mutation fields optional, additive per-tool keys via `catchall`), so the registered schema now rejects a drifted row at the protocol boundary; the registered-callback test is coupled to real plugin-handler output and red-proofed against a reintroduced legacy-only/`instanceId` row. This makes literal Q26's claim that "registered-callback tests assert the returned key set … so `looseOutput` cannot re-mask drift," which the audit found unmet. **R5 (ledger):** the Phase 6 Three-Layer-Boundary and Layer-1 items keep their `[x]` but carry an explicit "implemented pending Phase 7 (nested strictness)" label, so the checkmark is not read as the full D7 Layer-1 "unknown keys at any depth" contract. **R6 (wording, logged amendment):** the PRD/task "report/retry exactly the failed items" shorthand is corrected to "every non-success row (`failed` and `skipped`)", matching the already-corrected emitted tool descriptions. **R9 (progress vocabulary):** intermediate `node_delete` progress payloads rename `successCount`/`failureCount` to the shared envelope names `succeededCount`/`failedCount`, so Q26's "no second count vocabulary" rule reaches the progress channel, not only the final return. R2/R3/R9 are code changes with tests; R1/R5/R6 are contract/ledger text. **Twenty-nine open questions remain resolved; R4/R7/R8 are tracked as open remediation, not open decisions** (closed in Rev 42).
- **Rev 42, 2026-07-25** — closed the three remaining closure-audit remediation items (R4/R7/R8), all test/wording work under already-resolved decisions. **R7:** the `create_component_set` missing-`parentId` error now matches its corrected schema — it names `node_info` and describes an "appendable parent container" (not a bare "parent container") and asks for both the ID and the exact current name; a phase-2 assertion pins the text. **R8:** the two production fixes gain their required regressions — a C4 early-field-success/later-field-failure test proves the failure row discloses BOTH the main-component swap and the already-applied override field (`before.appliedFields`), and C9 present-empty exact-match/mismatch cases land for BOTH parent paths (ordinary creator via the `create_svg` dispatcher in phase 1, component-set via the `create_component_set` dispatcher in phase 2), each red-proofing the truthiness bug — a present-empty `parentNodeName` is a `PARENT_NAME_MISMATCH`, and an exactly empty-named parent is usable. **R4:** the aggregator matrix gains an annotation all-failed + skipped case (annotation was success-only), and the mislabeled "node-gone and not-TEXT" text test is split so the not-`TEXT` branch is the first/only item and actually executes (previously a leading missing node made the handler skip the frame). No production behavior changed beyond the R7 error string. Two Rev-41 test-acceptance items were also completed here so their findings close cleanly: R6 gains an emitted-`tools/list` assertion over all four batch descriptions, and R9 gains a delete progress-emission regression that rejects the legacy `successCount`/`failureCount` vocabulary. **All R1–R9 closure-audit findings are now remediated.** *(Superseded in part by Rev 43: a second recheck reopened R2 and R3 and raised R10.)*
- **Rev 43, 2026-07-25** — remediation of the critique's **second** closure recheck, which reopened R2/R3 and raised R10 against the Rev 41/42 tree. **R2 (reopened — fixed properly).** The Rev 41 gate re-asserted the predicate set but was not the last operation before mutation: the dispatcher then awaited `getSourceInstanceData`, and each target awaited its own `getMainComponentAsync()`, before `swapComponent()` — every `await` an event-loop yield in which the validated target can drift. Three changes close it. (1) The predicate set is extracted into a **synchronous** `checkTargetPredicates` (identity/ID — newly checked — plus `INSTANCE` type, exact name, scope membership, lock/locked-ancestor); it reads only resolved node state, so it can run in the same turn as the mutation it guards, and both gates share one definition. (2) The dispatcher resolves source data **before** the target gate. (3) `setInstanceOverrides` hoists every target's `getMainComponentAsync()` await, re-asserts the whole batch synchronously **after** the hoist and **before** the first mutation, and re-asserts each target immediately before its own `swapComponent()` with no intervening `await`. Pre-mutation drift throws **outside** the P6-5 envelope catch, so it surfaces as a Layer 2 refusal (structured error, no envelope, no mutation) rather than an all-failed envelope; drift detected mid-loop, after earlier targets mutated, stays a failure row so the D7 envelope is not lost (the C3 lesson). A new end-to-end suite drives the real dispatcher and injects same-object drift *inside* the awaited windows (during source resolution, during the target's own main-component read, an id-mismatch impostor, and a later target drifting during an earlier target's await), each asserting a command error and `swapComponent()` call count **zero**, plus a no-drift baseline proving the gate does not over-reject. **R3 (reopened — cheap fix + honest wording).** The row schema now requires a non-empty actionable `error` on every `failed`/`skipped` row (Q25's "actionable per-item reason", previously optional regardless of status), red-proofed. The remaining gap is recorded rather than overstated: Q26's claim that callback tests make `looseOutput` unable to re-mask drift is **corrected in place** to state the actual enforcement level — the advertised schema cannot be exact at the top level (the SDK validates `structuredContent` on `isError` results and cannot advertise a union, forcing optional fields + `catchall`), so a legacy-only top-level payload still validates; per-row Q25 conformance is schema-enforced, top-level envelope exactness is **test**-enforced, and a runtime exact-result validator is explicitly deferred. **R10 (new — accepted diagnostic residual).** D7's promise that "the restoring write can be composed directly from the error in one round trip" is **withdrawn**: no registered tool applies a per-segment font map (no segment-range write surface exists outside `setCharacters`' internal strategies) and none swaps an existing instance back to a main component by ID, so the before-values are truthful *diagnostic evidence*, not an executable recovery payload. Q24's "genuinely restorable" is corrected to a record/evidence claim. Building the write surface or automatic rollback is deferred as new write capability needing its own verification and safety review. **Twenty-nine open questions remain resolved; R2/R3/R10 were remediation and contract-honesty work under recorded decisions.**
- **Rev 44, 2026-07-25** — remediation of the critique's **third** closure recheck (R11–R13), which found three non-deferred defects after Rev 43. **R11 (same-object removal):** Figma preserves stored references after collaborative deletion, so `checkTargetPredicates` now rejects `removed === true` on both the target and editable scope root; dispatcher tests delete each during an awaited main-component read and prove zero swaps. **R12 (truthful instance before-value):** the Rev 43 hoist could stale an earlier target's saved main ID during a later target's await, and swallowed capture errors as `null`. The handler now requires a valid async main-component read during batch preflight, captures target 0 authoritatively followed by a whole-batch synchronous recheck before the first swap, and captures each later target immediately before its final synchronous gate/swap; capture failure/null refuses before that target mutates, and a multi-target regression proves the returned `before.mainComponentId` equals the state observed immediately before the swap. **R13 (actionable-row totality):** `node_delete` and `annotation_set` copied `error.message` directly, allowing empty or non-`Error` throws to produce a protocol-invalid Q25 row; all four batch handlers now use the hardened shared `describeError()` fallback, which returns a nonblank diagnostic for empty strings/errors, raw values, `null`/`undefined`, null-prototype objects, and hostile renderers. Real-handler/schema tests cover empty `Error`, raw string, and `null` across all four aggregators. The live `before` schema description is corrected to diagnostic evidence, not guaranteed restoring-write input. Rev 43's accepted R3 top-level looseness and R10 missing write surface remain explicit scope decisions, not silently claimed as implemented.
- **Rev 45, 2026-07-25** — remediation of the critique's **fourth** closure recheck (R14/R15), which found two post-mutation telemetry paths capable of losing the D7 envelope after Rev 44. **R14 (notification isolation):** `setInstanceOverrides` called `figma.notify()` after deriving a real result inside its execution `try`, then called it again in the catch; a throwing notification after `swapComponent()` therefore erased the result and rethrew. Both sites now use a best-effort helper whose failure cannot enter outcome accounting; a regression proves that a post-swap notification failure still returns one success row with coherent counts. **R15 (progress diagnostic totality):** `sendProgressUpdate` caught `postMessage` failure but interpolated `err.message`, allowing a hostile Proxy to throw again while the catch reported it. Progress diagnostics now use the total `describeError()` formatter; text/delete regressions inject that Proxy and prove that row cardinality, counts, mutation truth, and the final envelope survive. The dormant server-side `setInstanceOverridesResult` interface is aligned to the Q25/Q26 envelope and row vocabulary so it no longer advertises the removed `totalCount`/`instanceId`/`message` fields. These are implementation remediations under the existing C3/D7 decision, not new open questions.

# v2.3.3 Implementation Task List

This document tracks the tasks required to fulfill the requirements in the [v2.3.3 PRD](file:///Users/neozhehan/Git/figma-edit-mcp/documentation/v2.3.3/prd.md). It is divided into 14 phases, covering both Track 1 (Type-check restoration) and Track 2 (Safety-contract gap closure).

> Release decision status, review outcomes, verification history, and current findings are maintained in [Change 14 of the v2.3.3 release changelog](release-changelog.md#change-14-change-13-verification-and-leg-1-guard-coverage-rev-70). The 14 implementation phases remain below.

---

## Phase 1: Fix the Config (D1)
- [x] Modify `figma_plugin/tsconfig.json` to replace the broken `typeRoots` path with `"types": ["@figma/plugin-typings"]`
- [x] Confirm `lib` is set to `["es2018"]` and does **not** include `"dom"` (to avoid redeclaring globals owned by `@figma/plugin-typings`)
- [x] Include only the imported `../src/shared/nodeTypes.ts` shared file, then run `tsc --noEmit -p figma_plugin/tsconfig.json` and verify the decided configuration reports exactly **10 residual errors**. The intermediate probe before the explicit shared root reports 9.

---

## Phase 2: Triage the Residual (D2)
- [x] Resolve residual errors 1 & 2 in [componentHandlers.ts:707](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/handlers/componentHandlers.ts): Explicitly type `parentNode` (e.g., as `BaseNode`) so negative branch narrowings do not reduce to `never`.
- [x] Resolve residual errors 3 & 4 in [main.ts:194](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/src/main.ts): Explicitly type `parent` to prevent `never`-narrowing in `validateCloneWrite`.
- [x] Resolve residual error 5 in [connectHandlers.ts:42](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/handlers/connectHandlers.ts): Narrow/cast `scopeNode` to `PageNode` before invoking `loadAsync()`.
- [x] Resolve residual errors 6 & 7 in [nodeReaders.ts:8](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/handlers/nodeReaders.ts) and [nodeUtils.ts:6](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/utils/nodeUtils.ts): rewrite the impossible `../../shared/nodeTypes.js` paths to `../../src/shared/nodeTypes.js`. Keep the imports type-only and scope the plugin tsconfig root to the exact imported `../src/shared/nodeTypes.ts` file, not the entire shared directory; the rewrite is emitted-JS-identical.
- [x] Resolve residual errors 8 & 9 in [exportUtils.ts:124–125](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/utils/exportUtils.ts): Add a minimal ambient declaration for `TextDecoder` (do not import `dom`; prefer the typings if a future `@figma/plugin-typings` version provides it, with a comment pointing to the PRD). Declared in `figma_plugin/types/sandbox-globals.d.ts` (covered by the tsconfig `include`), with typed parameters rather than `any`; the file omitted from `bf4a4c0` was supplied by immediate corrective commit `3f62505`.
- [x] Resolve residual error 10 in [nodeTypes.ts:40](file:///Users/neozhehan/Git/figma-edit-mcp/src/shared/nodeTypes.ts): the exact shared-file root exposes Zod 4's TS2554 for `z.record(z.unknown())`; use `z.record(z.string(), z.unknown())`. The pinned Zod runtime accepts both forms and `NodeEntrySchema` has no runtime importers, so this is runtime-equivalent.
- [x] Re-run `tsc` and reproduce the decided **10-error → 0-error** boundary with no new suppression. Per-fix transient compiler outputs were not retained in history, so no stronger durable claim is made about every intermediate count.
- [x] Guardrails (D2/D4): no `@ts-nocheck`, no `strict` relaxation, no blanket `any` casts. If any fix appears to require a runtime behavior change, **stop and escalate** — do not silently alter a live-verified code path.
- [x] Re-run `tsc --noEmit -p figma_plugin/tsconfig.json` and verify the compilation completes with **0 errors**.
- [x] Run the full unit suite after the residual fixes (`bun run test`): Track 1 edits can break static source-matching tests — the Phase 4 §3a test asserting the `loadAsync` call shape required updating for the `PageNode` cast (updated in the same change); suite green (647/647).
- [x] **Review decision 4 (accepted 2026-07-17):** at both `never`-narrowing guard sites fixed above (the `createComponent` parent check in `componentHandlers.ts` and the `validateCloneWrite` parent check in `main.ts`), add a one-sentence comment recording that the typings prove the guarded branch statically unreachable (`node.parent` is typed `(BaseNode & ChildrenMixin) | null`, so every non-null parent has `appendChild`) and that the guard is kept deliberately as runtime defense-in-depth — removing it would be a behavior change on a live-verified path (D4). Comment-only; its purpose is to stop a future cleanup from deleting the guards as dead code.
- [x] **Review decision 3 (accepted 2026-07-17) — zero-drift rework of the two alias casts:** replace `const parentNodeBase = parentNode as BaseNode` (the `createComponent` parent check) and `const parentBase = parent as BaseNode` (the `validateCloneWrite` parent check) with a zero-emitted-JS form — a type annotation on the existing declaration, or the inline-cast idiom `connectHandlers.ts` already uses — so Track 1 contributes no code-line changes to the rebuilt bundle. Re-run `tsc` (0 errors), rebuild, and confirm the `code.js` diff against the pre-v2.3.3 baseline is comment-text only. Do this **before the Phase 1–2 commit**, in the same change as the decision-4 rationale comments at the same two sites.
- [x] **Phase 1–2 adversarial closure (2026-07-18):** completion is the sequence ending at `3f62505`, not `bf4a4c0` alone: `bf4a4c0` accidentally omitted `figma_plugin/types/sandbox-globals.d.ts` and still had two TS2304 errors; `3f62505` immediately added it and established the zero-error state. Preserve the commits and record the correction rather than rewriting history. The per-fix compiler-count log was not retained, so the ledger now makes only the reproducible 10-error → 0-error boundary claim. The unrelated documentation churn already present in `bf4a4c0` is likewise recorded as historical; subsequent phase reviews use phase-scoped diffs.

---

## Phase 3: Regression Tests & Gate (D3)
- [x] Add `check:types:plugin` script pointing to `tsc --noEmit -p figma_plugin/tsconfig.json` in [package.json](file:///Users/neozhehan/Git/figma-edit-mcp/package.json). (Sequenced by condition: added only after Phase 2 reaches 0 errors.)
- [x] Wire the `check:types:plugin` gate into the GitHub Actions CI pipeline [ci.yml](file:///Users/neozhehan/Git/figma-edit-mcp/.github/workflows/ci.yml) alongside `check:plugin` / `check:versions`. The esbuild build pipeline is unchanged — the gate is additive.
- [x] Prove the gate red/green locally: temporarily reintroduce a type break (or revert the `types` config), verify `check:types:plugin` fails, then revert and confirm it passes. **Durable record (verified 2026-07-18, review P3-5):** a transient `figma_plugin/utils/zz-red-proof.ts` containing `const zzRedProof: string = 5` turned `check:types:plugin` red (TS2322 at its file:line) and its removal restored green; a transient `// @ts-nocheck` fixture likewise turned `check:suppressions` red (naming the file:line with removal guidance) and green on deletion. Note: a red-proof file must sit inside a tsconfig-included directory (`src/`, `handlers/`, `utils/`, `types/`) — a plugin-root file is outside the program (though any file *imported* from the entry graph is always type-checked).
- [x] **Review decision 2 (accepted 2026-07-17) — suppression policing beside the type gate:** the `tsc` gate stays green when a contributor adds a new suppression, so ship an enforcement check in an adjacent CI step (ratification 5: two adjacent steps satisfy the intent): `@ts-ignore` **and `@ts-nocheck`** are forbidden in `figma_plugin/**/*.ts` (review P3-2 — `@ts-nocheck` is a file-wide opt-out both gates would otherwise miss), and `@ts-expect-error` is allowed only with a meaningful description (at least one alphanumeric character — bare punctuation rejected, review P3-4). Comment detection is delegated to the TypeScript parser (`ts.createSourceFile`, Q28/Rev 37 — superseding the earlier substring-and-string-mask approach, which carried a regex-desync false negative); the checking logic is exported and covered by negative fixtures in `checkSuppressions.test.ts` (P3-3), including the regex-desync regression. Failure output names the offending file:line and states the fix, per the one-round-trip criterion. (`@typescript-eslint/ban-ts-comment` is the equivalent if ESLint is ever adopted.) The check passes on the current tree — every remaining suppression (49 as of 2026-07-18; two `TS2551` suppressions merged into one during the Phase 4 bind-loop rework) is a described `@ts-expect-error`, tracked for touch-it-clear-it burn-down.

---

## Phase 4: Structured Error Transport + Design-System Verification (D9/D5)
- [x] **Structured Transport Implementation (D9/Gap 11)** — do this first; the D5 errors ride on it
  - [x] Replace the `describeError` string flattening in [main.ts](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/src/main.ts): every refusal and operational failure leaves the plugin dispatcher as a structured `{error: {code, message, details?}}` object. **Verified 2026-07-18:** the test-environment compat shim that forked this behavior (string in tests, object in production) was found and removed by the follow-up review; the dispatcher now unconditionally emits the structured object, confirmed by the full test suite and the official-SDK boundary suite.
  - [x] Preserve the structured error object through the UI relay and socket transport. **Verified 2026-07-18:** `ui.html`'s `command-error` handler forwards `message.error` (now the structured object) into `sendErrorResponse`, which serializes it whole via `JSON.stringify`; `socket.ts` broadcasts `data.message` unmodified. Neither layer parses or flattens the object.
  - [x] Update error handling in [figma-client.ts](file:///Users/neozhehan/Git/figma-edit-mcp/src/mcp_server/figma-client.ts) to stop reconstructing plain `Error`s: surface failures at the MCP boundary as `isError: true` with machine-readable `structuredContent` **plus a text fallback carrying the same code**. Codes are never reconstructed by parsing prose. **Verified 2026-07-18:** `FigmaError` carries `code`/`message`/`details`; `getStructuredError`'s substring/prefix matching over the `ERRORS` table was found and deleted — classification is now purely structural (coded object passes through; else `UNKNOWN_ERROR`).
  - [x] Define all refusal messages centrally as **origin-scoped message factories** — handlers pass operands in; no handler-local refusal strings (Q16, corrected by Change 5 P9-F3 and Rev 57). The current inventory is thirteen operational codes plus ten verification codes — **twenty-three codes plus `UNKNOWN_ERROR`** (Rev 61 adds plugin-origin `PAGE_SCAN_FAILED` and `VARIABLE_IN_USE`). The four D13 admission codes live only in socket `CHANNEL_REFUSALS`; client-origin `CHANNEL_NOT_BOUND` lives only in `CLIENT_REFUSALS`; the eight Phase 10–11 operational codes and D5/D6/D10 verification codes live in plugin `REFUSALS`. Codes are never derived from prose, origin/absence invariants forbid dead mirrors, `ERRORS` remains the closed legacy pre-v2.3.3 surface, and the plugin's `UNKNOWN_ERROR` fallback is one named constant.
  - [x] `channel_join` pass-through (Q20, resolved 2026-07-18: Option A; completed across both legs and hardened by Rev 58): every structured `error.code` reaching `channel.ts` passes into the in-band `errorCode` verbatim — unknown codes and an explicit `UNKNOWN_ERROR` included, never collapsed or double-prefixed — and only an absent code selects fallback wording. Delete prose classification and code local failures at origin. When replacing an actually healthy binding, internal `releasedChannel` metadata crosses both the wire join and following `get_connect_payload`; thrown and structured leg-2 failures merge it into the originating message/details, cleanup preserves it for `CHANNEL_NOT_BOUND`, same-channel replacement qualifies, invalidated state does not, and successful output omits it. `figma-client` keeps locally decided predecessor evidence separate from public details, and `channel.ts` supplies authoritative attempt context on both legs even when absent. Plain-record details without a reserved-key collision gain the field directly, absent details become `{releasedChannel}` when evidence exists, and non-record or colliding details remain intact under `originDetails`; origin details can never fabricate recovery state. The in-band `status: "error"` envelope itself is unchanged — convergence to `isError` is the v2.3.4 PRD's Q1, not this release.
  - [x] Every new or changed message embeds its own recovery: name the cause distinctly (missing vs. mismatched vs. stale), name the read tool that supplies the correct value (`variable_list`, `style_list`, `node_info`), and say "pass it back verbatim". Acceptance check per message: an agent given only the error text and the tool list can produce the correct retry without further discovery.
- [x] **D5 Schema Updates** (Q1/Q14 mechanism: flat object shapes, no discriminated unions)
  - [x] Update [variable.ts](file:///Users/neozhehan/Git/figma-edit-mcp/src/mcp_server/tools/variable.ts): `currentVariableName` required for `UPDATE_VARIABLE`; `collectionName` and `scopes` required for `CREATE_VARIABLE`. An explicitly supplied empty `scopes` array remains the caller's deliberate choice; omission is rejected.
  - [x] Update [style.ts](file:///Users/neozhehan/Git/figma-edit-mcp/src/mcp_server/tools/style.ts): add `currentStyleName`, required whenever `styleId` is supplied; make `name` optional at the schema level.
  - [x] State each conditional requirement **twice in prose**: in the field description (e.g. "REQUIRED for UPDATE_VARIABLE — the variable's **current exact** name, passed back verbatim from `variable_list`") and in the top-level tool description ("UPDATE_VARIABLE requires `currentVariableName`"). **Verified 2026-07-18 against the emitted `tools/list` result** (Q21), not just raw registration config — see the boundary suite.
  - [x] Implement server-side `.superRefine()` checks in `variable.ts` and `style.ts` enforcing the conditional requirements. Each refinement error is actionable per D9: the violation, the read tool that supplies the correct value, and "pass it back verbatim". Extended (P4-2/P4-6, 2026-07-18) to reject an explicitly empty `styleId` (create/update must split on presence, not truthiness) and explicitly empty `name` on update (an object named `""` could never pass exact-name verification again).
- [x] **D5 Plugin Handler Implementation** (defense in depth — the server is not the trust boundary, AS1)
  - [x] Enforce fail-closed verification in [variableHandlers.ts](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/handlers/variableHandlers.ts) and [styleHandlers.ts](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/handlers/styleHandlers.ts) before any mutation: absent ⇒ reject, mismatch ⇒ reject, for `currentVariableName` (update), `collectionName` (create, same pattern as `variable_delete`), and `currentStyleName` (update by `styleId`). Extended (P4-2/P4-6): `styleId` presence (not truthiness) selects create vs. update, and empty names are rejected before any mutation at this layer too.
  - [x] Implement validate-before-mutate ordering in `UPDATE_VARIABLE`: a supplied `value` requires a valid `modeId` **before** `name`, `description`, or `scopes` are touched; an alias `value` (`type: "VARIABLE_ALIAS"`) additionally resolves its target variable via `getVariableByIdAsync` before any mutation (Q17).
  - [x] Implement validate-before-mutate ordering in `style_manage`: resolve every requested variable binding before applying `name` or properties; a PAINT-style `bindVariables` request additionally verifies the style has paints to bind **before** `name`/`description`/`properties` apply (Q17). Deep value-type validation is not attempted — the Q17 boundary rule is: pre-validate what one read can confirm; disclose what only execution can reveal. **Extended (P4-3, 2026-07-18):** an empty `bindVariables` map (`{}`) is a documented no-op — the pre-check and the execution loop now gate on the identical condition (resolved entries present), closing the gap where `{}` used to reach execution and mutate before the paints-present check fired.
  - [x] Order TEXT-style font loading per Q19 (resolved 2026-07-18, Option A): on **update**, load the target font (the supplied `properties.fontName`, else the style's actual `fontName`) before `name`/`description`/`properties` apply; on **create**, create the style, read its **actual** `fontName` (or use the supplied one), load, then write properties — inside the existing rollback guard. No hardcoded default font. A one-sentence comment at the fork records why the two paths order operations differently.
  - [x] Enforce `style_manage` `name` semantics: required on create; on update, rename **only** when `name` is explicitly provided (removes the accidental-rename side effect).
  - [x] Predictable failures return with zero mutation; an unexpected mid-update failure is reported **explicitly as partial** via a D9 error — never as a clean failure. Per Q18 (resolved 2026-07-18, Option A), the error's `details` reuses the Q9 vocabulary: `partialMutation: true`, a plain-language statement of what changed, and cheap before-values (the original `name`/`description`, already in hand from verification). A clean failure never carries the flag; the field names are shared with the Phase 6 batch failure rows.
  - [x] Replace the ambiguous `Expected`/`got` operands in the variable verification error with explicit `stored name` / `received currentVariableName` labels.
- [x] **Unit Tests**
  - [x] Transport tests: code and details preserved end to end; `isError` and `structuredContent` asserted at the **actual MCP boundary**, not only plugin-internally. **Delivered 2026-07-18** via `mcpBoundary.test.ts`: the real registered server, driven through the official SDK `Client` over `InMemoryTransport`, with client-side output-schema validation primed by `listTools()` — closing the P4-7 gap where the original transport tests stopped at a synthetic callback or a mocked `figma.ui.postMessage` and never reached the validation path that P4-1 found broken.
  - [x] `channel_join` pass-through tests (Q20): each representative join failure returns its originating code verbatim in `errorCode`; an unknown structured code passes through unchanged; no code is derived from message text.
  - [x] Schema tests via `safeParse`: omitting `currentVariableName` (update), `collectionName`/`scopes` (create), or `currentStyleName` (update by ID) is rejected with the actionable message.
  - [x] Description-marker tests (Q14 as corrected by Q21, resolved 2026-07-18: Option B): the literal "REQUIRED for …" marker is present in the **field** description, and the **tool** description states the requirement as a natural sentence naming the action and the field (e.g. "UPDATE_VARIABLE requires `currentVariableName`"); both asserted against the **emitted `tools/list` result**, not raw registration configuration.
  - [x] Plugin-level tests: missing, stale, and mismatched names fail closed and mutate nothing.
  - [x] Validate-before-mutate tests: a failing `modeId` or unresolved variable binding leaves name/description/scopes/properties unchanged; likewise an alias `value` with a dangling target, and a PAINT-style bind against a style with no paints (Q17).
  - [x] Partial-disclosure tests (Q18): a deterministic injected mid-update failure returns a D9 error whose `details` carry `partialMutation: true`, the what-changed statement, and before-values; a clean failure never carries the flag; the field names match the Phase 6 batch-row vocabulary.
  - [x] Font-order tests (Q19): a created TEXT style with text properties loads the style's **actual** default font — the mock's default is deliberately **not** Inter, so a hardcoded guess fails the test; an update loads the target font before any mutation.
  - [x] Properties-only style updates do not rename the style.
  - [x] Positive path: fully verified operations succeed.
  - [x] Update existing tests that assert on changed message text in the same change (D9). **Verified 2026-07-18:** every dispatcher test asserting on the plugin's string error (`permissionMatrix`, `atomicityAndValidation`, `phase1`, `phase2`, `componentHandlers`, `variableDelete.phase2`, `safetyContract`) migrated to `.error.message`, closing the gap where the removed compat shim had let these tests validate a code path production no longer executes.

---

## Phase 5: Explicit-Parent Conformance (D6)
- [x] **Schema Updates**
  - [x] Remove `.optional()` from `parentNodeName` in `create_shape`, `create_frame`, `create_text`, `create_svg`, and `create_instance` schemas in [create.ts](file:///Users/neozhehan/Git/figma-edit-mcp/src/mcp_server/tools/create.ts).
  - [x] Make `parentId` and `parentNodeName` required in the `create_component_set` schema — it loses its no-parent exception (the placement fix itself lands in Phase 8).
- [x] **Handler Updates**
  - [x] Reword `ERRORS.PARENT_NAME_MISMATCH` to also cover the omitted-name case (e.g. "parentNodeName is missing or does not match…"), per the D9 convention, so an agent is not steered into swapping a correct `parentId`. Keep the plugin's "missing" branch as defense in depth for older servers and non-conforming clients (Q2). *(Interim state, superseded by Q22 below: the merged reworded string landed 2026-07-18 and was replaced by the coded pair 2026-07-19; the checkmark records the historical step.)*
  - [x] **Q22 (resolved 2026-07-18: Option A — review findings P5-1/S1, PRD Rev 31):** replace the merged string with the factory-built coded pair — `PARENT_NAME_MISSING` (field omitted) and `PARENT_NAME_MISMATCH` (field does not match) — defined in the central `REFUSALS` registry, each embedding its D9 recovery (read the parent's current name with `node_info`, "pass it back verbatim"). Route every parent-name throw site through the registry; the `create_component_set` missing-`parentId` branch gets its own message naming `parentId`; tests asserting the interim message text update in the same change; playbook entries land in Phase 12. *(Landed 2026-07-19: codes in `src/shared/errorCodes.ts` + plugin `REFUSALS`, `verifyParentNameOrThrow` and `validateCreateComponentSetPlan` route through them, parity test updated, phase1/phase2 message assertions migrated to `.error.code`.)*
- [x] **Unit Tests** (Q2 layer split: omission is asserted here at the schema boundary; the live *mismatch* probe is Phase 14)
  - [x] Schema-boundary tests: all **six** tools reject calls missing the parent fields at the server parser.
  - [x] Plugin-level tests (bypassing the server): missing and mismatched parent names still fail closed.

---

## Phase 6: Batch Contract Corrections (D7)
- [x] **Schema & Envelope Adjustments**
  - [x] Add `.min(1)` to all four batch arrays (`node_delete`, `text_set_content`, `annotation_set`, `instance_set_overrides` inputs).
  - [x] ~~Correct the registered `outputSchema` field names to the handlers' real fields (`nodesDeleted`/`nodesFailed` for `node_delete`; `replacementsApplied`/`replacementsFailed` for `text_set_content`)~~ and add the new envelope fields (`status`, `requestedCount`, `succeededCount`, `failedCount`, `skippedCount`) so registered-callback tests can validate them and `looseOutput` can no longer mask drift. *(The strikethrough is superseded by Q26 below — the tool-specific counts are dropped, not aligned. The envelope-field half stands.)*
  - [x] **Q26 (resolved 2026-07-18: Option B — review finding P6-11, PRD Rev 35):** the batch handlers and schemas expose **only** the shared envelope counts. Remove every tool-specific duplicate from both handler return and `outputSchema` — `node_delete`'s `nodesDeleted`/`nodesFailed`, `text_set_content`'s `replacementsApplied`/`replacementsFailed`, `annotation_set`'s `annotationsApplied`/`annotationsFailed`, `instance_set_overrides`' duplicate `totalCount`, and the `totalNodes`/`totalReplacements`/`totalAnnotations` totals (general rule: no field duplicates an envelope count). Also drop the now-unused progress-payload copies of these fields. Update every test asserting the removed names in the same change; the CHANGELOG before/after example lands in Phase 13.
  - [x] Update the four batch tools' descriptions to instruct the agent to treat `partial_success` as an incomplete operation and report every non-success row (`failed` and `skipped`). *(R6, closure audit — corrected from "report the failed items"; the emitted tool descriptions already say "every non-success item (both failed and skipped)".)*
- [x] **Three-Layer Boundary** — **[implemented pending Phase 7: Layer-1 nested strictness]** (defined explicitly — the aggregators alone cannot deliver it)
  - [x] **[implemented pending Phase 7 — nested strictness]** Layer 1 — schema rejection: `[]`, a malformed item, or a duplicate target (Q23 — the `.superRefine()` lands with the reopened Handler item) is an MCP validation error with **no** execution envelope. **Partial (C10/R5): top-level** unknown keys are rejected today; **nested/at-any-depth** unknown-key rejection is the recursive-strictness wiring deferred to Phase 7 (D8) and is NOT yet enforced on batch-item objects. The "at any depth" half of this box closes when Phase 7 lands; it is checked here only for the top-level + `.min(1)` + duplicate-rejection that Phase 6 delivers. **The `[x]` records the Phase-6-scoped delivery, not the full D7 Layer-1 contract — read the label.**
  - [x] Layer 2 — refused before execution: dispatcher prevalidation, permission, and scope refusals return a structured D9 error (no envelope, **no mutation**) instead of a thrown string. **Rev 44:** the final synchronous instance-target predicate also rejects a stored target or scope-root reference whose Figma `removed` flag became true during an awaited window.
  - [x] Layer 3 — accepted execution: every accepted call returns the envelope below. **Rev 45:** progress and user-notification delivery, including formatting a delivery failure, are best-effort telemetry and cannot alter mutation accounting or erase the envelope.
- [x] **Handler Execution Adjustments**
  - [x] Enforce duplicate-target rejection **before any mutation** for `node_delete`, `text_set_content`, and `instance_set_overrides`, after normalizing URL/API node-ID spellings (`1-2` vs `1:2`). `annotation_set` is exempt — annotation batches may legitimately repeat a node. *(Landed as an inline dispatcher check; superseded in role by Q23 below — under Option B this plugin-side check is **retained as uncoded defense in depth** on the `UNKNOWN_ERROR` fallback (Q2 precedent), not the first-class rejection. The checkmark records the step, not phase completion.)*
  - [x] **Q23 (resolved 2026-07-18: Option B — review findings P6-6/S2, PRD Rev 32):** add the first-class duplicate rejection at **Layer 1** — a per-tool schema `.superRefine()` on the `node_delete`/`text_set_content`/`instance_set_overrides` inputs that normalizes spellings (via the exported `normalizeNodeId`) and rejects a repeated target at the boundary with an authored message (the offending ID, the normalized-spelling rule, and "remove the duplicate entry and resend the batch"). **No D9 code is minted** — a Layer 1 rejection is an MCP validation error, so the header inventory is unchanged. Keep the dispatcher check above as documented defense in depth. `annotation_set` stays exempt. Re-point the duplicate unit test at the schema boundary (see Unit Tests below).
  - [x] Update `deleteMultipleNodes` in [nodeModifiers.ts](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/handlers/nodeModifiers.ts) to return `success: true` only when `failureCount === 0 && successCount > 0`.
  - [x] Update all four batch aggregators to return tri-state `status` (`"success" | "partial_success" | "failed"`) derived from row outcomes, the four counts, and the invariant **`success === (status === "success")`** on every return path (early returns ⇒ `"failed"`).
  - [x] Ensure exactly one row per input, in input order; stop-on-first handlers (e.g. text content) emit explicit `skipped` rows for unattempted items instead of omitting them.
  - [x] Every failure/skip row carries an actionable per-item reason so a `partial_success` lets the agent retry every non-success row (`failed` and `skipped`) in one follow-up call. **Rev 44:** batch catches normalize empty `Error`s, raw strings, `null`/`undefined`, and hostile renderers through the shared null-safe `describeError()` helper; arbitrary JavaScript throws cannot erase the D7 envelope or emit a blank `error`.
  - [x] Treat progress and notification delivery as best-effort: a transport/notification failure and its diagnostic formatting run outside outcome accounting and cannot turn a completed mutation into a rejection or a duplicate failure row. **Rev 45:** both paths use the total `describeError()` formatter; `figma.notify()` is isolated by `notifyBestEffort()`.
  - [x] **Q25 (resolved 2026-07-18: Option A — review finding P6-9, PRD Rev 34):** unify the per-item row vocabulary across all four aggregators — every row carries `nodeId` (identity), `status`, and `error` (the reason, on any non-success row); the Q9/Q24 partial-mutation fields attach where applicable. Rename `setInstanceOverrides`' rows from `instanceId`/`instanceName`/`message` onto `nodeId`/`error` (keep `instanceName` as an additive field if useful); the top-level `message` summary stays. Update every test asserting the old instance-row keys in the same change; the CHANGELOG before/after example lands in Phase 13.
  - [x] **Partial-Mutation Disclosure (Q9, text before-value corrected by Q24)** — **scope note (R10, Rev 43):** what ships is *truthful disclosure* of what changed, not a guaranteed one-call restore. No registered tool applies a per-segment font map or swaps an existing instance back to a main component by ID, so the before-values are diagnostic evidence; the "restoring write in one round trip" promise is withdrawn in D7 and the missing write surface / automatic rollback is explicitly deferred, not silently assumed.
    - [x] Confirm the exact mutate-then-fail paths during implementation: text content (font fallback **or mixed-font normalization** before character assignment) and instance overrides (main-component swap before later override fields). *(Q24/Rev 33 identified the fallback path; C1/C2 later added mixed-font normalization. Rev 44 requires the instance's authoritative original main-component ID to be captured immediately before the final synchronous target gate and swap; a thrown, null, or invalid capture fails before that target is swapped.)*
    - [x] **Q24 (resolved 2026-07-18: Option A — review findings P6-2/P6-3/P6-4/S4, PRD Rev 33):** correct the text failure row to carry `before: {fontName: {family, style}}` and a `whatChanged` statement (the property the fallback actually mutated), **not** `before: {characters}`; set `partialMutation: true` **only when the font fallback was applied**, which requires `setCharacters` to report that fact (a small return/out-parameter change). The zero-mutation throw paths (node deleted, node not `TEXT`, a character-assignment failure with no prior fallback) carry neither the flag nor a before-value. The instance row's `partialMutation: true` + `whatChanged` + `before: {mainComponentId}` is already correct and unchanged. *(Interim implementation stamped `partialMutation`/`before: {characters}` on every caught text error — review P6-2/P6-3; the `whatChanged` field was also missing from text rows — P6-4.)*
    - [x] The field vocabulary is shared with Phase 4's non-batch disclosure (Q18) — identical field names; drift between the Phase 4 and Phase 6 shapes fails review.
- [x] **Unit Tests**
  - [x] `[]` batches and duplicate IDs (both `1-2` and `1:2` spellings) are rejected with no envelope and no mutation. **Per Q23 (Option B):** the duplicate case is asserted at the **schema boundary** (a `safeParse` failure / MCP validation error, no execution envelope) for all three de-duplicated tools, plus a plugin-level test that a client bypassing the schema still fails closed via the retained defense-in-depth check; assert the first (valid) item was left unmutated. *(Reopened: the interim test covered only `text_set_content` and asserted the dispatcher command-error — review P6-7.)*
  - [x] A Layer 2 refusal returns a structured error with no envelope and no mutation.
  - [x] A simulated mid-batch `remove()` failure returns `success: false` and `status: "partial_success"`.
  - [x] Property test across **all four** aggregators: `success === (status === "success")` holds on every return path.
  - [x] **Q25:** across all four aggregators, every per-item row carries the shared keys `nodeId`/`status`/`error` (on non-success rows); a test asserts the renamed `instance_set_overrides` rows expose `nodeId`/`error`, not the old `instanceId`/`message`.
  - [x] Exactly one ordered row per input, including explicit `skipped` rows with reasons.
  - [x] Registered-callback tests validate the output fields and envelope fields — **and, per Q26, assert the returned key set is exactly the envelope with no legacy count field present**. Update the stale representative samples (`totalNodes`/`totalReplacements`/old shapes) in `outputSchema.test.ts` in the same change. **Enforcement level (R3, Rev 43):** the schema enforces the per-row Q25 vocabulary (`nodeId` + `status`, and a non-empty `error` on every `failed`/`skipped` row); the *top-level* envelope exactness is enforced by these tests, **not** by the advertised schema, which the SDK error-envelope constraint forces to stay loose. The original "so `looseOutput` cannot re-mask drift" parenthetical overstated this and is corrected in Q26.
  - [x] Deterministic injected failures prove `partialMutation: true` appears with its before-values, and a clean failure never carries the flag. **Per Q24 (Option A):** the text test drives the real fallback-then-assign path and asserts `before: {fontName}` + `whatChanged`; separate deterministic tests assert the zero-mutation text throw paths (node deleted, not TEXT, no-fallback assign failure) carry **no** flag and **no** before-value; the instance test asserts `before: {mainComponentId}`. *(Reopened: the interim test used a half-assigning `characters` setter real Figma lacks and asserted `before: {characters}` — review P6-3.)*
  - [x] **Rev 44 fault matrix:** dispatcher tests remove the target and scope root during awaited main-component reads and assert zero swaps; capture-error/null tests assert zero swaps; a multi-target test proves a later await cannot stale the earlier target's `before.mainComponentId`; and real-handler/schema tests inject empty `Error`, raw-string, and `null` throws into all four aggregators and require a nonblank, schema-valid failure row.
  - [x] **Rev 45 telemetry fault matrix:** a hostile progress-delivery Proxy cannot corrupt text/delete row cardinality or erase a post-delete envelope; a throwing notification after an instance swap still returns one truthful success row and coherent envelope counts.

---

## Phase 7: Annotation Repair + Recursive Strictness (D10/D8)
- [x] **Annotation Contract Realignment (D10)**
  - [x] Modify the `annotation_set` item schema in [annotation.ts](file:///Users/neozhehan/Git/figma-edit-mcp/src/mcp_server/tools/annotation.ts): require `nodeId`, `nodeName`, and `labelMarkdown` (rejected when blank); make `categoryId` optional; remove the unsupported `annotationId` and `status` fields; define `properties` as an array of `{type}` objects matching the pinned Figma shape, with duplicate types rejected.
  - [x] Update [annotationHandlers.ts](file:///Users/neozhehan/Git/figma-edit-mcp/figma_plugin/handlers/annotationHandlers.ts): align handler logic with the updated schema; verify a supplied `categoryId` via `getAnnotationCategoryByIdAsync` **before** mutation; adopt the D7 envelope.
  - [x] **Q30 (resolved 2026-07-25: Option A — PRD Rev 46):** the category refusal is the coded `ANNOTATION_CATEGORY_NOT_FOUND` factory in the central `REFUSALS` registry, carrying the offending operand and its own recovery (`annotation_list` with `includeCategories`, "pass a returned category ID back verbatim", or omit `categoryId`). Added to the shared inventory and the parity test; playbook entry lands in Phase 12.
  - [x] **Q31 (resolved 2026-07-25: Option A — PRD Rev 46):** `annotation_set`'s description carves out of R6's shared retry sentence — it states append is NOT idempotent, that a `failed` row may already have appended, and that the caller must call `annotation_list` and compare labels before retrying any non-success item. Asserted against the emitted `tools/list` in the boundary suite.
  - [x] **Q32 (resolved 2026-07-25: Option A — PRD Rev 46):** a commit-then-throw row carries `partialMutation: true`, `whatChanged`, and `before: {annotationCount}`; a zero-mutation failure carries none of them. The before-value is captured pre-attempt in loop scope (the C1 pattern), so a throw escaping the per-item handler cannot report a count read back after the failure (R12).
  - [x] Retry identity (Q10): each result row carries per-item **before/after annotation counts**. **[handler side complete; guide side pending Phase 12]** — the list-before-retry guidance and the identical-text-duplicate caveat are Phase 12 guide work and are NOT delivered by this box; per Q31 the always-visible half of that guidance now lives in the tool description.
  - [x] **Rev 51 unknown-outcome correction:** counts are verified observations, not guessed numbers. Each row carries nullable `beforeCount`/`afterCount` plus required `beforeCountVerified`/`afterCountVerified`; a setter-crossing failure with unreadable post-state carries `partialMutation:true`, `outcomeUnknown:true`, and optional `postStateError`, preserves the initiating row `error`, and requires list-before-retry reconciliation.
  - [x] Correct `annotation_list`'s registered output schema to the grouped `annotatedNodes` shape the handler actually returns, preserving node ownership in both page and node modes.
  - [x] Remove `idempotentHint` from `annotation_set` metadata (append is not idempotent).
- [x] **Recursive Strictness (D8)**
  - [x] Modify the central strict wrapper in [tools/index.ts](file:///Users/neozhehan/Git/figma-edit-mcp/src/mcp_server/tools/index.ts) to apply `.strict()` at every object depth, not only the top level.
  - [x] Enumerate the two intentional catchalls — `style_manage.properties.paints[]` and `style_manage.properties.layoutGrids[]` — as the **only** exemptions, independently pinned by test.
  - [x] **Q34 (resolved 2026-07-25: Option B — PRD Rev 46):** record the inventory as a **closed, test-pinned list**, not one derived from "polymorphic Figma payload" — `node_set_effects.effects[]` and the `node_set_*` paint objects take the same unions and stay strict because their handlers rebuild the value from enumerated fields, so an extra key would be accepted and discarded. The membership test for any future entry is that the handler forwards the object to Figma untouched.
  - [x] **Q35 (resolved 2026-07-25 — PRD Rev 47; field parity completed by Rev 50):** `style_manage.properties.effects[]` **leaves** the inventory (down to two entries). Decided on the Golden Rule — the old `{type: string, blendMode?} + catchall` shape failed first-call correctness (two fields described) and one-round-trip recovery (wrong keys accepted silently, no error to learn from). Three parts, all landed: (1) `effects[]` is a discriminated union over the seven pinned `type` literals with per-variant fields and gated secondary discriminators (`blurType` + PROGRESSIVE ramp, `noiseType` + `secondaryColor`/`opacity`); optional-with-documented-default for handler-defaulted fields, exact 19-literal `BlendMode` rather than an arbitrary string, and no `boundVariables` (it is a read-side projection). (2) Parity tests pin both the effect and BlendMode inventories to `plugin-api-standalone.d.ts`, following type aliases and `extends` bases — the `ANNOTATION_PROPERTY_TYPES` discipline. (3) `normalizeEffects` fills defaults **on top of** the caller's object instead of rebuilding from a field list, fixing a latent bug where a PROGRESSIVE blur was silently downgraded to a normal one and NOISE/TEXTURE/GLASS lost every field; an unrecognised `type` is still forwarded untouched. CHANGELOG before/after in Phase 13.
- [x] **Unit Tests**
  - [x] A conforming annotation append succeeds end to end and is rediscovered via `annotation_list`'s corrected grouped output.
  - [x] `annotation_list` traverses a `GROUP` root that lacks `AnnotationsMixin` and returns its annotated descendant through the registered callback.
  - [x] Legacy `annotationId`/`status` fields are **rejected, not stripped**.
  - [x] Blank `labelMarkdown` is rejected.
  - [x] A category mismatch fails before mutation.
  - [x] `annotation_set` metadata no longer reports idempotent.
  - [x] Strictness tests inject unknown keys at every object depth: rejected on every non-catchall tool; the two ratified catchalls are compared with an independent literal oracle.
  - [x] `node_set_effects` rejects cross-variant fields (including `INNER_SHADOW.showShadowBehindNode`) before dispatch and accepts the same field on `DROP_SHADOW`.
  - [x] Effect schemas reject negative shadow/blur/GLASS radii and GLASS `lightIntensity`/`refraction`/`dispersion` outside `0..1`; boundary values, including live-accepted `depth: 0`, remain valid.
  - [x] Effect `blendMode` accepts exactly the pinned 19 literals on both tool surfaces and rejects `NOT_A_BLEND_MODE`; the MONOTONE/`opacity` negative fixture uses otherwise-valid RGBA and pins the exact issue path/message.

---

## Phase 8: Containment (D11)
The guarantee is an **observable success boundary**: no successful command may await, emit progress or return while its created node sits outside the verified destination. Pattern per creator: resolve and verify the destination *first*; call the implicit creator; make append/insert into the verified parent the **immediate next synchronous operation** — before any `await` or fallible property assignment; then configure inside a guarded block. A failed attempt removes its artifact and treats cleanup as successful only when `removed === true`; if removal cannot be confirmed, it preserves the initiating error and reports `partialMutation` plus the survivor's observed ID/location. For `create_component`, cleanup additionally requires positive proof that the source is live, every original child is restored, and the component is empty; unreadable state never authorizes deletion. A disclosed cleanup survivor is the explicit failure residual, never a clean failure.
- [x] Audit and modify each creator to that pattern:
  - [x] `create_shape`
  - [x] `create_frame`
  - [x] `create_text` (insertion occurs **before** font-loading awaits and character assignment)
  - [x] `create_svg`
  - [x] `create_instance`
  - [x] `create_component`
  - [x] `node_clone` (append immediately after `clone()`, which defaults to `currentPage`)
- [x] Modify `node_flatten` to capture and verify the source's parent and index, and pass both to `figma.flatten(nodes, parent, index)` — true zero-transient placement.
- [x] Modify `create_component_set` to pass its (Phase 5-required) verified explicit parent directly to `combineAsVariants`.
- [x] Fix the dead `componentId` response field in `createComponentInstance` while it is open for the audit above (Rev 20; live-verified 2026-07-17 on channel `p28j`: `InstanceNode.componentId` does not exist at runtime, so the advertised field is dropped from every response): return `componentId: component.id` from the already-resolved component — both the `componentId` and `componentKey` paths hold it — and remove the now-satisfied `@ts-expect-error`. Additive behavior fix; CHANGELOG entry lands in Phase 13.
- [x] **Q33 (resolved 2026-07-25: hybrid — PRD Rev 46):** every creator resolves its destination as the **last `await` before creation**, so no event-loop yield separates the verified parent from the append. `create_instance` was the one violator — its parent read moved after `importComponentByKeyAsync`, closing the bounded 15 s import window between the verified destination and the placement. Handler-level error precedence changes only when both the component and the parent are invalid; the dispatcher still refuses a bad parent first in production. The full synchronous parent-predicate re-assert (the instance-target treatment) is **deferred** with a recorded escalation trigger.
- [x] **Q32 (resolved 2026-07-25: Option A — PRD Rev 46; recovery completed by Revs 50–51):** `create_component` and `create_component_set` disclose every failure that leaves the document durably changed (`partialMutation`, `whatChanged`, diagnostic evidence via the D9 error's `details`) instead of surfacing it as clean. Creator cleanup reports the survivor, verified destination, and `survivingParentState:"located"|"detached"|"unknown"` with a nullable actual-parent ID; only observed null is detached. `create_component` also reports tri-state source removal, restored/still-owned/unknown/relocated children, per-child restoration failures, and component child count while continuing after individual recovery failures. A failed component-set combine inspects placement before any recovery name write: ordinary live members restore original names best-effort, members in a surviving set retain or confirm variant names, and unknown parent type blocks unsafe restoration. Evidence reports `appliedComponents`, `restoredComponents`, `unrestoredComponents`, `removedComponents`, `unknownRemovalComponents`, `reparentedComponents`, `unverifiedPlacementComponents`, `survivingComponentSets`, `retainedVariantComponents`, and `unconfirmedVariantComponents`. Post-combine identity/location reads are guarded, the actual parent is rechecked, and optional projections cannot erase a confirmed success. Hostile error getters/stringification cannot erase this disclosure at either plugin or registered MCP boundary. A failure whose cleanup fully succeeds still throws untouched.
- [x] Report the destination in every creator's response (`parentId`), so D11's containment guarantee is confirmable from the result rather than only from a follow-up `node_info`. Added to `node_clone`, `create_svg`, `create_instance`, `create_component`, and `create_component_set` (the five that omitted it) and declared in all eight advertised output schemas. *(Found during Phase 8 live verification, 2026-07-26 — every one of those probes needed a follow-up read to prove containment held.)*
- [x] `create_component` refuses an instance-interior source in the real dispatcher, with a handler-level backstop, before `figma.createComponent()` can create a detached artifact.
- [x] **Rev 52 correction to F78-15:** reject explicit empty `name` on `create_shape`, `create_frame`, `create_text`, and `create_svg`, and explicit empty `componentSetName` on `create_component_set`, at both the MCP schema and plugin defense-in-depth boundaries before any creator/combine mutation. Omission remains valid and applies the established defaults. Live channel `2476` proved native Figma normalization (`Rectangle`, `Frame`, text content, `Frame`, `Component`) and a separate `node_rename` discriminator ruled out a stale bundle.
- [x] **Rev 53 extension (historical, corrected by Rev 54):** `node_rename.name` and `node_group.name` reject explicit empty assignments at MCP and plugin boundaries; grouping omission retains Figma's default. Live `a7ps` reproduced both pre-fix silent substitutions and established exact whitespace preservation; `uy38` verified the two repaired paths. The accompanying claim that a manual eight-entry list closed the whole assignment class is superseded by Rev 54.
- [x] **Rev 54 Change 3 assignment-sink closure:** classify fields per action and refuse explicit empty assigned names before mutation. `CREATE_COLLECTION.modeName` may be omitted for native `Mode 1`; `UPDATE_VARIABLE.name` and style UPDATE `name` may be omitted to keep the current name; style CREATE requires a non-empty `name`; component-property ADD requires a non-empty assigned `propertyName`; component-property EDIT uses `propertyName` as lookup and may omit `newPropertyName` to keep the current property name. Every MCP check has a plugin defense-in-depth counterpart, and each handler refusal supplies recovery appropriate to required/defaulting/no-change semantics. C9 remains the two-path `parentNodeName` exactness decision, not a universal lookup-field rule. Emitted variable/style/component field descriptions state the same action-sensitive contract and are pinned through official `tools/list`.
- [x] Make `create_text` reject `fontSize < 1` and weights outside `100|200|...|900`, use presence-safe callback defaults, and route post-placement font-load/font-size failures through the Phase 8 cleanup/disclosure path instead of swallowing them.
- [x] **Hand-off to Phase 12.** Rev 50 applied the corrected G1 success-boundary/cleanup-survivor wording; the same-stack micro-transient and Q33 destination-freshness records remain explicit Phase 12 tasks below.
- [x] **Unit Tests**
  - [x] Mocked call-trace tests: insertion is the immediate next synchronous call after every implicit creation; `create_text` inserts before font awaits.
  - [x] Q33: `create_instance` resolves its parent after the component import, and no `await` follows the destination read before the placement.
  - [x] Cleanup removes uncommitted nodes only on failed attempts; a six-creator adverse-path matrix pins primary-error preservation and exact survivor/current-parent/verified-parent disclosure when cleanup fails, with a real-dispatcher transport regression.
  - [x] Q32: `create_component` distinguishes full cleanup from a surviving component, treats unreadable source/child state as partial, and restores later children after an earlier failure while preserving sibling order.
  - [x] Q32: `create_component_set` continues restoration after a setter failure; classifies applied/restored/unrestored/removed/reparented/location-unreadable members; snapshots post-combine identity/location without masking the primary error; rejects a wrong returned parent; and turns optional projection failures into success warnings.
  - [x] Revs 51–52 adverse paths: a surviving component set keeps valid variant names (including unreadable parent type), member removal is tri-state, survivor parent evidence distinguishes located/detached/unknown, and hostile thrown Proxies still resolve to structured plugin/MCP errors. The corrected five-surface name matrix requires MCP rejection and plugin backstop rejection of explicit empty new names before creation/combine, plus unchanged omission defaults; it no longer encodes the false exact-empty host oracle.
  - [x] Revs 53–54 assignment paths: real handler tests prove pre-mutation refusal for node rename/group, collection mode, variable update, and component-property ADD/EDIT names while pinning each omission/default/no-change branch. Official-SDK calls assert exact `-32602` issue paths rather than treating schema parsing/private validation as the public boundary.
  - [x] Rev 54 first-call guidance: the official `tools/list` result describes required/non-empty/default/no-change semantics for the action-conditional variable, style, and component-property name fields; tests pin the emitted descriptions rather than only source registration objects.
  - [x] T78-14 independent oracle: scan direct source `.name` assignments plus an explicit inventory of recognized Figma naming APIs and compare them with the public action/field classification, including internal/derived sink disposition. A new direct write or call to a recognized naming API fails parity; a genuinely new naming API must be added to the scanner inventory. The expected classification is not imported from or generated by the production enforcement list.
  - [x] `create_text` boundary/handler tests reject zero, negative, and off-grid typography values, accept the minimum/ratified weights, and prove font configuration failure cannot return a false success.
  - [x] All eight handlers return `parentId`, all eight emitted output schemas advertise it, and `create_instance` both returns and explicitly advertises `componentId`.
  - [x] Final full-suite compatibility fixtures model the host evidence the repaired contracts require: `combineAsVariants` returns its set under the supplied parent, and `create_component` cleanup is asserted only with an explicitly live source plus an observably empty new component (T78-07/T78-08).

- **Phase 7–8 closure (2026-07-25, superseded by the Rev 46 re-verification below):** 28/28 dedicated Phase 7–8 tests and 856/856 full-suite tests passed at that point. Note on the "plugin-bundle check passed" claim: `check:plugin` rebuilds and then diffs against **HEAD**, so it necessarily fails while a phase is uncommitted — it cannot distinguish a stale bundle from uncommitted work, and its failure text ("out of date relative to its TypeScript sources") misreports that state. The bundle itself was and is in sync.
- **Rev 46 re-verification (2026-07-25):** full suite green at **863/863**; `check:types:plugin` and `check:suppressions` pass; rebuilding `figma_plugin/code.js` twice produces the same SHA-256 (`21f2fa5f499dddb0e867b12df295d6419e24f0a92883252a7b954c3490e84667`, updated from `5da8b5cc…` by the Q30–Q34 plugin edits).
- **Rev 50 re-verification (2026-07-27; superseded by Rev 51 below):** the then-current 28-finding subset in the [release changelog](release-changelog.md) was implemented; the full suite was green at **904/904** (**4,626 assertions across 47 files**), and `build:all`, `check:types:plugin`, `check:suppressions`, `check:generated`, `check:versions`, `check:plugin`, and `git diff --check` passed. The official SDK client confirmed the 46-tool registration surface, but repeated read-only joins to requested live channel `o4g6` returned `CHANNEL_NOT_FOUND`; its six then-live-dependent closure probes remained pending channel reconnection and were not represented as completed live verification.
- **Rev 51 repository re-verification (2026-07-27; empty-name conclusion superseded by Rev 52 below):** the bounded follow-up audit expanded the current ledger to **20 functional gaps, 12 testing gaps, 6 documentation errors, and 1 nitpick**. The full suite was green at **921/921** (**4,768 assertions across 47 files**), and `build:all`, `check:types:plugin`, `check:suppressions`, `check:generated`, `check:versions`, `check:plugin`, and `git diff --check` passed. The official SDK still advertised all 46 tools, but the final read-only `o4g6` join retry returned `CHANNEL_NOT_FOUND`; no Rev 51 live mutation occurred.
- **Rev 52 F78-15 live correction (2026-07-27):** channel `2476` completed the disposable Phase 7–8 matrix with **16/21 pre-fix checks passing**. Only the five explicit-empty creator/set-name checks failed; Figma normalized them to `Rectangle`, `Frame`, the text content, `Frame`, and `Component`, and an independent `node_rename` discriminator normalized `""` too. The contract, implementation, tests, and public docs now reject explicit empty new names before mutation at both boundaries while retaining omission defaults. Repository verification is green at **925/925** (**4,809 assertions across 47 files**) plus every build/static/bundle gate. Replacement channel `4mor` then passed all **10/10** post-fix F78-15 functional probes: the five explicit-empty calls were refused with a 59→59 descendant count after each, the five omissions returned `Rectangle`, `Frame`, `Text`, `Frame`, and `Component 1`, and deleting the disposable root restored the 59-descendant baseline.
- **Rev 53 Change 2 re-verification (2026-07-27; class-closure conclusion superseded by Rev 54):** F78-21/T78-13 brought the then-current ledger to **21 functional gaps, 13 testing gaps, 6 documentation errors, and 1 nitpick**. Repository verification was green at **932/932** (**4,839 assertions across 47 files**). Channels `a7ps`/`uy38` supplied the pre/post-fix node rename/group and whitespace evidence recorded in the release changelog.
- **Rev 54 Change 3 correction (2026-07-27):** the current ledger is **24 functional gaps, 15 testing gaps, 9 documentation errors, and 1 nitpick**—Change 3 adds three functional, two testing, and three documentation rows to Rev 53. Required/default/no-change recovery now also distinguishes style CREATE from style UPDATE, and the corresponding variable/style/component field descriptions are pinned at the official `tools/list` boundary. Focused five-file verification is **181/181** (**1,205 assertions**); the full suite is **941/941** (**5,005 assertions across 47 files**); `build:all`, all type/suppression/generated/version/bundle gates, and `git diff --check` pass (the version gate's synchronized `2.3.2` is expected before Phase 13). The final safety/guide contract rerun is **56/56** (**237 assertions**). The `9sxx` official-SDK pass began at 59 descendants / 12 collections / 10 variables, reproduced the collection-mode silent default and component ADD generic failure, and confirmed the already-correct `-32602` paths. A later 8/8 control pass independently read back component-property names of one and two spaces plus a one-space collection mode, confirming that non-empty whitespace is preserved. Initial collection cleanup hit the Figma host's 10-second timeout; a later official-SDK `variable_delete` retry removed `VariableCollectionId:1413:35`, `variable_list` returned no match, and final state returned to 59 descendants / 12 collections / 10 variables. No artifact or environment blocker remains. Three consecutive post-build rejoin attempts returned `CHANNEL_NOT_FOUND` before mutation, so post-fix live replay was unavailable and the newly repaired exact-empty paths are closed by repository/official-SDK tests without a false live claim.

---

## Phase 9: Peer-Bound Channel (D13)
- [x] **Peer-Bound Channel Protocol**
  - [x] The plugin's join message carries `clientType: "plugin"` and its build version.
  - [x] Update the socket server in [socket.ts](file:///Users/neozhehan/Git/figma-edit-mcp/src/socket.ts): assign each connection a random `peerId`; reserve a channel as **exactly one plugin peer plus one MCP session**.
  - [x] Implement pair-only command/response routing; responses are correlated by peer, and a response from any other peer is ignored.
  - [x] Implement the refusals at their delivery boundaries: an MCP joining with zero plugin peers ⇒ `PLUGIN_PEER_UNAVAILABLE`; a second plugin is refused `PLUGIN_PEER_AMBIGUOUS` at its own join and remains unadmitted; a second MCP client ⇒ `CHANNEL_IN_USE`.
  - [x] `channel_join`'s result includes `serverVersion` and `pluginVersion`; a known-unequal pair is refused with `VERSION_MISMATCH`. (Self-reported version check, not attestation — `SAFETY.md` claims it as exactly that, Phase 12.)
  - [x] Update `resetChannel` in [figma-client.ts](file:///Users/neozhehan/Git/figma-edit-mcp/src/mcp_server/figma-client.ts) to clear local state, perform a real socket-level leave that unbinds the pair, and close the bridge socket if leave acknowledgement cannot be confirmed. Recovery for `phase: "leave-previous-channel"` says the requested join was never sent and requires reconnect plus an explicit join; it never claims the previous channel remains held.
  - [x] Invalidate the binding when the bound peer disconnects; **every subsequent tool call is blocked until a successful rejoin**.
  - [x] Give every pending route a cancellable inactivity timer: progress refreshes liveness; terminal/replacement/detach/server-close paths clear the timer; expiry works without another MCP dispatch; and plugin-frame arrival rejects an expired route even if timer delivery is late.
- [x] **Scope-Ready Race Verification (Q11)**
  - [x] Confirmed: `ui.html` opened the socket immediately after posting `set-scope`, before plugin-main acknowledged committed permissions. The UI now waits for the matching one-shot `scope-ready` acknowledgement; stray, duplicate, and late acknowledgements cannot create a socket.
- [x] **Unit/Integration Tests**
  - [x] Peer-binding states: zero plugin peers, a second plugin at its own join, and a second MCP client — each refused with its code; refused peers remain unadmitted and receive no pair traffic.
  - [x] Missing `clientType` reserves neither slot and receives no pair traffic; padded plugin/MCP versions normalize at production ingest; version mismatch is refused and mismatch → detach → matching rejoin succeeds.
  - [x] Bound-peer disconnect invalidates the binding and blocks tool calls until rejoin.
  - [x] No cross-peer response acceptance: a response from an unbound peer never resolves a command.
  - [x] Both `channel_join` legs preserve a healthy predecessor; tests cover thrown and structured scope-payload failures, same-channel replacement, invalidated exclusion, cleanup-state persistence, and clean success output. Registered-callback regressions additionally cover explicit structured `UNKNOWN_ERROR` with and without a predecessor, absent/record/non-record/reserved-key-colliding details, stale origin `releasedChannel` on both legs with authoritative absent attempt context, and the integrated real-join → registered leg-2 failure → real-cleanup → `CHANNEL_NOT_BOUND` seam.
  - [x] An idle route expires without another MCP command; late progress/terminal frames are rejected on arrival, progress refresh preserves an active neighbour, and teardown cancels outstanding timers.
  - [x] A leave-previous failure closes the socket, leaves local state unbound, emits no requested join frame, and returns recovery naming both the previous and requested channel.

- **Phase 9 baseline closure (Rev 55, 2026-07-28):** the Q11 race was confirmed and repaired with a correlated post-commit `scope-ready` acknowledgement; the then-current focused matrix was **98/98** (**705 assertions**) and full suite **959/959** (**5,122 assertions across 50 files**), with all recorded gates passing.
- **Phase 9 adversarial review (Rev 56, 2026-07-28):** [Change 5](release-changelog.md#change-5-phase-9-adversarial-review-and-live-closure-revs-5657) found ten functional gaps, one testing gap, and three documentation errors. P9-F1/F3/F5/F6/F8/F9/F10, P9-T1, and P9-D1–D3 closed as recorded. The initial P9-F2 wire-leg disclosure, P9-F4 dispatch pruner, and P9-F7 cause branch were later proved incomplete and are superseded by Rev 58. Channels `8vj3`/`pt3j` supplied the historical live protocol/disconnect/rejoin evidence.
- **Phase 9 coded-block extension (Rev 57, 2026-07-28):** client-origin `CHANNEL_NOT_BOUND` raised the inventory to twenty-one codes plus the fallback. Channel `cgah` live-verified the wire-join refusal, coded follow-on block, and explicit rejoin. The audited pre-Change-6 baseline was **976/976** (**5,219 assertions across 50 files**) and the seven-file Phase 9 matrix **115/115** (**812 assertions**).
- **Phase 9 final review closure (Rev 58, 2026-07-28):** [Change 6](release-changelog.md#change-6-phase-9-closure-correction-rev-58) closes both-leg released-channel propagation, autonomous/arrival-enforced route expiry, truthful leave-failure recovery, explicit-`UNKNOWN_ERROR` preservation, non-record/colliding-detail preservation, and authoritative both-leg attempt context including absence, with the missing role/version/adverse-path and integrated-seam tests plus full contract synchronization. The reproducible seven-file Phase 9 matrix passes **130/130 tests with 914 assertions**; the full `src/mcp_server/tests` suite passes **991/991 with 5,321 assertions across 50 files**; and `build:all`, all type/suppression/generated/version/bundle gates, and `git diff --check` pass. Live `rhn9` is a 24/24 pre-Rev-58 baseline with 59→59 cleanup and is not claimed as proof of the injected leg-2/timer/leave failure paths.

---

## Phase 10: Page-Load Isolation (D14)
- [x] **Page-Load Wrap & Isolation**
  - [x] Wrap each page's `loadAsync()` individually across the multi-page read surface: `page_info` (pageIds), `node_info` traversals, `component_list`, `variable_list` consumer scans, and `annotation_list` by page.
  - [x] A failing page becomes a structured per-page error (`PAGE_LOAD_FAILED` / `PAGE_NOT_FOUND` / `TARGET_NOT_PAGE`) inside a shared `coverage` object (`{complete, pagesAttempted, pageErrors: [{pageId, error}]}`) while successful pages still return their data; invariant `complete === (pageErrors.length === 0)`, carried by the emitted JSON Schema as a two-branch union rather than a refinement the conversion drops (Rev 61, C8-D1).
  - [x] Single-page commands return the structured error **directly** instead of Figma's raw error string.
  - [x] **Change 8 (C8-F2):** a page that LOADED and then failed while being read raises `PAGE_SCAN_FAILED`, not `PAGE_LOAD_FAILED` — D9's distinct-cause rule, because the load-failure message's "retry the call" is the wrong instruction for a deterministic read failure.
  - [x] **Change 8 (C8-F1):** a `node_info` target dropped because its containing page failed is named in `pageFailedNodes: [{nodeId, pageId}]`, whose `pageId` joins to `coverage.pageErrors`. `missingNodeIds` keeps its narrow "not found" meaning; `page_info`'s `missingPageIds` becomes every requested ID absent from `pages`, whatever the cause (C8-D5).
  - [x] **Change 8 (C8-F4):** `coverage.pagesAttempted` is required, so `complete: true` after a document-wide scan is distinguishable from `complete: true` on a call that touched no page.
  - [x] **Change 8 (C8-F3):** the scope-payload load on `channel_join`'s second leg — the one page load left outside the coordinator — joins it and inherits the Q12 bound. The hand-rolled `DOCUMENT_LOAD_FAILED`, never in the D9 inventory, is retired.
  - [x] **Change 8 (C8-F5/F6/F7/F8):** the DOCUMENT-root `descendantCount` omission is pinned as a deliberate contract; the dropped root-parent check on page resolution is restored with a guarded root read; `getNodesInfo`'s outer catch uses `describeError`; and `PageLoadCoordinator.fail()` is typed `PageLoadFailure`, removing four `if (!failed.ok)` guards that could never be false.
- [x] **Bounded Per-Page Timeout (Q12)**
  - [x] Implement a single bounded per-page timeout for `loadAsync()`: a hung load becomes a structured `PAGE_LOAD_TIMEOUT` page error instead of wedging the serialized command queue. The timeout value is implementation-chosen and documented as **behavior, not contract**.
  - [x] A `loadAsync()` that settles *after* its timeout is provably ignored — the late result may not alter a returned response or authorize later work.
- [x] **Destructive Scan Protection**
  - [x] `variable_delete` (including collection mode and apparently empty collections) aborts **before any `remove()`** with `DOCUMENT_SCAN_INCOMPLETE` when any page could not be loaded and scanned — a page error can never mean zero consumers. Today's accidental fail-closed-by-thrown-error becomes an explicit, tested contract.
  - [x] **Change 8 (C8-C1):** the sibling outcome — a completed scan that finds consumers — is the coded `VARIABLE_IN_USE`, thrown with `details.variablesInUse`, not a non-error result carrying a bare `error` string. Top-level `error` is now the D9 envelope on every tool without exception.
  - [x] **Change 8 (C8-D4):** an apparently-empty collection still loads every page (so the gate above is unchanged in strength) but skips the per-node traversal, which searched for members of an empty set and could only ever find nothing.
- [x] **Unit Tests**
  - [x] One failing page yields partial data plus `coverage.complete: false` on reads.
  - [x] Variable and collection deletion fail closed with no mutation on incomplete coverage, including an apparently empty collection and one failing page among successes.
  - [x] A timed-out page yields `PAGE_LOAD_TIMEOUT` while other pages return their data; a late settlement is ignored (does not alter the returned result or enable later mutation).
  - [x] Single-page failures return structured codes, not raw Figma strings.
  - [x] **Change 8 regressions:** the dropped node is named with its page and joins to coverage; `PAGE_SCAN_FAILED` carries its distinct recovery text; node-mode annotation traversal failure surfaces the same code; the DOCUMENT `descendantCount` contract is pinned; a hostile thrown value cannot make `getNodesInfo`'s own reporting throw; the in-use refusal is coded, carries its consumers, and mutates nothing; the empty-collection path loads every page but walks none; the advertised JSON Schema carries the coverage invariant; and `pagesAttempted` separates the two meanings of `complete: true`.

- **Phase 10 closure (Revs 59–60, 2026-07-28):** [Change 7](release-changelog.md#change-7-phase-10-page-load-isolation-revs-5960) centralizes all Phase 10 page loads behind one per-command coordinator with a 10-second behavioral timeout, cached/settlement-guarded outcomes, and shared structured coverage across the five read tools. `variable_delete` now waits for every bounded page attempt and refuses incomplete load or traversal coverage before every variable/collection `remove()`, including an apparently empty collection. Live channel `08rz` reproduced real Page 2/3 load failures with Page 1 retained, then exposed an official-SDK `-32602` collision between the completed in-use refusal's `error:string` and D9's `error` envelope; Rev 60 preserves both advertised shapes and the post-fix live matrix passed without mutation at 59 descendants / 10 variables / 12 collections. The focused handler/schema/official-SDK matrix passes **94/94 tests with 872 assertions**; the full `src/mcp_server/tests` suite passes **1,009/1,009 with 5,421 assertions across 51 files**; and `build:all`, all type/suppression/generated/version/bundle gates, and `git diff --check` pass. Timeout, late-settlement, injected traversal-failure, and incomplete destructive-scan cases remain deterministic repository evidence; the apparently-empty-collection case was later verified live on `sx3b` (Change 8, C8-D6).
- **Phase 10 review closure (Revs 61–64, 2026-07-29):** [Change 8](release-changelog.md#change-8-phase-10-adversarial-review-and-remediation-revs-6164) reproduced Change 7's verification exactly and then found **nine functional gaps, one contract inconsistency, and six documentation errors**, all fixed above. C8-F9 was found by live-replaying C8-C1's own fix: the in-use refusal listed two different nodes as byte-identical text because it printed names without IDs, so its recovery named references the agent could not address. The inventory grows to twenty-three codes plus the fallback. The focused Phase 10 matrix passes **19/19**; the full `src/mcp_server/tests` suite passes **1,020/1,020 with 5,515 assertions across 51 files**; and `build:all`, all type/suppression/generated/version/bundle gates, and `git diff --check` pass at synchronized `2.3.2`. Live `sx3b` is pre-fix reproduction of C8-F4/F5/C1/D5; `8mvc`, `zhfj`, and `r3yk` closed C8-C1, C8-D1, C8-D4, C8-D5, C8-F3, C8-F4, C8-F5, C8-F9 (all three consumer kinds), and the empty-collection delete post-fix, finishing at the opening baseline of 59/31/2 descendants, 12 collections, and 10 variables. C8-F1/F2/F6/F7/F8 need injected faults and stay repository-verified. Rebuilding `figma_plugin/code.js` drops the bound plugin peer, so a fix found live must be replayed in a fresh session and the on-disk bundle must be checked by reading it, never with `check:plugin`, which rebuilds.
- **Change 8 closure correction (Rev 65, 2026-07-29):** [Change 9](release-changelog.md#change-9-change-8-closure-verification-and-remediation-rev-65) found and fixed two remaining functional boundary gaps, one testing gap, and four documentation errors. `get_connect_payload` now uses the private `details` key consumed by `channel.ts`, preserving coordinator diagnostics into public `channel_join.errorDetails`; PAGE direct-root validation fails closed with structured coverage when either root or parent identity is unreadable; and the hostile-value regression now reaches `getNodesInfo`'s actual outer catch. The focused Phase 10 matrix is **21/21 with 95 assertions**, the two-file Phase 10/connect seam matrix is **52/52 with 279 assertions**, and the full suite is **1,022/1,022 with 5,530 assertions across 51 files**. The prior `sfr5` official-SDK matrix remains the successful-host baseline; the corrected paths require injected faults and are not mislabeled as post-fix live evidence.
- **Change 10 closure correction (Revs 66–67, 2026-07-30):** [Change 11](release-changelog.md#change-11-change-10-verification-and-real-handler-seam-closure-rev-67) confirms Change 10's production projector is correct but closes its remaining test gap: the rejected-load and timeout regressions now invoke the actual `getConnectPayload()` in an isolated plugin process before forwarding its exact return through registered `channel_join`. Handler-local diagnostic stripping fails both tests. Focused connect is **33/33 with 191 assertions**, Phase 10 plus connect is **54/54 with 286 assertions**, and the full suite is **1,024/1,024 with 5,537 assertions across 51 files**. No production behavior changed and no fresh Figma channel is needed.

---

## Phase 11: Connector Repair (D12)
- [ ] **Connector Re-design (creation-only; Q13 revised hybrid — Design-file support kept)**
  - [ ] Make `create_connection` creation-only: remove the set-default and check-default modes, the `clientStorage` cache, and the silent `currentPage` auto-adoption (both scan sites, including the one inside `createConnections`).
  - [ ] Require `connectorId` **and** new `connectorName` on every call; resolve and verify (existence, exact current name, `CONNECTOR` type, in scope) **before any mutation**.
  - [ ] A call with no valid template returns the structured `CONNECTOR_TEMPLATE_REQUIRED` error whose message is the bootstrap UX: find a connector with `page_info`/`node_info` (pasting one from FigJam if the file has none) and pass its ID and exact current name.
  - [ ] Adopt the D7 envelope: per-item ordered rows, honest `status`, no unconditional `success: true`, and error rows never counted as created connections.
  - [ ] Apply D11 containment: verify the (derived) destination; insert as the immediate next synchronous operation; track every created ID; clean up uncommitted clones and cursors on later failure — a cursor created for one endpoint may not leak when the other endpoint fails.
  - [ ] Update the reaction prompt and guides to teach explicit template discovery instead of default management (guide sync itself is Phase 12).
- [ ] **Unit Tests**
  - [ ] A missing or name-mismatched template fails closed with no mutation; the bootstrap error carries its recovery.
  - [ ] Injected per-item failures clean up their clones and cursors (no leaks).
  - [ ] The aggregate never reports error rows as created connections; D7 envelope outcomes hold.
  - [ ] The Design-file clone path is covered by unit tests (the live probe is Phase 14 and fixture-dependent).

---

## Phase 12: Contract Sync (D8)
- [ ] **SAFETY.md**
  - [ ] Revise G2 to the universal existing-object rule (Q3 wording): *"No write against an existing object proceeds unless the caller-supplied current name matches the resolved object's actual name — nodes, variables, styles, and collections alike. Creation verifies the identified parent or collection instead."*
  - [ ] **Publish gate:** before publishing the revised G2, audit every write tool against the rule (the `safetyContract` mechanical diff provides most of it; the Part B matrix stays the per-tool proof).
  - [ ] Update A3, the "Name fields" bullet, and the Part B matrix rows for every changed tool (`variable_manage`, `style_manage`, the five creators, `create_component_set`, `node_flatten`, `node_clone`, `annotation_set`, the batch tools, `create_connection`, and the channel-binding surface).
  - [x] **From Phase 8 (D11), corrected by Rev 50:** G1 is an observable **success** boundary. A successful command never returns with its created node outside the verified destination; a failed cleanup may terminate with a survivor only when the initiating error carries mandatory partial-mutation and location evidence. The absolute “creation failures never leave orphan nodes” claim is withdrawn.
  - [ ] **From Phase 8 (D11) — residual risk 1:** the unavoidable same-stack micro-transient (an implicit Figma creator constructs on `currentPage` for part of one synchronous stack, before the append that contains it).
  - [ ] **From Phase 8 (Q33, Rev 46) — residual risk 2:** destination freshness. Every creator now resolves its parent as the last `await` before creation, so nothing yields between the destination read and the placement — but the scope/name/lock predicates were verified by the dispatcher one read earlier, so a rename or reparent in that dispatcher-to-handler hop is not re-detected. Record the deferred full synchronous parent-predicate re-assert and its escalation trigger (a reported or reproduced creation landing outside its verified destination).
  - [ ] Add the D13 claim ("peer-bound, self-reported version check — not cryptographic attestation").
- [ ] **Agent guides** ([skills/figma-edit/references/](file:///Users/neozhehan/Git/figma-edit-mcp/skills/figma-edit/references/): constraints, error-playbook, tool-selection, workflows)
  - [ ] State the G2 rule in exactly **one sentence** in the guides.
  - [ ] `error-playbook.md`: add an entry for every D9 code — the full header inventory plus every changed refusal (including the Q22 `PARENT_NAME_MISSING`/`PARENT_NAME_MISMATCH` pair, Rev 31, and Change 8's `PAGE_SCAN_FAILED`/`VARIABLE_IN_USE`, Rev 61). Retire the `DOCUMENT_LOAD_FAILED` row: that code no longer exists (C8-F3). No Phase 10 code has a row yet.
  - [ ] Teach the new batch `status` semantics (treat `partial_success` as incomplete; retry every non-success row — `failed` and `skipped`), the one shared row vocabulary in one sentence (every row carries `nodeId`/`status`/`error`, Q25), the annotation list-before-retry guidance (Q10), and explicit connector template discovery (D12).
  - [ ] D9 acceptance review per new or changed message: the correct retry is derivable from the error text and the tool list alone.
  - [ ] Mirror all guide changes to the `figma-edit://guide/*` server resources.
- [ ] **Unit Tests**
  - [ ] The `safetyContract.test.ts` mechanical diff passes in both directions after the `SAFETY.md` matrix updates.

---

## Phase 13: Version & Docs
- [ ] Bump the release version `2.3.2 → 2.3.3` on every surface the `check:versions`/`check:plugin` mechanism enforces:
  - [ ] [package.json](file:///Users/neozhehan/Git/figma-edit-mcp/package.json)
  - [ ] Root [package-lock.json](file:///Users/neozhehan/Git/figma-edit-mcp/package-lock.json)
  - [ ] **Both** version fields in [server.json](file:///Users/neozhehan/Git/figma-edit-mcp/server.json) (top-level `version` and `packages[0].version`)
  - [ ] Root [manifest.json](file:///Users/neozhehan/Git/figma-edit-mcp/manifest.json) (the versioned manifest; `figma_plugin/manifest.json` carries no version field)
  - [ ] The plugin About handshake
- [ ] Rebuild the plugin bundle using `bun run build:all`.
- [ ] Verify `check:plugin` and `check:versions` pass.
- [ ] Add the `CHANGELOG.md` entry for v2.3.3 naming **every** newly required field and breaking repair, each with a before/after example. The PRD's Compatibility posture section is the checklist: required `currentVariableName`/`currentStyleName`/`collectionName`/`scopes`; required parents on the five creators and `create_component_set`; the `annotation_set` field-set change; **`annotation_list`'s node mode returning the grouped `annotatedNodes` shape instead of the old flat `{nodeId, name, annotations:[{nodeId, annotation}]}`** (Rev 46 — the mode that previously matched its advertised schema); recursive strictness rejecting nested unknown keys; **`style_manage`'s per-variant strict `effects[]`** (Q35, Rev 47 — undeclared keys inside an effect are now rejected instead of accepted-and-discarded; progressive blurs and NOISE/TEXTURE/GLASS newly work end to end); `.min(1)` and duplicate refusal on batches; the batch handlers dropping their tool-specific count fields in favor of the shared envelope counts (Q26 — `nodesDeleted`/`replacementsApplied`/`annotationsApplied`/`totalCount`/`total*` removed); the new batch `status` field; the unified batch row vocabulary that renames `instance_set_overrides`' `instanceId`/`instanceName`/`message` rows to `nodeId`/`status`/`error` (Q25); `channel_join` version/peer fields and refusals; creation-only `create_connection` with the required name-verified template; the additive `create_instance` `componentId` response field (Rev 20); and Change 8's Phase 10 contract changes (Rev 61) — **`variable_delete`'s in-use refusal becoming the thrown, coded `VARIABLE_IN_USE` instead of `{success:false, error:"<prose>", variablesInUse}`** (before/after example required), `node_info` dropping `descendantCount` on a DOCUMENT root, `page_info`'s `missingPageIds` widening to every requested ID absent from `pages`, and the additive `node_info.pageFailedNodes` and `coverage.pagesAttempted` fields.

---

## Phase 14: Verification (Automated + Live Figma)
- [ ] Run and pass: `bun run build:all`, `bun run check:plugin`, `bun run check:versions`, `bun run check:types:plugin`, `bun run test` (full suite green: the 647+ existing tests as of v2.3.2 plus all new D5–D14 tests).
- [ ] Confirm the IDE opens the plugin source with zero spurious ambient-global diagnostics.
- [ ] **Track 1 rebuild-diff:** rebuilding `figma_plugin/code.js` after the Phase 1–2 commits produces no functional emitted-JS change (the residual fixes are types/casts/config only, confirmed via `check:plugin` rebuild + `git diff`); every other emitted-JS change must map to a deliberate D5–D14 edit, reviewed explicitly. Per review decision 3 (accepted 2026-07-17), the Track 1 expectation is strengthened from "no functional change" to **no code-line changes at all**: the Track 1 portion of the rebuild diff is comment-text only, machine-distinguishable from the deliberate D5–D14 edits.
- [ ] **Live Probes Matrix (against a live Figma document)**
  - [ ] ID-only `UPDATE_VARIABLE` call is refused.
  - [ ] `style_manage` update by ID without `currentStyleName` is refused.
  - [ ] Properties-only style update does not rename the style.
  - [ ] `CREATE_VARIABLE` without `collectionName` or `scopes` is refused.
  - [ ] `parentNodeName` omission is rejected at the server schema boundary (Phase 5 tests); a live **mismatched** `parentNodeName` returns the structured `PARENT_NAME_MISMATCH` refusal (coded per Q22, Rev 31; Q2 layer split).
  - [ ] `[]` and duplicate-node `node_delete` batches are refused before any mutation (schema boundary; both `1-2` and `1:2` spellings). Repeat for `text_set_content` and `instance_set_overrides` (P6-10).
  - [ ] A normal multi-item batch returns exactly one ordered row per input, with explicit `skipped` rows for unattempted items, and every row carries the shared `nodeId`/`status`/`error` vocabulary (Q25).
  - [ ] `instance_set_overrides` succeeds against a dynamic-page document (regression for P6-1: the main component is read via `getMainComponentAsync`, not the throwing sync getter) and an induced per-item failure returns a `partial_success` row carrying `partialMutation`/`before: {mainComponentId}` (P6-10).
  - [ ] A `text_set_content` batch with an induced mid-item failure returns truthful rows; a failure on the font-fallback path carries `before: {fontName}` while a clean failure carries no flag (Q24, P6-10).
  - [ ] Batch outputs carry only the shared envelope counts — no `nodesDeleted`/`replacementsApplied`/`annotationsApplied`/`totalCount` legacy fields (Q26).
  - [ ] An annotation append via `annotation_set` succeeds and is rediscovered via `annotation_list`.
  - [ ] A nested `node_flatten` result stays under its original parent.
  - [ ] `create_component_set` lands under its supplied parent.
  - [ ] `create_instance`'s response includes `componentId` matching the resolved component (regression for the Phase 8 dead-field fix; baseline: the 2026-07-17 `p28j` probe showed the advertised field absent).
  - [ ] `channel_join` reports `serverVersion` and `pluginVersion` for one bound peer; an empty channel is refused with `PLUGIN_PEER_UNAVAILABLE`, a second MCP client with `CHANNEL_IN_USE`, and a known version mismatch with `VERSION_MISMATCH`. Separately, a second plugin peer is refused `PLUGIN_PEER_AMBIGUOUS` at its own join, remains unadmitted, and receives no pair traffic; this code cannot reach `channel_join`.
  - [ ] `create_connection` without a valid template returns `CONNECTOR_TEMPLATE_REQUIRED`.
  - [ ] An explicit-template connection returns per-item truth. **Fixture-dependent:** requires a pasted FigJam connector in the test file; if unavailable, record the probe as blocked (per the Gap 9 precedent) rather than skipping silently.
  - [ ] **Manual plugin-UI check** (unreachable over MCP — only the UI's `validate-scope-link` path calls `parseNodeIdFromUrl`; MCP traffic is converted dash→colon server-side): paste a scope link using the percent-encoded node-id form (`node-id=1%3A2`) of a real node. If it validates, the sandbox provides `URL`: add a one-line ambient declaration beside `TextDecoder`'s and delete the suppression in `parseNodeIdFromUrl`. If it reports "Node not found" while the dash form validates, `URL` is absent: update the annotation to record the fact and defer the `decodeURIComponent` regex fix to post-release UI polish. Record the outcome in the PRD Provenance table either way.

---

## Follow-ups outside v2.3.3 scope (tracked, not scheduled)

- [x] **`SHADER` effect / typings pin (Q35 residual, decided 2026-07-26 — PRD Rev 48):** scheduled into the [v2.3.4 PRD](../v2.3.4/prd.md) as Track 2 (D8, Phase T2), not left as an unstated gap. Recorded facts: the pin is `@figma/plugin-typings@1.125.0`, `ShaderEffect` enters the `Effect` union at `1.131.0`, the live Figma runtime already accepts `SHADER`, the only other Effect-variant delta is an additive `noiseSizeVector?`, `check:types:plugin` reports **0 errors** against 1.131.0, and the Q35 parity test correctly goes red on the bump. Deferred out of v2.3.3 because the typings also generate `nodeFields.generated.ts`, so the bump changes which properties `node_info` can read — a diff that gets its own review. No escape hatch, in either release.
- [ ] **Server-side type-check gap (review decision 5, accepted 2026-07-17):** open a tracked follow-up (issue or next-release PRD item) for restoring an MCP-server type-check. Recorded facts: root `tsc --noEmit -p tsconfig.json` is currently unrunnable (TS6059 — `rootDir: "src"` is violated by tests importing `figma_plugin/` sources); CI runs no server `tsc` at all (`build` is tsup, which does not type-check); the Phase 4 `.superRefine()` enforcement and dual descriptions — the Q1/Q14 first-call-correctness mechanism — therefore live in unchecked code; and the single-argument `z.record` type error in `src/shared/nodeTypes.ts` sat invisible server-side until the plugin tsconfig surfaced it. Acceptance shape: a `check:types:server` gate analogous to `check:types:plugin`. The audit itself stays out of v2.3.3 per D4 — this item exists so the gap is tracked work, not an unstated non-goal.
